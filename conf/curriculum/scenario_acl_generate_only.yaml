defaults:
  - scenario_acl

scenario_acl:
  mode: mab_generate_only
  arm_space: generator
  use_mab: true
  use_scenario_buffer: true
  use_replay: false
  mab:
    num_arms: 7
    # Generator arms have no shared easy-to-hard ordering.
    initial_weight_decay: 1.0
