# @package _global_
defaults:
  - override /env: metadrive_base
  - override /agent: td3
  - override /planner: td3_mlp
  - override /preprocessor: identity
  - override /adapter: direct_action
  - override /reward: hybrid_rulebook
  - override /rulebook: baseline
  - override /experiment: rulebook_curriculum_auto

env:
  policy_mode:
    enabled: false
