# @package _global_
# Historical alias kept only for compatibility checks during migration.
# Prefer `presets/agent/td3_sb3` for new runs.
defaults:
  - /config
  - override /obs: lidar_state
  - override /agent/planner/encoder: none
  - override /agent/planner/decoder: td3_sb3
  - override /agent/planner/algorithm: td3_sb3
  - _self_
