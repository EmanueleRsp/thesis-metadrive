# @package _global_
defaults:
  - /config
  - override /agent/planner/algorithm: td3
  - override /reward: scalar_reward
  - override /curriculum: scenario_acl_scenarionet
  - _self_

# EVAL-PROTOCOL REQ-018: distinct comparison-block ID so this curriculum
# ablation arm is separable from BASELINE-SCALAR-01/EXTENSION-ALGORITHM-01
# and from its sibling ablation arms in the run registry.
analysis:
  experiment_group: ABLATION-CURRICULUM-SCALARREWARD-CURR-01
