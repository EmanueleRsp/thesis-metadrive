# @package _global_
defaults:
  - /config
  - override /run_profile: smoke
  - override /obs: lidar_state
  - override /agent/planner/encoder: none
  - override /agent/planner/algorithm: td3_sb3
  - override /reward: monitor_only
  - override /curriculum: disabled
  - _self_

# Keep the legacy test preset name distinct from the canonical `make run`
# entry point, whose default experiment name is `run`.
experiment:
  name: smoke
