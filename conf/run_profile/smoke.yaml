# @package _global_
run_profile:
  name: smoke

experiment:
  name: smoke
  total_timesteps: 2000
  log_interval: 100
  eval_interval: 1000
  eval_episodes: 2
  final_eval_episodes: 2

planner:
  learning_starts: 100
  batch_size: 32
  gradient_steps: 2

checkpoint:
  periodic_interval_steps: 10000
  keep_last_periodic: 1
