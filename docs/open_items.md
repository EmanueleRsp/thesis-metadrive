# Open Items

Single register of known-but-unfixed work. It exists because these were
previously scattered across "Notes" cells of `docs/project_index.md`, where they
were easy to lose: several have been open for weeks without being surfaced.

**Scope.** Items recorded in repository documentation, plus items opened by a
decision that has not yet been implemented. It is **not** an audit of the source
tree — an unlisted problem is not thereby absent.

**Convention.** `Blocking` means some approved contract is not yet met by the
code. `Deferred` means a decision was taken to postpone. `Open decision` means
the work is waiting on a choice the user has not yet made. Tick an item by
moving it to *Closed* with the date and the change that closed it. **Identifiers
are never recycled**: a closed `D2` stays `D2`, and the next new decision takes
the next free number.

Last reviewed: 2026-09-01 (second pass, after `RB51` closed). `D1` was briefly closed on 2026-08-20 and
**reopened the same day**: the value chosen presumed an episodic threshold, and
the mechanism is not chosen yet.

---

## Blocking — approved contracts not yet met

*(none open)*

| # | item | source | notes |
|---|---|---|---|

## Open decisions — waiting on the user

| # | item | opened | notes |
|---|---|---|---|
| D1 | **`τ₄` for the thresholded-lexicographic arm.** `τ₄ = 0` is wrong: exact L4 ties have probability zero, so at 0 the comparison stops at L4 on every pair and **L5 and L6 decide nothing**. A positive `τ₄` is load-bearing — it is the minimum progress a lane relaxation must buy to be worth taking. | 2026-08-20 | **Sequenced after the rulebook is frozen**, and not fixable before the algorithm is chosen: the object a threshold applies to differs by mechanism. *Absolute Thresholding* (Gábor et al. 1998) thresholds **Q-values**; *Absolute Slacking* (Li & Czarnecki 2019) thresholds a slack from the **state's own optimum**, which is already relative and therefore sidesteps the mission-length problem for free; a policy-gradient construction (Tercan & Prabhu 2024) compares **expected returns**, which is episodic. A rule phrased on the completion fraction presumes the third. Analysis kept: missions span 13–247 m, so no single absolute constant serves both ends under an absolute mechanism; `1 − success_route_completion_threshold = 0.05` is an already-declared constant available as a starting point **if** the episodic reading applies. Settled regardless: the **channel is not renormalized** (§4.1.1's rejection stands, scalar calibration untouched). |
| D4 | **Restart of the three production runs.** `sac-0` and `ppo-0` crashed on checkpoint reload and are stopped; `td3-0` remains at risk. | 2026-07-26 (ADR-026) | **No longer a decision about whether**: `γ = 1` (ADR-075), `speed_limit`, the four-to-six margin vector and the observation change break checkpoint compatibility by four independent approved routes, so nothing is resumable and nothing is comparable. **Sequenced, though** (user, 2026-09-01): the serious runs come *after* the A/B/C/D learnability tests — whether the rulebook+scalarization reward is learnable comparably to MetaDrive's native reward, and whether the ACL actually helps — and the ACL itself is not implemented yet. **Root cause of the original crash identified 2026-09-01** while running `V2`: see that item. |
| D9 | **Should PPO receive an ADR-078-style learner setting (minibatch `64 -> 128`, same samples in half the optimizer steps)?** | 2026-09-06 (ADR-078) | Deliberately **not** decided with SAC/TD3: PPO's update is synchronous (collect 2 048, then 10 epochs x 32 minibatches), not overlapped with the workers; the builder forces PPO onto CPU; and no PPO `step_timing.csv` exists under `lq_v3`, so it is unknown whether collection or update dominates. Decide from the first measured PPO run's `step_timing.csv`. PPO is more sensitive than SAC/TD3 to minibatch x epochs, so the "same samples, half the steps" argument is weaker there. |

| D10 | ([issue #6](https://github.com/EmanueleRsp/thesis-metadrive/issues/6)) **Removing the staged curriculum. Parked by user decision 2026-09-07** — the automatic (scenario-ACL) curriculum is the one that will be used, so the staged path should not be reached again; the question is recorded in the issue rather than acted on. Original framing: take it out, since the automatic curriculum is the one that will be used. The reason it should go is real — in staged mode the promotion gate does not use the frozen validation panels but samples with replacement from the arm/source cell, and PG `A3` holds **1** record and `A5` holds **2**, so a promotion is decided on one or two scenarios and is statistically a coin flip. | 2026-09-07, code audit (block B6) | **Not done in the C21–C28 change, deliberately.** Deleting the two Hydra presets removes the only coverage of `curriculum.staged` composition: `test_hydra_preset_run_configs.py::test_reward_variants_compose` asserts `staged.mode` and the stage count, and `::test_scenarionet_staged_curriculum_composes_with_semantic_arms` asserts the six stage names outright. AGENTS.md forbids deleting mandatory tests without explicit approval, and doing it inside an already broad defect-fix branch would have made the diff hard to review for no scientific gain — not selecting the preset achieves the same thing today. **Two ways to close it**, user's choice: (a) delete `conf/curriculum/stages.yaml` and `stages_scenarionet.yaml`, drop the two tests and repoint the five other call sites (two debug tools included) at `curriculum=disabled`, as its own change; or (b) keep the feature but make the promotion gate refuse a cell below a minimum scenario count, which kills the meaningless gate without removing anything. (b) is ~15 lines and loses no coverage. |
| D11 | **Pipelining the learner update against worker collection** (user-approved in principle 2026-09-07, estimated up to 1.8x). Deferred to its own change: it restructures the collection loop, and merging it with a defect-fix branch would make both unreviewable. The correctness argument stands — TD3/SAC are off-policy by construction, so collecting under slightly stale parameters is what they are for — and the real cost is bit-exact reproducibility, which survives if the **update:step ratio stays deterministic**; only *which* parameters generated a given transition becomes timing-dependent, and no claim rests on that. Same session: the user accepts running with `torch.use_deterministic_algorithms` off and re-enabling it for the final reported runs. | 2026-09-07 | Sequence after the reward/discount calibration lands, so throughput work is measured against the reward the thesis will actually report. |

| D12 | **PPO's rollout geometry does not divide, and `batch_size: 63` is almost certainly a typo for 64.** `conf/agent/planner/algorithm/ppo_sb3.yaml` sets `n_steps: 96` and `batch_size: 63`. At 20 environments the rollout buffer is 1920 and `1920 % 63 = 30`, so each epoch gets 30 full minibatches plus a 31st of 30 samples whose advantage normalisation is over 30 rows. **SB3 warns rather than crashing**, and under `C12`'s piped-run conditions the warning is invisible. `1920 = 64 x 30` divides exactly, and `D9` describes the intended geometry as "10 epochs x 32 minibatches" of 64. | 2026-09-07, code audit (block B, mechanical list item j) | **Not implemented: both halves need approval, and they are `D9`'s subject.** (a) `63 -> 64` is a change to a learner hyper-parameter with experimental impact, which AGENTS.md reserves for the user, and it belongs with the open `D9` decision about PPO's learner settings rather than in a defect-fix branch. (b) a fail-fast in `runtime/wiring/builders.py`'s `build_planner` — which already special-cases `ppo_sb3` and holds both `env.num_envs` and the resolved planner config — refusing `n_steps * n_envs % batch_size != 0` would make a currently-launchable configuration refuse to start; with (a) applied it is a no-op at the default geometry. **Latent today**: no shipped preset pairs PPO with 20 environments (`smoke_train_ppo.yaml` overrides to 8/4, and the only `num_envs: 20` presets are SAC/TD3), so nothing is currently mis-training. Decide (a) and (b) together with `D9`, from the first measured PPO run. |
| D13 | **`_recompute_current_max` rescans every priority, on the gradient-step path.** When the running maximum's multiplicity falls to zero, both the scalar and the batch bookkeeping rescan `raw_priorities` in full: measured **4.1 ms** per trigger at a 6M-element allocation, reached from `update_priorities` and therefore up to ten times per vector step. How often it actually triggers was **not** measured — it depends on whether the maximum is held by many freshly-primed rows (multiplicity high, rescans rare) or by a single re-prioritised one (rescans frequent), and that changes as the buffer fills with seen transitions. | 2026-09-07, found while fixing `C32` | **Not a defect and not fixed: a decision about what the priming value should be.** The textbook PER primes new transitions at the *maximum priority seen so far*, a monotone scalar that is O(1) and never needs a rescan; this repository deliberately tracks the **exact current maximum** instead, and `tests/test_transition_replay_per.py::test_per_insertion_priority_tracks_exact_maximum_across_updates_and_overwrite` is named for that choice, so relaxing it would weaken a mandatory test and needs explicit approval. Three options: (a) keep the exact maximum and accept the rescan; (b) carry a parallel max-tree beside the sum tree, making it O(log n) — a real data-structure addition, not a cleanup; (c) switch to the monotone running maximum, which is standard practice but changes sampling and voids that test's premise. **Measure the trigger rate first** — from a run's own `step_timing.csv` — because if it is rare, (a) costs nothing and the other two are unjustified complexity. **Widened by `C32`**: the rescan is now reachable from `add()` as well, once per masked data-abort slot that evicts the sole holder of the maximum, which puts it on the **collection** path and not only on the update path. That is the price of `C32`'s correctness, not a regression to undo — priming every unseen transition at a maximum the buffer does not contain was the worse of the two — but it means the decision above now covers both call paths. |

## Deferred — postponed on purpose

| # | item | source | notes |
|---|---|---|---|
| F2 | **Scalarizer binarisation (F0)** deferred to a separate ExecPlan. | Rulebook v4.10 ExecPlan | |
| F3 | **Calibrated stochastic tracking** deferred; ideal semantic tracking remains an explicit baseline limitation. | `OBS-V1.2` | |
| F4 | **`MOTORCYCLIST` collision class** deferred. | ADR-027 (`DEC-R1-07`) | **Restated 2026-09-01: not deferred pending a measurement — the measurement cannot be taken.** `DEC-R1-07` deferred this "pending an empirical check of motorcycle frequency in the selected scenario pool", but the frozen Waymo records carry exactly three object types (`VEHICLE`, `PEDESTRIAN`, `CYCLIST`; sampled over 12 records: 634/113/1), and **no motorcycle label exists in the source taxonomy**. You cannot count what the data does not label, so the gating check is unanswerable and the item cannot close on its own terms. Convert to a **declared limitation**: motorcycles, where present, sit inside `VEHICLE` and are priced with the car-occupant injury curve, which under-estimates their risk. Adding the class would require a classifier the dataset does not support. |
| F5 | **ACL implementation stage v3 not started, v4 deferred**; runtime learner smoke, live ScenarioNet/Rulebook wiring and resume validation pending before `VERIFIED`. | ACL ExecPlan | |
| F7 | **The PER sum tree is rebuilt leaf-by-leaf in Python at load.** `__setstate__` restores the leaves and walks the tree one leaf at a time. | 2026-09-07, code audit (block B, mechanical list item d) | **Deferred, with the measurement, because the blast radius is currently zero.** Measured **3.34 µs per leaf**, so 20.0 s for a full 6M-leaf buffer — the audit's "O(minutes)" is wrong by an order of magnitude. A bottom-up build (write the leaf slice, then 23 vectorised pairwise sums) is milliseconds. It is not urgent because `replay_persistence: false` on every non-smoke profile (`C19`), and the smoke buffers carry 24 000 rows, about 0.08 s. **One property to carry into that change**: the current rebuild is **already not bit-exact** with the tree it replaces — building one tree in insertion order and one in index order from identical priorities gives `array_equal → False`, total mass differing by 5.5e-12 — so bit-exactness is not an obstacle to vectorising it, but neither should the change be sold as preserving it. Sequence it with `V2`/`D11`, whichever turns replay persistence back on. |

## Known defects — identified, not fixed

| # | item | source | notes |
|---|---|---|---|
| C2 | **`SIGNAL` never selected at runtime on 209/828 (25.2 %)** of Waymo scenarios carrying `has_route_traffic_light=true`, after the route-membership fix reduced it from 57.1 %. | ADR-051 | A traffic-light rule that never fires on a quarter of the scenarios that have traffic lights. **25.2 % is an upper bound, not the defect size**: ADR-051 splits the 209 into 52 lost at adapter construction and 157 that are *"genuinely unrelated approaches **or** route ends more than one lane short"* — a mix of correct exclusion and recoverable shortfall that the single-hop version cannot separate. **Decomposed 2026-09-01** (`M8b`) on the 1100-record panel: `SIGNAL` 313 of 744 controls dropped by route reachability (42.1 %), **`STOP` 400 of 536 (74.6 %)**, 122 records with no selectable signal, and 13 569 controls lost earlier at adapter construction. So the route filter accounts for 122 records — the part `M9`'s multi-hop extension could recover — and adapter construction for the rest, a different defect with a different fix. **`STOP` is the worse case and was never measured before; this item should be restated to cover it.** Matters because `signal` sits at L3, above progress. **Parked by explicit user decision 2026-09-01**: revisit only if training runs and test animations show traffic lights are still not being identified. The corrective reading that made parking reasonable: `M8b`'s drop counts are *mostly correct* drops -- a four-way junction carries a control per approach and the ego uses one, which is why `waymo_static_adapter.py` calls them "correctly absent, not a data defect" -- and the selectable counts corroborate it (744-313 = 431 selectable signals against 409 records where the rule fires; 536-400 = 136 against 115). Only the **122** records that carry signals but can select none are suspect, and ADR-051 says even those mix correct exclusion with recoverable shortfall. |
| C3 | **M0 control-drop diagnostic counters** are collected but aggregated into no cross-episode report table. | Rulebook v4.10 ExecPlan | **This is `C2`'s missing reporting, not a separate defect.** `control_line_diagnostics(cache)` (`src/thesis_rl/rulebook/v2/transition.py:556`) already computes the split and is already static per scenario; only the cross-episode aggregation is absent. **Closed 2026-09-01**: the aggregation exists and its output is in the report (`control_line_coverage`). |
| C7 | ([issue #2](https://github.com/EmanueleRsp/thesis-metadrive/issues/2)) **The typed data abort works only on the vectorized path.** With `env.vectorized.enabled=false` a `RuntimeScenarioNotEvaluableError` is fatal instead of being handled as an ordinary scenario data abort, and the run then exits **139** (SIGSEGV) during teardown with the MetaDrive engine still alive. | 2026-09-02, diagnostic run during `AB-LEARN` `M2` | **Cause located, not fixed.** The conversion machinery lives entirely inside the subprocess vector environment: the worker catches the error (`deterministic_subproc_vec_env.py:145-147`) and hands the parent a `RuntimeScenarioDataAbort` marker (`:630`). `RuleRewardWrapper` only *annotates and re-raises* (`rulebook/v2/wrapper.py:277`), explicitly deferring the handling to "the parent-side truncation bootstrap". Remove the worker/parent boundary and nothing performs that bootstrap, so the exception reaches `train_loop.py:1565` and terminates training. Observed on `evaluate_signal_transition` (`components/controls.py:222`) rejecting a signal whose pre/post states are `UNKNOWN` -- an ordinary data condition that ADR-024 / `RSA-1` classifies as a typed abort, not a failure. **Does not affect the production runs**: every official configuration is vectorized, and the `AB-LEARN` smoke passed on both arms with 20 environments. It bites anyone who sets `vectorized.enabled=false` to get a cleaner traceback while debugging -- which is exactly how it was found, and it cost a diagnostic run. Two candidate fixes, neither attempted: hoist the abort conversion above the execution boundary so both paths share it, or declare the single-environment path unsupported for `env=scenarionet` and fail fast with a clear message at construction. The exit 139 is a secondary symptom of tearing down the engine after an unhandled exception, not an independent defect. |
| C8 | **A run's own provenance artifacts mislabel which rulebook produced it, and two artifacts of the same run disagree with each other.** `artifacts/run_metadata.yaml` recorded `rulebook.implementation_family: v1`; `checkpoints/*.reward_semantics.json` recorded `4.7-final-implementation-complete` for the same run; the run actually drives the **v2** machinery on `RULEBOOK-V5.1`'s six-level vector, and `specification_id` was `not-applicable` in both. | 2026-09-05, launch of `AB-LEARN` `M3` seed 0 | **Mostly closed 2026-09-06** by `C8-PROV` (`docs/implementation/rulebook_provenance_identity_bugfix_exec_plan.md`). *Cause*: `conf/config.yaml` declared only `rulebook.version`, and the two readers substituted **different** values for the undeclared keys -- `runtime/io/metadata.py:207` the literal `"v1"`, `contracts/reward_semantics.py:47` the version string -- while a third classifier, `runtime/wiring/builders.py:349-358`, routed that same string to the **v2** adapter. Three classifiers of one fact, three answers, and a missing key silently becoming a *wrong* record rather than an absent one. *Fix*: all three fields declared explicitly in `conf/config.yaml` as `v2` / `RULEBOOK-V5.1`, both writers routed through one shared resolver `rulebook_provenance`, and an undeclared field now reads `not-declared`. Verified by reproducing the divergence against the pre-fix tree before fixing it; 1616 tests pass. **Never behavioral** -- the instrument was always v5.1, which `_canonicalize_bounded` enforces by fail-closing on any margin vector that is not length 6. **Open half**: `RULEBOOK_V2_VERSION` still reads `4.7-final-implementation-complete` (`rulebook/v2/config.py:15`), deferred by `DEC-C8-004` because it is also stamped into cached rulebook-catalog provenance (`filter_rulebook_v2_catalog.py:321`), where a rename invalidates the cache. **Merge timing**: `DEC-C8-002` accepts that the change alters the fail-closed checkpoint-compatibility identity, so checkpoints written before it stop being resumable; merge after the in-flight screening runs, not during. **Check not run**: `make smoke`, because the GPU is saturated by the two in-flight runs; the change touches no reward, environment or learner path and the full suite covers both writers, so the residual risk is low. Run `make smoke` before merging. *Operational note for whoever tests in a worktree next*: `third_party/` are git submodules, so a fresh worktree has them empty and the suite fails to collect on `No module named 'metadrive'`; mount the primary checkout's `third_party/` read-only. |
| C9 | **The constrained-triangulation coverage check budgeted an aggregate against a per-piece constant, and killed a screening run.** `_constrained_components_after_ear_exhaustion` discards triangulation slivers at or below `AREA_EPSILON_M2`, then required the **sum** of what it discarded to sit inside that same constant. N slivers each individually inside the per-piece bound sum to as much as N times it. | 2026-09-06, `AB-LEARN` `M3` arm B | **Fixed the same day.** Killed arm B at **100 000 steps after 7 hours**, inside `vehicle_yield` conflict-zone occupancy prediction (`predict_conflict_zone_occupancy_intervals` -> `deterministic_convex_decomposition`). **Measured, not guessed**: the original message reported no number, so the magnitude was added first -- the `C6` lesson. Reproduced three times, shortfall **1.2x-1.3x** the bound from **two to four** discarded slivers on polygons of 111-261 m2, with the shortfall equal to the discarded area **exactly** and the decomposition never escaping the polygon. In absolute terms the rejected shortfall was **0.13 mm2 on 261 m2**, a relative error of **5e-7**. Fix: budget the shortfall **relatively** -- at most one part in 100 000 of the polygon's own area, scale-free from a 1 m2 to a 1000 m2 zone. Overshoot stays a hard failure at any magnitude: claiming area the polygon does not have is not a tolerance question. Both messages now carry the measured magnitude. **The per-triangle filter is untouched, so the change is inert on every input the pre-fix code could already decompose**: verified over **1107** generated polygons, 1107 identical decompositions, zero differences -- it converts crashes into results and cannot alter a number the previous code could produce. That property is what let the fix be applied to the live tree under the running arm A without contaminating it (user decision, 2026-09-06). Regression test `tests/test_constrained_decomposition_coverage.py`, two-sided: it raises the exact pre-fix error on the pre-fix tree, and separately pins that the guard **still** rejects a material shortfall and that overshoot stays fatal. **Residual risk**: the relative bound is a new constant chosen to clear the measured cases with margin, not calibrated from a specification; a decomposition losing more than one part in 100 000 now fails loudly rather than silently, but the threshold itself is engineering judgement. |
| C10 | **A failed run leaves its container hanging for hours, so `tmux` liveness and the tail of `train.log` both report it as still running.** Arm B recorded `run_failed` at 09:47:44 on 2026-09-06; its `tmux` session was still listed at 11:19 and the last line of `train.log` was a stale `chunk_started`, which reads exactly like progress. The container exited and flushed its log only at ~11:47, two hours later. | 2026-09-06, `AB-LEARN` `M3` arm B second failure | **Not fixed; the monitoring practice is.** Directly caused a **false status report to the user**: the run was described as progressing through chunk 2 while it had been dead for ninety minutes. Same family as `C7` -- teardown after an unhandled exception with the MetaDrive engine still alive, which there produced exit 139 and here a long hang. **The authoritative liveness signal is `"event": "run_failed"` in `logs/events.jsonl`**, not the `tmux` session and not the last log line; the supervision watcher was changed to key on it the same day. The underlying teardown hang is unfixed and its cost is bounded -- a dead run wastes GPU-free wall clock until someone notices -- but it makes every liveness check that relies on process or session presence unreliable, which is worth knowing before writing another one. **Bounded 2026-09-06 by `C12`**: after `run_failed` the failure handler now closes the evaluation manager and the training environment, flushes the logs and arms a 120 s watchdog that forces the exit (`run_forced_exit` event) if teardown hangs anyway; the hang's root cause in engine teardown is still not fixed, only its duration. |
| C11 | **Arm A of the learnability screening is not a valid control: MetaDrive's native reward, under the thesis termination convention, makes a standstill the return-maximizing behaviour within reach of the learner.** Measured on arm A's own `eval_episodes.csv` (seed 0, 25k-175k, 1 887 episodes, the Rulebook attached as instrument): on `validation_pg` 853 of 1 050 episodes are stationary (`route_completion` 0, every episode a timeout, identical mean length 244.77 at five of six evaluations, longitudinal acceleration within +-0.25 m/s^2); on both panels the episodes that do not move score about 0 and the episodes that move score -9 to -40. The worst episodes are -198.9 over 200 steps and -452.8 over 465 steps, that is -1 per step: MetaDrive's `on_lane_line_penalty` (line or `crash_sidewalk` contact) is applied every step after `no_negative_reward` has clamped the dense term at 0, and ADR-058's physical-exit termination never ends such an episode, so the penalty is unbounded while standing still in lane scores exactly 0. Arm A's Waymo `route_completion` is largely inherited momentum: the logged ego starts at 5.1 m/s on average (0.0 on every PG record), the policy brakes at -7 to -9 m/s^2 at every evaluation, and the median completion of 0.09 is the stopping distance. Consequences in training: the chunk return is flat at -11 to -19, `route_completion` falls 0.11 -> 0.08, and between 150k and 175k the critic loss grows 22 -> 506 with returns spanning 0 to -450. | 2026-09-06, `AB-LEARN` `M3` seed-0 readout | **Not fixed; instrumented.** It is a design error of the screening, not of MetaDrive nor of ADR-058: the native reward was calibrated for MetaDrive's route-relative termination (4 m lateral), where the per-step penalties last a few steps, and was adopted as a control without checking that it stayed well posed under a termination that lets line and boundary contact continue to the end of the scenario. The Rulebook is not exposed to the same optimum: L6 `progress_rate` charges a stopped ego 1 per step, and on the same episodes its raw channel sum ranks standing still *worst* (PG: -247 still, -229 moving). §7.3's learner-versus-reward reading is therefore not available from arm A as configured. Two instruments added: `tests/test_reward_return_ordering_runtime.py` (integration) replays the logged expert in the live runtime and asserts `still < partial < full` on the first scenario of each validation panel for both arm rewards -- a necessary condition the native reward *passes* (Waymo: 9.5 < 198.7 < 386.4), which is why it is not sufficient; and `scripts/evaluate_constant_action_baseline.py`, which evaluates `brake`, `coast` or `random` through the same panels and CSV schemas as a run, so every curve has its floor and the neighbourhood pathology (`random` far below `brake`) is measured before a launch. Floors measured 2026-09-06 (`seed=0`, full validation panels, native / scalar mean return): brake Waymo 3.33 / -1.06, PG 0.006 / -4.90; random Waymo -36.37 / -37.00, PG 1.18 / -3.16. On PG both rewards rank random above brake, so the standstill that arm A converged to on PG is *not* a local optimum of the native reward at the random-policy level there; on Waymo both rewards rank random far below brake, the Rulebook included, because a random driver collides in 13.5 % of the episodes, which is the intended ordering. The native-specific pathology is therefore the unbounded line-contact penalty on the *trained* policy's trajectories, not a property visible from a uniform-random policy alone; the per-episode `eval_episodes.csv` of the baseline runs is retained for that reading. Whether to make arm A a valid control (switch off the native per-step penalties, or terminate arm A on line/boundary contact) was decided 2026-09-06 as `DEC-AB-007`: arm A is relaunched **unchanged and relabelled** as the descriptive baseline "MetaDrive native reward under the thesis episode contract"; it is not a control, and arm B's frozen reward is not revisited on this evidence. |
| C12 | **A run launched through a pipe has no step counter anywhere, and a failed run has no bounded end.** `docker compose run ... \| tee` allocates no TTY, the Rich `Live` training monitor renders nothing on a non-terminal console, and its step counter, fps and EMA losses exist in no artifact (`AB-LEARN` §10 M3, 2026-09-05): between two evaluations the only exact step figure is the chunk boundary. Independently, `C10`'s hang after `run_failed` kept container, `tmux` session and GPU memory alive for two hours. | 2026-09-06, `AB-LEARN` `M3` seed-0 relaunch (user request) | **Fixed the same day, observability and lifecycle only; no training semantics touched.** (1) `train_vectorized` emits a TTY-independent progress snapshot every 1 000 collected steps and at chunk end (`runtime/training_progress.py`); `train_loop` appends it to `logs/events.jsonl` as `"event": "training_progress"` with `chunk_id`, `stage`, `run_env_steps`, `chunk_env_steps`, `fps`, `elapsed_seconds`, `episodes`, `ep_len_mean`, `ep_rew_mean`, `ema_actor_loss`, `ema_critic_loss`, `update_calls`, `gradient_steps`; when the console is not a terminal the same snapshot is printed as one plain line, so the `tee` log and the pane show progress. About 350 events per `medium` run. The non-vectorized `Agent.train` path is **not** instrumented. (2) `runtime/failfast.py`: the `run_failed` handler closes the async evaluation manager and the training env on a best-effort basis, flushes logging, and arms a daemon watchdog that calls `os._exit(1)` after 120 s, recording `run_forced_exit` first; the original exception is still re-raised, so nothing that observed it before observes anything different. Tests: `tests/test_training_progress_failfast.py` (11 tests: bucket/due semantics, plain-line format, `train_vectorized` emission through a two-slot fake env under a non-terminal console, disabled channel, close/flush/watchdog behaviour). **Verified before the fix**: the 2026-09-05 pair's `events.jsonl` carry no intra-chunk event and the 2026-09-06 arm-B run stayed alive two hours after `run_failed`. **Residual**: the forced exit is exercised by unit tests with an injected exit function, not by a real hung teardown; the 120 s grace and the 1 000-step interval are engineering constants, not configuration. |
| C13 | **ADR-071's not-at-fault truncation was dead code: `done_function` read an actor-less snapshot.** `_install_mission_runtime` installs a snapshotter with `actors=()` and `contact_onset_records=()` so the mission tracker can run without the Rulebook; `_install_rulebook_v2_adapter` built the actor-complete snapshotter for the Rulebook but never replaced `_mission_snapshotter`, so in `done_function` the onset list was always empty, `_collision_is_not_at_fault` returned `False` on every step, and **every** collision terminated with a zero bootstrap while R1 charged nothing for the not-at-fault ones — the exact "worse than the status quo" combination ADR-071's own comment warns about. | 2026-09-06, code audit (block A1) | **Fixed the same day.** The adapter installation now reassigns `_mission_snapshotter` and `_mission_pre_snapshot` to the actor-complete snapshot. No test covered the real installation path (the `done_function` tests inject the snapshotter by hand). Regression: `tests/test_audit_block_a_episode_contract.py::test_rulebook_adapter_installation_replaces_the_actor_less_snapshotter`, fails on the pre-fix tree. Consequence for past runs: the 14–19 % `crash_vehicle` rate in `train_chunks.csv` includes rear impacts on a stopped ego, all terminated. |
| C14 | **The env and the Rulebook classified fault on different states.** `done_function` committed the post-snapshot into `_mission_pre_snapshot` *before* calling `_collision_is_not_at_fault`, which then read the post-state ego and the post-state actors, while `evaluate_collision_impact` reads the pre-state ego and `pre_actors_by_id`. An agent crossing the 0.05 m/s stopped threshold or the ego's rear half-plane within one control step made one side truncate and the other charge, or vice versa. Separately, `_is_agent_ahead` took `max` of the axis-aligned bounds, i.e. half the **length** (~2.4 m) as the frontal band, so lateral impacts inside it were classified frontal and unconditionally at fault. | 2026-09-06, code audit (block A6) | **Fixed the same day.** The genuine pre-snapshot is captured before the commit and passed explicitly; actors are read from it; an actor with no pre-record but present post is treated as R1 does (appeared, not at fault); the frontal band is half the shorter side of the minimum rotated rectangle. Regression: `test_fault_is_classified_on_the_pre_state_like_the_rulebook`, `test_front_band_is_the_ego_half_width_not_half_length`, `test_front_band_does_not_inflate_with_the_ego_heading` (same file), all fail on the pre-fix tree. |
| C15 | **A geometry abort in vectorized ACL mode killed the run.** The ACL episode-end callback recognised only `runtime_scenario_data_abort`; a geometry abort also has no `acl_episode_id`, so `int(payload["episode_id"])` raised `TypeError`. | 2026-09-06, code audit (block A2) | **Fixed the same day.** `aborted_acl_slots()` classifies both typed aborts and any payload without an `episode_id`. Regression: `tests/test_audit_block_a_acl_geometry_abort.py`. Never observed in a run because no ACL run has hit a geometry abort yet (`GEOM-ABORT` landed 2026-09-06). |
| C16 | **`best_*`, `latest` and periodic checkpoints written after an asynchronous validation held the live learner's weights, not the evaluated ones.** `_record_async_evaluation` called `save_intermediate_checkpoints`, which did `agent.save(...)` on the live agent; a validation takes ~35 min, so the file labelled with step `g`'s metrics contained weights from `g + Δ`. The evaluated snapshot was deleted right after. | 2026-09-06, code audit (block A3) | **Fixed the same day.** `save_intermediate_checkpoints` takes `source_checkpoint_stem` and copies the evaluated snapshot (zip and every sidecar) via `_copy_checkpoint_snapshot`; the callback passes `job.checkpoint_stem`, which is still on disk because `on_complete` runs before `_release_snapshot`. Regression: `tests/test_audit_block_a_checkpoint_and_resume.py`. **Residual**: `latest_rng_state.pkl` is still the live RNG state, so a resume from `latest` is coherent in weights and step counter but not bit-exact; with `replay_persistence: false` it is refused anyway (`C19`). Every `best_*` checkpoint of runs before this fix is mislabelled by up to one validation of training. The ACL driver path saves `latest` synchronously and was not affected. |
| C17 | **Every chunk boundary in the baseline path reset all workers mid-episode.** `train_loop` called `train_vectorized` without `initial_observations`, so `env.reset()` ran at every chunk start. The last transition of each slot was already stored with `done=0`, so the n-step sampler chained it into the next episode's rewards and bootstrapped it from another scenario's observation (`2·n_envs` corrupted transitions per chunk, sampled early under `new_transition_priority: current_max`); `n_envs` partial episodes also vanished from `train_chunks.csv`. The ACL driver already passed `summary["last_observations"]`. | 2026-09-06, code audit (block A4) | **Fixed the same day.** The baseline path carries `last_observations` across chunks (and into the PPO overshoot collection); the staged-curriculum path still resets because stage transitions change env overrides. `tests/test_audit_block_a_chunk_boundary.py` pins that `train_vectorized` honours `initial_observations` without a reset; the loop wiring itself is a closure and is not unit-tested. |
| C18 | **`context_history` covered ~10 steps instead of 21.** MetaDrive builds the observation before the Rulebook wrapper publishes the committed context, and the wrapper rebuilds it afterwards, so `_append_timestamped_context_row` ran twice per step and appended two rows to a `deque(maxlen=21)`. The stale row also mixed the previous context with the live ego. | 2026-09-06, code audit (block A5) | **Fixed the same day.** A repeated build for the same `step_index` reuses the committed row. Regression: `tests/test_audit_block_a_context_history.py`, fails on the pre-fix tree. **Checkpoint note**: the observation layout is unchanged, but the *content* of the 21-row trace differs, so policies trained before the fix saw a different feature distribution. The double `observe()` itself (≈2× observation cost) is untouched and is a design item. |
| C19 | **Resuming an off-policy learner without its replay buffer silently trained on ~20 transitions.** `num_timesteps` is restored from the checkpoint, so `learning_starts` was already spent; with `replay_persistence: false` (every non-smoke profile) the first vector step ran 10 gradient steps of batch 512 on a buffer of `n_envs` rows, without any error. Present in **both** resume paths: the baseline loop and the scenario-ACL driver, which resumes through its own code (`train_loop.py:917` returns into it before the loop's own resume block). The ACL path is the one that can actually resume from a periodic checkpoint, because it writes `latest_checkpoint_pair.json` itself. | 2026-09-06, code audit (block A8) | **Fixed the same day** as a fail-fast: `require_replay_buffer_for_resume` (in `sb3_extensions/replay/config.py`, so both callers share one implementation) refuses the resume of a replay learner past step 0 when persistence is disabled. Whether to persist the buffer periodically (~7 GB per checkpoint) remains the `V2` decision. Regression: `tests/test_audit_block_a_checkpoint_and_resume.py`, including that both modules bind the same function object. |
| C20 | **PER could emit `inf`/`NaN` importance weights.** A prefix search can land on a zero-priority leaf (a masked data-abort slot, or float drift of the incrementally maintained sums) and the active-range wrap can move an index onto one; probability 0 gives weight `inf`, the max-normalisation turns the batch into `NaN`/0, and the critic parameters become NaN with no exception. | 2026-09-06, code audit (block A7) | **Fixed the same day.** Zero-probability addresses are redrawn from the positive mass (bounded retries, then a `ValueError` naming the inconsistent tree), and the weights are asserted finite. Regression: `tests/test_audit_block_a_per_zero_leaf.py` (forced zero-leaf hit and inconsistent-tree cases fail on the pre-fix tree). Not observed in a run; the stratified draw makes it rare, which is why it would have been a mysterious NaN rather than a reproducible failure. |

| C21 | **The horizon truncated on an emptied world, and the critic bootstrapped from it.** `scenario_time_limit_reached` stopped the episode at `episode_steps >= scenario_length`, but `ScenarioTrafficManager.after_step` despawns **every** replayed participant as soon as `episode_step >= current_scenario_length` (`third_party/metadrive/.../scenario_traffic_manager.py:111`, cleanup at `:142`), and it runs before `BaseEnv._get_step_return` builds the observation. Both counters hold the same value there (`before_step` increments `episode_step`, `_get_step_return` increments `episode_lengths`, `done_function` runs between them). So the final observation of a horizon-truncated episode — which is exactly the state its value target bootstraps from under partial-episode bootstrapping — was the only state in the whole training distribution with zero actors, and systematically the easiest one in it: no actor can violate L1 or L2 and the road ahead is clear. With `n = 3` the contamination spans the last three transitions, and with `gamma = 1` it propagated to `t = 0` undamped. The same step also earned a free maximally-safe reward. | 2026-09-07, code audit (block B1a) | **Fixed the same day.** The horizon is now `scenario_length - 1 + extra_steps_after_scenario`, i.e. the last step carrying logged data; `extra_steps_after_scenario` keeps its documented meaning (steps taken *after* the data ends) and now actually delivers it, where the frozen 0 previously still spent one step in the empty world. Regression: `tests/test_thesis_scenario_env.py::test_truncation_happens_before_upstream_despawns_the_traffic`, which pins the **ordering** against upstream rather than the constant. Pre-fix evidence: executed, the contract fails for every scenario length tried (L = 2, 10, 91, 199 all truncate at exactly the despawning step). **Episodes are one step shorter**, so returns are not numerically comparable across the fix. |
| C22 | **The terminal observation was preprocessed twice.** `Agent.train` preprocessed each worker's `terminal_observation` into `info`, `normalize_vector_transition_boundary` copied that value into `final_observations`, and the collection loop then ran the preprocessor over it again. The doubly-transformed value is what reached the replay buffer as the truncated transition's `next_obs`, i.e. precisely the bootstrap state of C21. | 2026-09-07, code audit (block B) | **Fixed the same day.** The two stages are now `_preprocess_terminal_observations` and `_attach_terminal_observations`, and the second takes **no preprocessor argument**, so the defect cannot be reintroduced by an edit that does not also change a signature. Regression: `tests/test_audit_block_b_terminal_observation.py` with a deliberately non-idempotent preprocessor; pre-fix evidence executed (the stored bootstrap row came out at 4x the raw value instead of 2x, with three preprocessor calls instead of two). **Latent, not active**: `conf/config.yaml` selects `agent/preprocessor: identity`, which is idempotent, so no completed run is affected. It would have become a silent numerical error the moment any normalizing preprocessor was selected. |
| C23 | **A typed abort was reported as a timeout.** An aborted episode has no success, no collision and no off-road, so the trailing `else` of the reason chain — spelled out inline at three sites — filed it under `timeout`. The timeout rate is the statistic used to judge how much of the training distribution reaches the horizon, and the abort rate sat at exactly zero beside it, so both were wrong in the same direction and neither showed it. | 2026-09-07, code audit (block B, promoted from non-blocking) | **Fixed the same day.** One `_episode_termination_reason` helper, `aborted` tested first, used at all three sites; the vectorized sites pass `idx in aborted_indices`. Regression: `tests/test_audit_block_b_termination_reason.py`. **Residual**: the numeric `timeout` column still means `truncated`, which an abort is; the abort count is now readable from `termination_reason` instead. Nothing was added to the CSV schema. |
| C24 | **Six encoder settings were configurable in YAML and connected to nothing.** `conf/agent/planner/encoder/lq_v3.yaml` ships `activation`, `dropout`, `attention_dropout`, `type_embedding`, `time_embedding` and `slot_embedding`; the factory passed none of them and `lq_encoder.py` hard-codes every one (ReLU in `_TokenProjection`/`_FeedForward`, `dropout=0.0` on both `MultiheadAttention` modules, embeddings constructed and added unconditionally). An override parsed, logged, reached the checkpoint's configuration record and changed nothing, so an ablation driven from those keys would have reported "no effect" for a knob that was never connected. | 2026-09-07, code audit (block B, promoted from non-blocking) | **Fixed the same day.** `_reject_non_core_latent_query_settings` refuses any of them that differs from the frozen value, in all four latent-query branches — the same discipline the constructor already applies to `num_latents` and `depth`. Regression: `tests/test_audit_block_b_encoder_frozen_settings.py`, which also pins the guard list against the shipped config and against what the module implements, so making a setting genuinely configurable forces the guard to be removed rather than left refusing a value that now works. **This is a false-negative risk in a results table, not a performance one**; no completed ablation is known to have used these keys. |
| C25 | **`train_reset_seed_unique_count` reported `0` under ScenarioNet.** The counter stays empty whenever no reset-seed function drives resets, which is the normal ScenarioNet case: the provider selects records by UID and MetaDrive's seed never enters the choice. A reproducibility column reading "zero distinct seeds" is worse than an empty one, because it looks like evidence. | 2026-09-07, code audit (block B, promoted from non-blocking) | **Fixed the same day.** `_unique_reset_seed_count` returns `None` — not applicable — instead of `0`. Regression: `tests/test_audit_block_b_termination_reason.py`. **Residual, deliberate**: the statistic that *would* be informative under ScenarioNet is the number of distinct scenario UIDs visited per chunk. It is collected nowhere; adding it means extending the runtime-stats counters in `envs/thesis_scenario_env.py`, `runtime/wiring/builders.py` and the CSV schema, and was left out of this change rather than done badly. |
| C26 | **The ACL bandit divided a per-step quantity by a per-episode one.** `compute_td3_learning_potential` returns the *mean* positive-part TD residual over an episode's transitions; `update_reward_scale` fed the EMA `abs(ep_return)`. `LP_scaled` therefore had units of 1/steps, so an arm whose episodes end early — the hard arms, where the ego crashes in the first seconds — scored a systematically higher learning potential for no reason connected to learning. Worse than the bias: an episode whose progress nearly cancels its violations has `abs(G) ~ 0`, and the clamp at `_MIN_REWARD_SCALE = 1e-3` then multiplies its LP by up to a thousand, so arms sitting near the cancellation point would have dominated the teacher on arithmetic alone. | 2026-09-07, code audit (block B5) | **Fixed the same day.** `update_reward_scale` takes `episode_steps` and stores `abs(G)/T`; both driver call sites pass it, and the vectorized ACL payload now carries `episode_length` inside `metrics` so both paths read it from one place. The state schema moves to `acl_ema_v4` because the field names are unchanged and a v3 checkpoint would otherwise load without complaint and normalize by roughly two hundred times too much. Regression: `tests/test_scenario_acl_mab.py::test_reward_scale_is_a_per_step_magnitude_not_an_episode_return` plus the extended legacy-schema rejection. **Verified separately and *not* a defect**: the LP attribution itself is correct — for TD3/SAC the residuals are computed at collection time and accumulated per `(slot, episode_id)` in `planners/core/lifecycle.py`, so they belong to the episode they are credited to. |
| C27 | **Any evaluation failure terminated training.** `AsyncEvaluationManager._fail` set `_fatal_error` unconditionally, so a single evaluator fault — an OOM while the learner happened to peak, say — discarded every hour that produced the checkpoint being measured. | 2026-09-07, code audit (block B4), user-approved | **Fixed the same day.** A failure is recorded and skipped, releasing its snapshot so the checkpoint directory does not grow by one evaluated model per failure; `max_consecutive_failures` (default 2) still aborts, because a persistently broken evaluator produces no comparable metrics and continuing would only spend the remaining budget. A success clears the streak. Regression: five cases in `tests/test_async_evaluation.py`; the original `test_evaluation_error_is_fatal` is preserved verbatim under the explicit `max_consecutive_failures=1`. |
| C28 | **The asynchronous evaluator had no device of its own.** It rebuilds from `job.cfg` and therefore inherited `device`, loading a second copy of the policy onto the training accelerator *while* the learner was mid-update — the observed out-of-memory. | 2026-09-07, code audit (block B4), user-approved | **Fixed the same day.** New `eval_device` key, null by default so no existing run changes behaviour; `resolve_evaluation_device` is applied in the worker before anything is built. Because the evaluator is concurrent with training rather than on its critical path, `eval_device: cpu` usually costs nothing in wall-clock. Regression: three cases in `tests/test_async_evaluation.py`. |

| C29 | **The checkpoint reward identity carried `priority_base` but none of the weights beside it in the same formula.** `build_reward_semantics_identity` recorded the mode, the schema id, the base, the sigmoid sharpness and the tolerance, and omitted `severity`, `flat_tie_breaker`, `progress_weight`, `relaxable_weight`, `progress_rate_weight`, `step_dt_s` and `reference_time_s`. Harmless while every one of them was frozen; ADR-079 turns `severity` into a selected parameter, at which point a checkpoint trained at `σ = 0` would resume against `σ = 0.30` with no complaint and one set of curves would carry two different rewards. | 2026-09-07, found while applying ADR-079 | **Fixed the same day**, as part of the change that made it matter. All seven fields join the identity; an undeclared key stays `None` rather than defaulting, which is the `C8` rule. Regression: `tests/test_audit_block_b_reward_identity.py`, parametrized over every field plus a separate case for `severity` (the parametrization alone would still pass if someone removed `severity` and left the other six). Pre-fix evidence is structural rather than executed — the keys did not exist, so no value of them could change the identity — and visible in the diff. **Consequence**: the identity of *every* run changes, so checkpoints written before this commit are not resumable. That break is already unavoidable through `priority_base`, and `DEC-C8-002` accepted the same kind of break for the same reason. |

| C30 | **RULEBOOK-V5.1 §7's episode counters were never cleared at `reset()`.** `_l4_clip_binding_steps`, `_l5_reached_steps`, `_ego_speed_sum_mps` and `_ego_speed_steps` were initialised in `__init__` and nowhere else, while one `RulebookV2MonitorWrapper` instance serves **every** episode of its slot — the reset path rebuilds memory, cache, adapter and snapshot but left the counters alone. Each of the three §7 diagnostics is then unreadable in a different way: `l4_clip_binding_steps` exists to be **zero** (a non-zero value says the 80 km/h cap was overridden and the Test A figures no longer bound what the agent can earn), so one override reports forever; `l5_reached_steps` grows with the slot's age rather than with the episode, which destroys exactly the "does L5 ever decide" reading §1.1 needs; and `mean_ego_speed_mps` averages scenarios the episode never visited, against a requirement (limitation 13) to report the mean **per source**. | 2026-09-07, code audit (block B, mechanical list item a) | **Fixed the same day.** Both `__init__` and `reset` now call one `_reset_episode_counters`, so a counter added to one of them cannot silently survive an episode boundary — the `C22` discipline, applied to state instead of to a signature. Regression: `tests/test_rulebook_v51_diagnostics.py::test_reset_clears_the_episode_counters`, which drives three steps, resets into an episode with a different ego speed, and asserts all four counters restart; pre-fix evidence executed, `assert 4 == 1` on `l4_clip_binding_steps`. **Latent, not active**: verified 2026-09-07 that no writer in `runtime/` or `analysis/` reads any of the three keys — they are published in `info` and consumed by tests alone, so no artifact or reported figure is wrong. The §7 aggregation that *would* consume them, `mean_ego_speed_by_source`, is not implemented anywhere; `REQ-RB51-19` is met at the wrapper and unmet in the report, which is a separate gap and is **not** closed by this fix. |

| C31 | **The vectorized ACL chunk boundary did not persist `recent_usefulness`, so a resumed teacher normalized against nothing.** The two chunk-end writers of `scenario_acl_state.json` had diverged: the non-vectorized one wrote the window, the vectorized one — **the production path**, since every serious profile runs 20 environments — wrote eight keys and omitted it, and the loader's `[float(v) for v in state.get("recent_usefulness", [])]` turned the omission into an empty window with no complaint. `_normalize_learning_potential` returns `1.0` whenever it has nothing to rank against (`len(rank_set) <= 1`), so the **first** post-resume episode fed the bandit a maximum whatever its true learning potential, and episode *k* ranked on a grid of step `1/(k-1)` until `recent_window_size = 100` episodes had refilled. At `mab.alpha = 0.10` each spurious 1.0 moves the pulled arm's EMA by about a tenth and decays over roughly ten updates, so the teacher's first decisions after a resume were noise on top of arm scores that had themselves survived correctly. | 2026-09-07, code audit (block B, mechanical list item c) | **Fixed the same day.** One `_scenario_acl_resume_state` builds every field the loader reads, and both writers now call it — the non-vectorized site keeps its extra diagnostic keys by spreading the shared payload and adding them, and the three fields it used to repeat after the spread (`mab`, `coverage`, `rng_state`) are gone, since repeating them is the drift this fix removes. Regression: `tests/test_scenario_acl_resume_state.py`, which round-trips a distinctive value through **every** field the loader reads rather than the window alone, plus a second case pinning why the window must survive (an empty window scores the first episode as a maximum by construction). Pre-fix evidence executed: the exact eight-key payload the vectorized path wrote restores `recent_usefulness == []`, length 0, with no error raised. **No resume-time warning was added** for a state file that lacks the key: such a file can only come from a run written before this branch, and `C29`'s reward-identity change already refuses those checkpoints, so the branch would be unreachable. **Narrower than the audit claimed** — the audit said "an ACL resume loses the window"; the non-vectorized path never did. |

| C32 | **A masked data-abort slot evicted the running maximum without releasing it, and the stale value was permanent.** The masked branch of `PrioritizedNStepReplayBuffer.add` wrote `raw_priorities.flat[i] = 0.0` and the leaf directly, bypassing `_set_raw_priority` and therefore `_update_current_max`. The sum tree stayed correct; only `_current_max_count` missed its decrement when the overwritten row was the one holding the maximum. The error is **sticky, not transient**: `_update_current_max` decrements only when the overwritten value equals the recorded maximum, so once no row holds it the count can never reach zero and the rescan that would repair it never runs — and `_set_raw_priorities_batch`'s `retained_max_count` keeps the stale value alive for the same reason. `_insertion_priority` then primes **every** unseen transition at a maximum the buffer does not contain, over-weighting it in sampling by `(stale/true)**alpha` with `alpha = 0.6`, for the rest of the run. It also survives a resume, because `__setstate__` restores both counters from the pickle. | 2026-09-07, code audit (block B, mechanical list item b) | **Fixed the same day.** The masked branch reads the previous value and calls `_update_current_max(previous, 0.0)`, so both branches share one bookkeeping path. A second boundary had to be closed to make that safe: the rescan now treats "no positive priority left" as the empty-buffer state (`1.0`, count 0) instead of recording a maximum of `0.0`, which `_set_raw_priority` would reject on the next valid insertion — reachable when a whole buffer cycle aborts, the condition `C7` describes. Regression: `tests/test_transition_replay_per.py::test_per_masked_overwrite_releases_the_maximum_it_evicts`, two-sided (the eviction case and the all-aborted cycle). Pre-fix evidence executed: `_insertion_priority()` returned **9.000001** against a true maximum of **1.0**. **The audit's direction was wrong** — it reported that the accounting "silently under-counts"; it over-states the maximum. **No completed run is affected**: the defect needs a ring wrap to overwrite a priced row, which at capacity 300 000 over 20 environments is 300 k env steps, and the `AB-LEARN` screening stopped near 175 k. Every serious run (`D4`) would have been. |

| C33 | **Every reset built the static adapter result twice and discarded the second.** `_install_causal_observation_builder` called `_build_static_adapter_result(scenario, record)` eagerly, though `_install_mission_runtime` had already built it from the same scenario and record and kept it in `_mission_static_result` — and `_get_reset_return` runs the mission installer, then the Rulebook adapter, then this builder, unconditionally and in that order. The second result was used only as the `else` fallback of the `route_lanes` choice, and on the production path that branch is never taken, because the Rulebook's `initial_cache` is installed by then and wins. So the work was not merely duplicated, it was **thrown away**. | 2026-09-07, code audit (block B, mechanical list item e) | **Fixed the same day, throughput only; no number changes.** Both fallbacks now read `self._mission_static_result`, which is the raw result the fallbacks want — `_mission_episode_cache` is the elevation-aligned one and is a different object on purpose. An explicit precondition error replaces the silent rebuild, because this file has a history of installation-order defects (`C13`). **Measured, not estimated**: 61 ms per reset on `sd_waymo_training_20s_104202f4f2590dff` (234 map features, 109 lanes) on the persisted-route branch, 254 ms on the map-matching branch. With 20 slots and ~200-step episodes at least one slot resets on ~9.5 % of vector steps, and a vector step waits on its slowest worker, so about 5–6 ms of a ~179 ms step, ≈3 %. Regression: `tests/test_thesis_scenario_env.py::test_causal_builder_reuses_the_mission_static_adapter_result`, which counts constructions and requires **zero**; the existing installer test keeps its assertion and gains the stored result in its fixture. Pre-fix evidence executed: the counter recorded one construction. **Residual risk**: the fallback now shares one object with the mission runtime and the Rulebook cache instead of holding a private copy. That is exact only because the two functions between them are purely functional — `build_episode_cache` constructs a new `EpisodeCache` that *shares* `route_lanes` by reference, and `align_episode_cache_to_live_elevation` builds shifted `RoutePolyline`s and `replace`d lanes rather than mutating them, verified by inspection when this fix was written. An edit that made either mutate in place would silently give the fallback elevation-aligned geometry where it wants raw. Note the production path is unaffected either way, because the Rulebook cache wins the choice; only a Rulebook-disabled configuration reads the fallback at all. |
| C34 | **The latent-query tokenizers asserted an invariant that holds by construction, and paid a GPU synchronisation per forward for it.** `tokenize_structured` ended with `if not valid_mask.any(dim=1).all(): raise ...` in both the v2 and v3 encoders. `valid_mask` is a `torch.cat` in which three of the concatenated blocks are literal `torch.ones((batch_size, 1))`, so every row carries at least three valid tokens whatever the data masks contain: the branch cannot be taken. Reading the result into Python nevertheless forced a device-to-host synchronisation on the critical path. | 2026-09-07, code audit (block B, mechanical list item f) | **Fixed the same day, throughput only; the branch was unreachable, so nothing behavioural changed.** Both guards removed. **Measured on the GH200**: the host read costs **14 µs** on top of the device-side reduction, and identically at batch 20 and batch 512 — the signature of a fixed synchronisation overhead rather than work. `td3_sb3.yaml` sets `share_features_extractor: false`, so SB3 runs about six extractor forwards per gradient step, and at 20 environments with `update_to_data_ratio` 0.5 that is ten gradient steps per vector step: **~61 forwards, ~0.85 ms per vector step** of a ~179 ms budget, about 0.5 %. End-to-end forward timings were **not** usable as evidence — the GPU was saturated at 100 % by the user's live run and the deltas swung by ±1.7 ms, negative included — so the isolated synchronisation cost is what is claimed here. Coverage: the invariant was **already** pinned on an all-zero observation by `tests/test_encoders_v10.py` and `test_encoders_v11.py`, which is the executed proof that the guards were unreachable; both now assert `mask.any(dim=1).all()` explicitly, tying the invariant to the removed check. **Left in place deliberately**: the second synchronisation per forward, `torch.isfinite(flat_obs).all()` in `encoders/base.py`, is the NaN/inf check AGENTS.md requires. Removing or gating it is a user decision, not a cleanup. |

| C35 | **Seventeen of the twenty keys in `conf/obs/semantic_v3.yaml` were connected to nothing.** Only the three signal-camera keys have a reader (`envs/factory.py`, the deliberate `C5`/ADR-045 threading). The token counts and history lengths are frozen inside `SemanticObservationSchemaV12` (`flat_dim = 3011`, frozen by `OBS-V1.3.1`); the radii and the prediction horizon are literals in the builder kwargs of `envs/thesis_scenario_env.py`; the LiDAR geometry is a literal the factory itself writes into `vehicle_config`. `interaction_prediction_horizon_s` is even spelled differently from the kwarg it corresponds to (`prediction_horizon_s`), which is part of why the disconnect went unnoticed. Three of the seventeen are the **causality declarations** of `OBS-V1.2` — `future_ground_truth`, `other_agent_navigation`, `future_disambiguation`, all `forbidden` — so the configuration could request a leak the contract forbids and be silently ignored. | 2026-09-07, code audit (block B, mechanical list item h) | **Fixed the same day**, as `C24` one layer down and with `C24`'s remedy: `_reject_divergent_semantic_v3_settings` refuses any of the seventeen whose value differs from the frozen one, so the YAML keeps documenting the observation accurately and a divergent override fails loudly instead of reporting "no effect" for a knob that was never connected. The three ADR-045 keys stay overridable and a test asserts the guard does not cover them. Regression: `tests/test_audit_block_b_semantic_v3_frozen_settings.py`, which loads the **shipped file** rather than a copy of it and pins four things — every frozen key rejects a divergent value, the shipped config still installs the observation, every shipped key is either threaded or guarded (so a new key cannot be added unnoticed), and no frozen key reaches the environment configuration (so if one ever becomes threaded the guard must be dropped rather than left refusing a value that now works). Pre-fix evidence executed: overriding `dynamic_radius_m`, `context_history_length`, `route_tokens` **and** `future_ground_truth: allowed` together raised nothing and produced a **byte-identical** environment configuration. **Two corrections during review, both recorded because the second is a lesson.** (1) The comparison was `int(value) == frozen` for integer-valued keys, so `history_length=5.4` truncated to the frozen 5 and was accepted — silent acceptance by the guard built to refuse it; it now compares as `float`. (2) A first attempt also **required** the three causality declarations to be *present*, on the `C8` reasoning that an absent one reads as compliant. That was the wrong layer and the full suite caught it: `tests/test_semantic_state_v3.py::test_factory_exposes_v3_as_an_explicit_observation_type` calls `_configure_agent_observation(env_cfg, {"type": "semantic_v3"})`, a legitimately partial config, and a new precondition on the runtime function broke it. The risk that actually exists is a *config file* omitting a declaration, so it is pinned over `conf/obs/*.yaml` instead — every file selecting v3 must declare all three as `forbidden` — which covers more than the runtime check did, since it inspects every config rather than the one a given run selects. **The focused test selection missed it**: `test_env_factory.py` was run and `test_semantic_state_v3.py` was not, which is precisely the coverage argument for the full suite being the gate. |
| C36 | **A payload that is static per scenario was pickled through the worker pipe on every step, and the route that was supposed to carry it was dead.** `rulebook_control_line_diagnostics` is computed once at `reset()` from the episode cache, and the wrapper put it into **every** step's info dict. The evaluation loop's `metadata_keys` reads the key from both the reset info and the last step info, and only the second ever fired, because `RulebookV2MonitorWrapper.reset` returned the inner info unmodified — so the mechanism the exec plan describes ("same mechanism as `scenario_uid`", and `scenario_uid` does arrive in reset info) never worked, and the terminal-step read saved the feature by accident. | 2026-09-07, code audit (block B, mechanical list item g) | **Fixed the same day.** Published in `reset()`'s info and removed from the step info; the only consumer keeps working, now through its intended path. **The audit filed this as throughput and that part is negligible** — measured at 5 integer keys, 195 bytes pickled, 3.9 KB per vector step across 20 workers, noise beside `rulebook`, `rule_components` and `scalarization` in the same dict. The finding worth having is the dead read. Regression: `tests/test_rulebook_v51_diagnostics.py::test_control_line_diagnostics_are_published_at_reset_not_on_every_step`, two-sided (present in reset info, absent from step info); it fails on the pre-fix tree on both halves. **Residual risk, and it is the mirror of the defect**: the key now travels *only* in reset info, and an SB3-style `VecEnv.reset()` returns observations without info. The one consumer (`agent/agent.py`'s evaluation loop) uses a raw `gym` env — it unpacks `obs, reset_info` from `reset()` — so nothing is affected today, and `control_line_coverage` in the §7 report is built by `scripts/measure_expert_rulebook_transition.py` from its own accumulators rather than from this key, checked when this fix was written. But if evaluation ever moves onto the vectorized path the key disappears silently, and **no test covers that boundary**: exercising the wrapper through the deterministic subproc vec env was judged disproportionate for a consumer that does not exist. |

## Verification debt — checks not run

| # | item | notes |
|---|---|---|
| V1 | **`make smoke` not run** since ADR-058's episode-contract change, and not run after `γ = 1`. The discount change alters value-target scale, which is what an end-to-end smoke test would surface. | **Partly retired 2026-09-01**: `make smoke` passed under `γ = 1` and the six-level reward after `RB51`/`M6` fixed the scalarization config. It could not have passed before — the four-margin default would have raised on the first step. **Closed 2026-09-01**: re-run after `M3`, `M4`, `M5`, `M7` and the §7 diagnostics landed — `make smoke` exit 0 on the finished six-level rulebook under `γ = 1`. |
| V2 | **Transition replay**: source-backed learner smoke and checkpoint/resume remain pending. | `TRANSITION-REPLAY` ExecPlan | **Half closed, half converted into a finding, 2026-09-01.** The *source-backed learner smoke* is **PASS**: `make smoke` runs `td3_sb3` with `transition_replay.enabled=true, prioritized=true, n_steps=3` against frozen ScenarioNet records, and completed exit 0 with both evaluations. The *checkpoint/resume* half was run and **failed for a structural reason worth recording**: `TRANSITION-REPLAY` v1 hard-codes `persistence.trigger = final_or_manual` and forbids `periodic_frequency_steps` (`sb3_extensions/replay/config.py:73-75`), so the replay buffer is paired only with the **`final`** checkpoint. The periodic `latest` checkpoint has no buffer, and resuming from it raises *"Replay continuation requires a checkpoint pair manifest"*. **A crashed run never reaches `final`, so it can never resume with replay continuation** -- which is almost certainly the `sac-0`/`ppo-0` failure recorded in `D4`. **The mechanism itself is sound**: resuming the same run with `checkpoint.resume.checkpoint_name=final`, which does have a paired buffer, completed **exit 0**. So the defect is not the resume path but *which* checkpoint carries a buffer. The code fails closed with a clear message rather than resuming on an empty buffer, so this is a design limitation, not a bug. Note the ACL driver *does* write `latest_checkpoint_pair.json` periodically (`curriculum/scenario_acl/driver.py:1256`), so runs with the curriculum enabled are not affected; the smoke ran with the curriculum disabled. **Decided 2026-09-01 (user): park it.** Mid-run replay continuation is a convenience, not a requirement — a crashed run restarts, and with the ACL enabled the periodic pair is written anyway. Revisit only if crashes prove frequent enough that losing the buffer costs real time. The failure mode is now documented rather than mysterious, which is what `D4` was missing. |

---

## Closed

| # | item | closed | how |
|---|---|---|---|
| D2 | **L6 rewards speed, and PG admits no speed limit.** | 2026-08-20 | Decided: **accept**. PG blocks are synthetic, so there is no traffic law to encode and MetaDrive's design speeds are simulator parameters, not norms; inventing a PG limit would be the unmotivated calibrated constant this project rejects elsewhere. `offroad` at L3 constrains reactively, and it is a difference between *sources*, not between *arms*, so it does not confound the comparison. Recorded as `RULEBOOK-V5.1` limitation 13, with a new §7 diagnostic `mean_ego_speed_by_source` so the divergence is measured rather than assumed away. |
| D3 | **Whether to raise MetaDrive's `max_speed_km_h = 80`.** | 2026-08-20 | Decided: **do not raise**; declare the ceiling. The reason is quantitative: the episode ceiling is `a · distance / longest single step`, and the cap *is* the longest step, so it sits in the denominator of what a mission can be worth. A 110 km/h cap — the least that covers all nine records — costs **−27 %** of every mission's worth while every per-step penalty stays unchanged, and `λ₄` cannot compensate because `λ₄ + 0.1·(λ₅+λ₆) < a`. Completing those records *would* be legal (posted limits 65–70 mph), so the obstruction is the vehicle, not the rulebook. Recorded as `RULEBOOK-V5.1` limitation 14 with the full table. |

| B1 | **ADR-071 not implemented** — R1 at-fault classification and truncation. | 2026-09-01 | Implemented as `RB51` `M7`. `components/collision_fault.py` carries nuPlan's taxonomy; `evaluate_collision_impact` charges only at-fault contacts; `done_function` truncates a not-at-fault-only step through the existing `MAX_STEP` channel, calling the same classifier so cost and episode contract cannot drift. Undeterminable lane containment resolves to at-fault. |
| B3 | **`speed_limit` requires amending `OBS-V1.3` and `OBS-LIDAR-V2.0`.** | 2026-09-01 | Both amendments written and applied: `OBS-V1.3.1` (`D` 3009 -> 3011) and `OBS-LIDAR-V2.0.2` (frame 308 -> 310, stacked 6489 -> 6531). Checkpoint compatibility intentionally broken, as `DEC-RB51-001` approved. The observation resolves the limit through the reward's own `associated_speed_limit_mps`, so the "unavailable" encoding fires under exactly the condition that makes the sub-rule inapplicable. |

| F6 | **PG coverage measurement** for the rulebook. | 2026-09-01 | Run as `RB51` `M8a`. It was one call site, as limitation 5 said. Result: 1089/1100 PG records replay, and **five of the six L3 sub-rules never apply** — `signal`, `stop`, `crosswalk` and `speed_limit` are inapplicable on every PG record, as is `clearance`, so L3 on PG is `offroad` alone and PG carries no traffic controls at all. The two sources are graded by substantially different rulebooks, not merely under different speed regimes as limitation 13 records. Bears on `D5`. |

| B2 | **`RULEBOOK-V5.1` not implemented in `src/thesis_rl/`.** | 2026-09-01 | `RB51` `M0`-`M8` complete. Six levels, `SCAL-V1.4` selected by config, the inherited sub-rule redefinitions, `speed_limit`, the at-fault gate, R1 at-fault classification with truncation, and the §7 diagnostics. 1528 tests, lint and format clean, `make smoke` exit 0; production reproduces the measurement instrument with zero divergence and the 1100-record panel reproduces §5.5 to the decimal. |
| D5 | **Arm/source confounding in the frozen train split.** | 2026-09-01 | Decided: **accept, it is the design.** PG is deliberately simpler and covers a gap logged scenarios do not, per ScenarioNet's own rationale, and the ACL exists precisely to move from simple to complex — so arms ordered by source is the curriculum, not a confound. `M8a`'s finding that five of six L3 sub-rules never apply on PG is a *sharper statement of the same intent*: "fewer active rules" is a concrete, measurable notion of "simpler". **One reporting rule survives**: never publish a single score pooled across sources, because the expert's +142.06 on PG against +70.70 on Waymo is largely rule count, not driving quality. Report per source. |

| C1 | **RSS standstill-exit transient** identified but not fixed. | 2026-09-01 | **Retired by ADR-063, not fixed.** `rss` longitudinal now carries `contributes_to_channel=False` (`rulebook/v2/registry.py`), so it is evaluated and published but never reaches a channel. The transient therefore cannot affect any cost, reward or termination — it is a defect in a diagnostic. Verified by inspection of the registry entry. |
| C4 | **Mission artifact regeneration pending** a provisioned `pyarrow`. | 2026-09-01 | **Verified obsolete.** The `pyarrow` gap ended 2026-08-06 (pyarrow 25.0.0 in the `dev` image) and `scenario_catalog_driving_mission_v1_1_1.parquet` is produced reproducibly by `scenarionet-v1-2-rebuild` (`Makefile:354`). The one artifact the mission ExecPlan still lists as pending, `scenario_selection_mission_v1_1_1.json`, is **referenced by no code path** — `grep` over `src/`, `conf/`, `Makefile` and `scripts/` returns only the *schema string* `scenarionet_frozen_selection_mission_v1_1_1`, which `frozen.py:461` accepts as an alternative to the plain schema. The runtime reads `scenario_selection_index.json`, which exists and works. |
| D6 | **Should `EVAL-PROTOCOL v1.3` record the feasibility ceiling?** | 2026-09-01 | Decided: **yes, record it; change nothing else.** Written as `docs/specifications/evaluation_protocol_v1.3.1_amendment.md` (`EVAL-PROTOCOL-V1.3.1`): three requirements — report the ceiling alongside every Waymo success figure, re-measure it if the panel or the vehicle changes, and exclude no record. §3 records *why* no remedy was adopted: raising the cap to 110 km/h would cover all nine records but costs **−27 %** of every mission's worth, because §4.1 puts the cap in the denominator of the episode ceiling, and `λ₄` cannot compensate under §5.4's bound. Nine unreachable records out of 1805 against a 27 % devaluation of all 1805 — the first is a declared ceiling, the second reopens the degeneracy the rulebook exists to close. |
| D7 | **The git-tracked frozen index is not the index in use.** | 2026-09-01 | **Was never true; raised in error from a stale ExecPlan note and withdrawn the same day.** The mission ExecPlan's 2026-08-04 entry said the tracked copy was still the pre-mission snapshot, but the sync happened on 2026-08-08 (commit `aa30e40`) and the note was never updated. Verified by hash: `data/scenarionet/frozen/scenario_selection_index.json` and the host copy at `/scratch/.../frozen/` are both `sha256 2829a1e26f50d65f4bfda1cb2945691ccad7fee8c81b515631d01898bf499b39`, same 196 461 242 bytes, same `created_at` 2026-08-04T21:23:53Z and same `selection_hash`. Reproducibility is intact. |

| F1 | **M5: solid-line penetration deadband (audit `P5c`) and signal persistence (audit `P7.3`).** | 2026-09-01 | **Both halves superseded, neither needs its own work.** `P5c` asked for a minimal-penetration deadband on `solid_line`; ADR-065 went further and replaced the binary cost outright with **graded lateral penetration, zero below tolerance 0.3**, detected against the marking's real half width 0.075 m instead of a 1 cm epsilon — a deadband is a special case of what was implemented, and it was calibrated by a reported sweep rather than chosen. `P7.3` asked about the red-light persistence latch; ADR-064 **removed** the traffic-control persistence latches. Both landed in `RB51` `M3` on 2026-09-01. |

| C5 | **Every `obs=semantic_v3` run was unstartable for a month.** `envs/factory.py:128-133` injected `semantic_v3_signal_range_m`, `semantic_v3_signal_fov_degrees` and `semantic_v3_signal_camera_height_m` into the environment configuration; `ThesisScenarioEnv.default_config()` declared none of them; MetaDrive's `BaseEnv.__init__` merges with `allow_add_new_key=False` and raises `KeyError` before the first reset. | 2026-09-02 | **Found and fixed the same day.** Introduced 2026-08-01 (`52b8ad4`) and invisible for a month, because the repository's only end-to-end check, `make smoke`, selects `obs=lidar_state` and `encoder=none` -- a different code path from `make run-train`, which passes `obs=semantic_v3`. Surfaced by the `AB-LEARN` screening smoke, which existed precisely to exercise that unverified path. Fixed by declaring the three keys at the `OBS-V1.2` SS6.2 baseline `80.0/65.0/1.2`, identical to the defaults the readers already applied, so no behavior changed: the environment either failed to construct or now constructs with the values the code expected. Regression test parametrized over `conf/obs/*.yaml` and asserting MetaDrive's own construction merge rather than a key-set comparison, because the rejection recurses into nested dictionaries and the passthrough also writes into `vehicle_config.lidar`. Verified to fail on the pre-fix code. The **coverage gap that hid it is not closed by the fix**: see `SMOKE-COV` `M2`, deferred by `DEC-COV-001`. |

| C6 | **The evaluation path cannot start under `obs=semantic_v3`.** `_ego_local_projection` (`envs/observations/causal_semantic.py:79`) requires the re-projected ego station to equal the mission tracker's committed station within `1e-6 m`; at episode reset it does not, and the first asynchronous evaluation dies in a vector worker (`reset_slots`). | 2026-09-02, `AB-LEARN` smoke | **Measured, not guessed** (diagnostic added to the error message the same day): projected `s = 0.022900 m` against committed `s = 0.0 m`, divergence **0.0229 m** on a 23.17 m route -- four orders of magnitude above the tolerance and three below anything semantically meaningful. **Cause identified**: `mission/runtime.py:281` constructs `RouteCoordinateMissionTracker(mission, route, 0.0)` with a hard-coded initial station, so at reset the guard compares a zero *by definition* against a geometric projection of the actual spawn pose. They agree only if the vehicle spawns exactly on the route origin. The magnitude is consistent with the projection jitter ADR-073 already measured (`sigma - s` non-zero on 20.99 % of expert steps, never above 0.053 m). **Not floating-point noise and not a stale snapshot** -- both were excluded by the magnitude. Blocks the `AB-LEARN` screening and therefore the freeze of rulebook and scalarization. Never caught before because the whole `obs=semantic_v3` path was unstartable since 2026-08-01 (`C5`), and **no test covers this invariant**. Resolution is an open decision: seed the tracker from the projected spawn pose, widen the tolerance to a measured bound, or exempt the reset observation, each with different consequences for `route_completion` at `t=0` and for L4's first increment. **Closed 2026-09-02**: resolved as the user chose -- the consistency check is enforced from the first tracked step (`step_index > 0`), the `1e-6 m` tolerance is unchanged, and a two-sided regression test in `tests/test_causal_semantic_batch.py` pins both the reset exemption and the fact that the same divergence still raises once the tracker has advanced. Both smoke arms then completed `exit 0`. |

| D8 | **Should TD3 receive ADR-078's learner setting (batch 512, `update_to_data_ratio` 0.5)?** Opened when ADR-078 changed SAC only, because the 179 ms/step measurement was taken on SAC. | 2026-09-06 | **Closed the same day: yes, same setting, same ADR.** The TD3 backend has the same update structure (`auto -> n_envs` gradient steps of batch 256, four `lq_v3`-backed networks, update overlapped with the workers), and an asymmetric update-to-data ratio would be a second varied factor in the `EVAL-PROTOCOL` algorithm comparison. `td3_sb3.yaml` and the six non-smoke profiles now carry 512 x 0.5; `policy_delay=2` unchanged. The `REQ-013` documentation debt noted here is recorded in ADR-078 §Consequences. |

| W1 | **"`PER.add()` runs a Python loop"** — the audit's mechanical list item d, first half. | 2026-09-07 | **Withdrawn on measurement: the vectorised version already exists and is four times slower.** `add()` does loop over `n_envs`, each iteration walking the depth-23 tree in `_SumTree.set`, and that costs **64.8 µs per vector step** at production geometry (20 leaves x 23 levels). The alternative is `set_batch`, which `update_priorities` already uses: it costs **256 µs** on the same input, because it runs `np.unique` plus `np.add.at` at each of 23 levels on 20-element arrays. Both produce bit-identical trees. The loop is the faster of the two at this width, so the "fix" is a 4x regression. Do not touch it. |
| W2 | **"`required_observation_schema` is declared in the encoder YAML and read by exactly one test, nothing else"** — the audit's mechanical list item i. | 2026-09-07 | **Withdrawn as framed: the pairing it would guard is already enforced, and more strongly.** `agent/planners/encoders/factory.py` refuses the encoder outright unless the observation schema *is* `SemanticObservationSchemaV12` and `input_dim` equals its `flat_dim` — at three sites, one per latent-query variant, plus a fourth check inside `lq_encoder.py`. So the key is neither a dead knob (as `C24`'s six were) nor an unimplemented guard: it is descriptive, and `tests/test_audit_block_b_encoder_frozen_settings.py` classifies it as such deliberately. **Residual, not a defect**: the YAML string is a third copy of the schema version, which a bump already has to update by hand in four files. Hardening it (refusing a declared value that differs from the selected schema's `.version`) is about four lines, but it buys nothing today and was left out rather than added as unrequested scope. |

*(move further items here with the date and the change that closed them)*
