# Open Items

Single register of known-but-unfixed work. It exists because these were
previously scattered across "Notes" cells of `docs/project_index.md`, where they
were easy to lose: several have been open for weeks without being surfaced.

**Scope.** Items recorded in repository documentation, plus items opened by a
decision that has not yet been implemented. It is **not** an audit of the source
tree — an unlisted problem is not thereby absent.

**Convention.** `Blocking` means some approved contract is not yet met by the
code. `Deferred` means a decision was taken to postpone. `Open decision` means
the work is waiting on a choice the user has not yet made. Tick an item by
moving it to *Closed* with the date and the change that closed it. **Identifiers
are never recycled**: a closed `D2` stays `D2`, and the next new decision takes
the next free number.

Last reviewed: 2026-09-09, second pass (`C49`, `C50`, `C51`, `V3` and `V4` opened from the
rulebook architecture review, `docs/audits/rulebook_architecture_2026-09-09/`. `C49` is the
first entry in *Blocking* since the section was emptied: ADR-081's hierarchy bound was
evaluated at the Waymo-only 199-step horizon while the longest training episode is 500, so
27.09 % of training records run past the inversion point, and the guard test hardcodes the
same figure. `C50` is an executed exploit of the progress channel's negative clip — +72
reward units per closed lap at zero net displacement — currently blocked by a property of
the frozen population whose re-audit script does not exist (`V3`). `C51` records that the
statistic which would detect `C50` discards its sign, and withdraws an earlier reading of
the same number as an exploit; it is a deficit. No decision was taken and no behaviour
changed).
Earlier the same day: (`D14` half-closed: `REQ-EF-15`'s corridor diagnostic is now
aggregated per episode and reaches `eval_episodes.csv`, with `route_fully_outside_max_run`
as the statistic that separates a clipped corner from driving another carriageway;
`l4_clip_binding_steps` rides the same path, making `AC-RB5.1-05` measurable. The decision
half of `D14` stays open: nothing has been run, so the frequency is unknown).
Earlier: 2026-09-08 (`D15` opened: `γ = 0.996` spends ordering O3, measured at
−3.5789 against +0.2000 undiscounted and resolving at L4 rather than L5; `RULEBOOK-V5.1`
§1.1, §4.1, §4.4, §5.5.1, §9, §10 and §11.12 amended to match, with `AC-RB5.1-05`
downgraded to `NOT ESTABLISHED` because the engine-force cap bounds the ego's travel and
not its projection). Earlier the same day: (`OBS-AUDIT-FIX-001` merged: its `C14`–`C17`, drafted on a branch,
became `C44` and `C45` on merge, and its other two fixes extend `main`'s `C17` and `C18`, which
had fixed the same defects independently; the audit's reward-contract findings — running a red
light is positive expected value under SCAL-V1.4, the goal-before-stop-line case, ADR-061 still
unimplemented — remain user decisions and are not registered as defects). Earlier the same day:
`C30`–`C39` merged onto `main`; see the identifier note below.
Same day, documentation-versus-reality audit (eight drift dimensions, adversarially verified):
`D14`, `C46` and `C47` filed, `V1` reopened, and the confirmed stale claims reconciled in
`RL-BASELINES`, `OBS-V1.3`, `ENC-V1.3`, the two `README` authority lists, `docs/project_index.md`
and the `RB51` ExecPlan.
Previous passes: 2026-09-07 (`C30`–`C37`, the mechanical list of the block-B audit), 2026-09-06
(`C13`), 2026-09-01 (after `RB51` closed). `D1` was briefly
closed on 2026-08-20 and **reopened the same day**: the value chosen presumed an episodic threshold,
and the mechanism is not chosen yet.

**Identifier collisions, twice, and the rule that resolves them.** `C13` was drafted as `C12` on a
branch and renumbered on merge, because `C12` had been taken on `main` in the meantime by the
intra-chunk progress and fail-fast defect, whose identifier source code already referenced. It was
then renumbered *into* a `C13` that the audit lineage had been using since 2026-09-06 for ADR-071's
dead not-at-fault truncation, because neither branch could see the other. On merging the two
lineages on 2026-09-08 the audit side moved — `C13` → `C40`, and its `ADR-079` → `ADR-081`,
`ADR-080` → `ADR-082`, `TRANSITION-REPLAY-V1.0.1` → `V1.1.1` — because `main` was published and
referenced. Two branches taking the same next free number is the way this register breaks, and the
rule that identifiers are never recycled is what forces the renaming onto whichever side has not
landed yet. **Take the next free number from `origin/main`, not from your branch.**

**Checks executed for `C30`–`C37`** (2026-09-07, branch `worktree-audit-block-a-fixes`): full suite
**1789 passed, 5 skipped, exit 0** in 27m52s — re-run **after** `C37`'s revert, so it covers the
tree as it stands and not an earlier one; the 5 skips are the non-v3 observation configs of
`C35`'s parametrized causality check, which is their intended behaviour. Ruff lint and format clean
on every touched file, focused scope. Production-path smoke **exit 0** on the GPU
(`obs=semantic_v3 agent/planner/encoder=lq_v3 reward=scalar_reward env.vectorized.enabled=true
curriculum=scenario_acl_scenarionet device=cuda`), two chunks of 1000 steps, driver
`scenario_acl_vectorized`, 40 MAB updates, 951 gradient steps — so it exercised every path this
batch touched, including the chunk boundary where `C31`'s state write happens. An earlier full-suite
run caught a regression introduced by `C35`'s first form; see that entry.

**`C19` is superseded, not carried.** `RESUME-ABRUPT-001` replaced
`require_replay_buffer_for_resume` with `classify_replay_resume`, which makes the same refusal,
names the exact override that would proceed, and additionally distinguishes a partially committed
checkpoint pair from an explicitly accepted empty replay segment. The function and its four test
cases were removed on merging; `tests/test_resume_snapshot.py` covers every branch of the
replacement, so the contract is enforced more strictly than `C19` enforced it. `C16`'s half of that
test file stays, and `C16` itself is now partly moot on the same lineage: `latest` and periodic
checkpoints are no longer written from the asynchronous-evaluation callback at all, since it passes
`write_resume_snapshot=False`, so only `best_*` still needs the evaluated-snapshot copy — which it
does, through `_persist`.

---

## Blocking — approved contracts not yet met

| # | item | source | notes |
|---|---|---|---|
| C49 | **ADR-081's hierarchy-preservation bound is evaluated against a Waymo-only horizon, and the shipped discount does not satisfy it on the real training mixture.** The ADR requires `Δ = ln(a) / −ln(γ) > L` and took `L = 199` from `RULEBOOK-V5.1` §4.6's measured p5/p50/p95 of 197/199/200 — **which is the 1100 Waymo `train` records only**. The training mixture is half procedurally generated (`conf/env/scenarionet.yaml`, `source_probability` 0.5/0.5) and PG episodes are far longer: train/pg p50 **241**, p95 **501**, max **501**, against train/waymo max 200. The chain to the runtime is closed: the frozen index's `length` is the scenario's own `SD.LENGTH` (`scripts/validate_frozen_scenarionet_content.py:52-56` cross-checks catalog against description), the runtime reads the same field (`third_party/metadrive/metadrive/manager/scenario_data_manager.py:106`), and the episode truncates at `episode_steps >= scenario_length - 1 + extra` (`src/thesis_rl/envs/thesis_scenario_env.py:47,1003`) with `extra_steps_after_scenario: 0` and `horizon: null` (`conf/env/scenarionet.yaml:16,69`). **The longest training episode is therefore 500 control steps, not 199.** At the shipped `a = 2.5, γ = 0.996` the break-even is **228.6** steps, so **596 of 2200 training records = 27.09 %** run past the point at which a future collision is discounted below a present non-relaxable violation — the exact inversion `γ` was chosen to prevent. **The guard reproduces the error rather than catching it**: `tests/test_hydra_agent_presets.py:257` hardcodes `horizon_steps = 199` under a docstring reading "The horizon is measured, not assumed". Classification per `AGENTS.md`: a defect of the repository — it holds for any contributor on any machine. | 2026-09-09 (`docs/audits/rulebook_architecture_2026-09-09/`, `g1_discount.py`) | **Neither of ADR-081's two reasons for leaving `γ = 1` is disputed; only the `L` the second was evaluated at.** The criterion restates exactly as `γ^L ≥ 1/a` — whole-episode damping must not fall below the one-level priority ratio — which puts the two reasons on one axis: the contraction argument wants `γ^L` small, the hierarchy argument wants it at least `1/a`. **Exits, priced.** (a) Raise `γ` to `0.4^(1/500) = 0.998169`: the effective horizon `1/(1−γ)` goes 250 → 556 steps (25.0 s → 55.6 s, ×2.2) and whole-episode damping of a bootstrapped constant 0.135 → 0.406, weakening ADR-081's own contraction argument by the same factor. **It costs nothing in calibration**: `episode_return += scalarized.reward` (`scripts/measure_expert_rulebook_transition.py:2038`) is an *undiscounted* sum and §5.4 is per-step and `γ`-free, so `a`, `σ`, `λ₄`, `η`, `λ₆`, `φ` and every calibration column are untouched and the 45-minute grid needs no re-run — the audit README says so itself ("What it does not measure: the discount"). (b) Raise `a` instead: `a ≥ 7.419` at `γ = 0.996`, taking `a³` from 15.6 to 408 — ×26 on the critic's dynamic range — and dragging `σ`, `λ₄` and the 217,189-transition grid; ADR-081 already measured and rejected `a = 3.0` in both directions. (c) Cap the horizon: a cap at 229 truncates **53.7 %** of PG train records, and the PG mean route of 183.81 m is 405 steps at the expert's 4.535 m/s, so any cap below ~410 makes the median PG mission structurally incompletable. **The durable half of the fix, whichever exit is taken**: the guard must compute `L` from `data/scenarionet/frozen/scenario_selection_index.json`, which is committed, instead of hardcoding it — then the docstring's claim becomes true and a future panel change fails the guard instead of passing it. Not decided here: raising `γ` is a specification change to `RULEBOOK-V5.1` §4.4 and an amendment to ADR-081. **2026-09-09: `γ = 0.9982` and the A7 architecture are APPROVED (explicit user approval, 2026-09-09); the value and the criterion's verdict land with the A7 ExecPlan change, and the measured-horizon half is done here.** `0.996` is therefore no longer the decided discount, only the one still in `conf/agent/planner/algorithm/*.yaml` until that change merges. `tests/test_hydra_agent_presets.py` now reads the horizon from the committed index — `max(length) − 1` over the 2200 `train` records = **500** — and cross-checks the two configuration values the truncation rule depends on (`horizon: null`, `extra_steps_after_scenario: 0`), so the docstring's claim is executed rather than asserted. The fixture pins **the requirement rather than any discount**: `horizon == 500` control steps, the two configuration values the truncation rule depends on, and `γ ≥ 0.998169` as the value the criterion needs at `a = 2.5` over that horizon — a function of `a` and `L` alone. It deliberately asserts neither a `γ` nor the criterion's verdict, because both belong next to the approved value on the A7 branch, and asserting them here would contradict that change for as long as the two are unmerged. A change to `a`, or a regenerated index whose episodes run longer, moves the requirement and fails there — the protection the old formulation was meant to give and could not, comparing against a horizon that could not move. **The margin is thin and is a property of the frozen index, not of the code**: the approved `0.9982` gives a break-even of 508.6 steps against a longest training episode of 500, so one 510-step scenario in a regenerated index would put the criterion back in deficit, and `horizon_steps == 500` is what would say so. **One figure corrected**: the 596 records / 27.09 % above compares the *scenario* `length` with the break-even; the criterion is about control steps, and at `length − 1` the count is **591 / 26.86 %**. The conclusion is unchanged. |

## Open decisions — waiting on the user

| # | item | opened | notes |
|---|---|---|---|
| D1 | **`τ₄` for the thresholded-lexicographic arm.** `τ₄ = 0` is wrong: exact L4 ties have probability zero, so at 0 the comparison stops at L4 on every pair and **L5 and L6 decide nothing**. A positive `τ₄` is load-bearing — it is the minimum progress a lane relaxation must buy to be worth taking. | 2026-08-20 | **Sequenced after the rulebook is frozen**, and not fixable before the algorithm is chosen: the object a threshold applies to differs by mechanism. *Absolute Thresholding* (Gábor et al. 1998) thresholds **Q-values**; *Absolute Slacking* (Li & Czarnecki 2019) thresholds a slack from the **state's own optimum**, which is already relative and therefore sidesteps the mission-length problem for free; a policy-gradient construction (Tercan & Prabhu 2024) compares **expected returns**, which is episodic. A rule phrased on the completion fraction presumes the third. Analysis kept: missions span 13–247 m, so no single absolute constant serves both ends under an absolute mechanism; `1 − success_route_completion_threshold = 0.05` is an already-declared constant available as a starting point **if** the episodic reading applies. Settled regardless: the **channel is not renormalized** (§4.1.1's rejection stands, scalar calibration untouched). |
| D4 | **Restart of the three production runs.** `sac-0` and `ppo-0` crashed on checkpoint reload and are stopped; `td3-0` remains at risk. | 2026-07-26 (ADR-026) | **No longer a decision about whether**: `γ = 1` (ADR-075), `speed_limit`, the four-to-six margin vector and the observation change break checkpoint compatibility by four independent approved routes, so nothing is resumable and nothing is comparable. **Sequenced, though** (user, 2026-09-01): the serious runs come *after* the A/B/C/D learnability tests — whether the rulebook+scalarization reward is learnable comparably to MetaDrive's native reward, and whether the ACL actually helps — and the ACL itself is not implemented yet. **Root cause of the original crash identified 2026-09-01** while running `V2`: see that item. |
| D9 | **Should PPO receive an ADR-078-style learner setting (minibatch `64 -> 128`, same samples in half the optimizer steps)?** | 2026-09-06 (ADR-078) | Deliberately **not** decided with SAC/TD3: PPO's update is synchronous (collect 2 048, then 10 epochs x 32 minibatches), not overlapped with the workers; the builder forces PPO onto CPU; and no PPO `step_timing.csv` exists under `lq_v3`, so it is unknown whether collection or update dominates. Decide from the first measured PPO run's `step_timing.csv`. PPO is more sensitive than SAC/TD3 to minibatch x epochs, so the "same samples, half the steps" argument is weaker there. |

| D10 | ([issue #6](https://github.com/EmanueleRsp/thesis-metadrive/issues/6)) **Removing the staged curriculum. Parked by user decision 2026-09-07** — the automatic (scenario-ACL) curriculum is the one that will be used, so the staged path should not be reached again; the question is recorded in the issue rather than acted on. Original framing: take it out, since the automatic curriculum is the one that will be used. The reason it should go is real — in staged mode the promotion gate does not use the frozen validation panels but samples with replacement from the arm/source cell, and PG `A3` holds **1** record and `A5` holds **2**, so a promotion is decided on one or two scenarios and is statistically a coin flip. | 2026-09-07, code audit (block B6) | **Not done in the C21–C28 change, deliberately.** Deleting the two Hydra presets removes the only coverage of `curriculum.staged` composition: `test_hydra_preset_run_configs.py::test_reward_variants_compose` asserts `staged.mode` and the stage count, and `::test_scenarionet_staged_curriculum_composes_with_semantic_arms` asserts the six stage names outright. AGENTS.md forbids deleting mandatory tests without explicit approval, and doing it inside an already broad defect-fix branch would have made the diff hard to review for no scientific gain — not selecting the preset achieves the same thing today. **Two ways to close it**, user's choice: (a) delete `conf/curriculum/stages.yaml` and `stages_scenarionet.yaml`, drop the two tests and repoint the five other call sites (two debug tools included) at `curriculum=disabled`, as its own change; or (b) keep the feature but make the promotion gate refuse a cell below a minimum scenario count, which kills the meaningless gate without removing anything. (b) is ~15 lines and loses no coverage. |
| D11 | **Pipelining the learner update against worker collection** (user-approved in principle 2026-09-07, estimated up to 1.8x). Deferred to its own change: it restructures the collection loop, and merging it with a defect-fix branch would make both unreviewable. The correctness argument stands — TD3/SAC are off-policy by construction, so collecting under slightly stale parameters is what they are for — and the real cost is bit-exact reproducibility, which survives if the **update:step ratio stays deterministic**; only *which* parameters generated a given transition becomes timing-dependent, and no claim rests on that. Same session: the user accepts running with `torch.use_deterministic_algorithms` off and re-enabling it for the final reported runs. | 2026-09-07 | Sequence after the reward/discount calibration lands, so throughput work is measured against the reward the thesis will actually report. |

| C37 | **`REQ-013` contradicts itself about the priming priority, and the code follows its prose while a change made on 2026-09-07 made it follow the formula instead.** The requirement's prose says "new transitions receive the current maximum raw priority; the initial maximum is `1.0`"; its invariant line says \(p_{new} = \max(1, p_{\max,current})\). Those are different rules, and the second is not implied by the first. `_insertion_priority` had always implemented the prose. **The internal contradiction is the root cause of this item; the code was not the defect.** | 2026-09-07, found while investigating `D13`'s provenance | **Changed, then the change's justification was refuted the same day. Awaiting a decision — see the recommendation below.** The floor was added (`95634fe`) on the reasoning that "any run whose residuals sit below 1 has a buffer maximum below 1". **That reasoning is wrong, and so were its numbers.** (a) The inference is an invalid mean→maximum step: SB3's `critic_loss` is an importance-weighted mean of *squared* residuals over one batch, while priming reads a **maximum over up to 300 000 accumulated and stale rows**. (b) The figure "critic losses 0.6–0.9" quoted in that commit **has no artifact**: the values actually recorded on 2026-09-07 are **0.0018–0.396**, and it should be treated as withdrawn. (c) The run whose `critic_loss` was 0.396 is the *same* run whose buffer maximum measures **8.42774** — the inference is off by 13.4x on the very run that motivated it. (d) Read directly out of all six PER buffers persisted on disk, every maximum is far above the floor: **8.43, 10.83, 5.35, 5.19, 3.91, 2.97**, all from runs predating the change, so they are TD-derived and not an artefact of the clamp. Even the degenerate run whose rewards all lie in `[0, 0.0905]`, where the reward term *cannot* push a residual above 1, measured a maximum of **2.97**. (e) The "pre-fix evidence, 0.400001" is a **hand-built state**: the test writes 0.4 into all four leaves of a four-leaf buffer, and at production geometry that state is unreachable by ordinary insertion, because primed rows are re-priced within 0–3 steps. **What is true**: priorities are `0.5·Σ|TD_i|` over the twin critics against an *n*-step target, so they live on the *return* scale — 22 % of rows in one buffer and 53 % in another exceed 1.0 — and the median sits at 0.017–0.645, which is where "typical residual < 1" came from. Both statements hold at once; only the maximum matters here. **Recommendation, on mechanism rather than on the document**: revert to the prose rule. The sampler is *exactly* scale-equivariant — leaves hold `p^α`, `P_i` = leaf/total, weights are max-normalised per batch — and the literal `1.0` is the only reward-unit term in it, verified bit-exact (under a `|TD|×100` rescaling the address stream is 100.000 % identical without the floor and 0.641 %, i.e. chance, with it). The deficit it would close measures **zero**: a conservation law pins the share of draws reaching never-priced rows at `n_envs/(gradient_steps·batch_size) ≈ 0.49 %` *independently of the priming value* (measured 0.39 % either way), first-sample latency is 0–1.7 steps against a residence of 10 000, and no transition was evicted unsampled in 12/12 configurations. Where the floor does bite it **destroys the machinery it sits in**: `_current_max_raw_priority` collapses onto 1.0 and stops measuring the critic. **Not inert in every regime, though** — on a stationary light-tailed stream it binds on 97.8 % / 77.9 % / 27.1 % / 0 % of insertions at σ = 0.02 / 0.05 / 0.2 / ≥0.5, a converged regime never observed here but not impossible, and `reward_compression: symlog` is an approved-but-off switch that would make the floor a 4–16x multiplier in the 0.01–0.1 band. **Resolved the same day by explicit user decision: remove the floor and amend the specification.** `_insertion_priority` is back to the exact current maximum, its docstring now carries the mechanism argument rather than a citation, and the constructor comment at `prioritized.py:119` — which described the prose rule ten lines above a formula implementation — is coherent again. The floor test was removed **with that approval** and replaced by `::test_per_priming_is_scale_equivariant_and_tracks_the_live_maximum`, which asserts the defensible property instead of a constant: the priming value is the live maximum and moves by exactly a rescaling factor. Coverage is redirected, not reduced. `docs/specifications/transition_replay_v1.1.1_amendment.md` restates `REQ-013` and `AC-016` so that prose and invariant agree and records `1.0` as Schaul's `p_1` *initialiser under rewards clipped to [-1,1]* — which this project does not use. **Approved the same day** (`TRANSITION-REPLAY-V1.1.1`, `Authoritative: YES` for that subset), recorded by `ADR-082`, indexed in `docs/project_index.md`; it requires no code change, because the code already implements it and did so before the episode. **Still unmeasured, deliberately**: the buffer maximum has never been logged during a run, so the converged light-tailed regime in which a floor would bind (27–98 % of insertions at σ ≤ 0.2) is simulated but unobserved; instrumenting it is deferred with `C38`. |

| D14 | **`DEC-EF-06`'s held `R4` corridor gate has no admissible option left, and the diagnostic it was staged on is aggregated nowhere.** The decision (2026-07-29) took option **B** — instrument now, measure, then decide — and `M6b-ii` was held by explicit user instruction on 2026-07-30. Its option **A**, `m_4 = min(0, m_prog) + max(0, m_prog)·(1 − q_out)`, attenuates positive `R4` by the footprint fraction outside the task corridor: an off-route `R4` term and a runtime lateral envelope. `DRIVING-MISSION-V1.1` §8 lists **off-route `R4` zeroing** and **runtime authority of any final lateral envelope** among the elements to remove (`docs/specifications/driving_mission_v1.1_specification.md:104`, approved 2026-08-04, i.e. **after** the decision), and §1 states the mission is "not … a way to put legality, heading, or lateral offset into `R4`". A specification outranks an ExecPlan, so no `M6a` measurement can unblock the held choice. | 2026-09-08 (documentation reconciliation) | **Two exits, both the user's:** formally **close** `DEC-EF-06` as foreclosed, or **amend** the mission specification to admit a corridor term. Verified while filing this row. (a) The diagnostic exists and is dead: `route_outside_fraction`/`route_adherence` (`REQ-EF-15`) are computed every step into the rulebook step info, and nothing reads them — zero references under `src/thesis_rl/analysis`, `src/thesis_rl/cli`, `scripts/` and `EVAL-PROTOCOL`. The measurement as framed would also read ≈ 0, because the logged expert stays on its own route by construction. (b) The uncovered case is **topological, not physical**: `is_physically_out_of_road` says "excluding route drift" in its own docstring (`src/thesis_rl/envs/scene_context.py:51`) and fires only on sidewalk/guardrail contact or a full-footprint exit from the drivable surface, which is the union of every vertically compatible lane (`src/thesis_rl/rulebook/v2/geometry/drivable.py:92`); `_recompute_terminated` covers success, collisions and that predicate only (`src/thesis_rl/envs/thesis_scenario_env.py:814`); and `mission_unreachable` is a **dead field**, the literal `False` beside `reachable=True` in the production tracker (`src/thesis_rl/mission/tracker.py:241`). So an ego that takes a wrong turn onto a legal parallel road keeps earning L4, terminates on nothing, and ends at the time limit. (c) Nothing in the reward references the final gate either: §8 of the same specification also removes *terminal bonuses* and *completion-as-success*. (d) **A penalty placed below L4 cannot oppose progress**, which is arithmetic from the configured weights, not an opinion: `conf/scalarization/default.yaml` gives L4 up to `λ₄ · ΔQ_MAX = 2.0` per step while the *entire* L5 channel is capped at `η · Δt/T_REF = 0.1` and L6 at `λ₆ · Δt/T_REF = 0.02`, a factor of 20, and §5.4's rank-preservation condition is what keeps those weights small. So if the user chooses to act on this at all, a new sub-rule below progress is the one shape that provably will not work; the candidates that remain are an episode-contract change (terminate or truncate when the gate becomes unreachable, which leaves the hierarchy and its calibration untouched) or a redefinition of what L4 measures. Not decided here. **Measured 2026-09-08, and the result narrows the item substantially** (`docs/audits/final_gate_carriageway_coverage_2026-09-08/`, `scripts/measure_final_gate_carriageway_coverage.py`, all 3500 records, instrument self-validated against the frozen gate at a maximum residual of 3.5e-12 m). The worry was a legal same-direction road running beside the route that the gate does not reach. **The geometry exists but is mostly remote**: uncovered same-direction surface at the goal cross-section in **1534/3500 (43.8 %)**, but at a **median gap of 16.55 m**, and the gate itself has median length **10.26 m** — about three lanes — with the goal point inside it on every record. Adjacent cases are the minority: **426 (12.2 %)** within one ego width. **And the tightest gaps are not parallel roads at all**: the six smallest are PG at 0.010-0.011 m, a hair over `FINAL_GATE_BUILDER_EPSILON_M`, beside gates already 11.5-34.7 m wide — a merge-tolerance artifact, filed separately as `C48`. What survives as a real exposure is narrower and different from the original framing: **289 records (8.3 %)** where a one-lane change to *either* side leaves the gate, and **95 (2.7 %)** whose entire gate is one lane width or less. **Not established**: the measurement covers the goal cross-section only, so it does not show that an ego could drive such surface *while banking the `R4` budget* — that needs the backwards route walk, which is designed but not run — nor whether any trained policy goes there, which is what the unaggregated `route_outside_fraction` would answer. **Half of this item is now closed (2026-09-09): the diagnostic is aggregated.** `RulebookV2MonitorWrapper` accumulates it per episode and republishes the running totals every step; `Agent.evaluate` carries the last step's values into the episode record through `EPISODE_DIAGNOSTIC_KEYS`; and all four `eval_episodes.csv` write sites emit `ROUTE_ADHERENCE_EPISODE_COLUMNS` (`src/thesis_rl/runtime/route_adherence_diagnostics.py`), declared in `CSVRecorder.SCHEMAS`. Six columns, of which **`route_fully_outside_max_run`** is the one that answers the question: a mean cannot separate clipping four corners from eighty consecutive steps on another carriageway, and only the second is the failure. `route_outside_evaluated_steps` ships beside them so a zero is never read as "never left the corridor" when it means "never measured". `l4_clip_binding_steps` rides the same path, which is what makes `AC-RB5.1-05` measurable rather than deduced. **What remains open is the decision, not the instrumentation**: nothing has been run yet, so the frequency is still unknown, and no remedy is proposed until a screening reports the distribution. **2026-09-09: the backwards route walk has now been run, and it establishes what this row records as not established.** `scripts/measure_final_gate_carriageway_coverage.py --walk-spacing-m` calls the builder's own cross-section decomposition at stations along the route instead of only at the goal, so it inherits that instrument's reconciliation against the frozen gate; 3,500 records walked, **zero unusable stations** (`docs/audits/progress_channel_integrity_2026-09-09/`). A *corridor* is same-direction drivable surface outside the route's own carriageway, at least one ego width wide, containing a position from which the ego misses the finite final gate, followed backwards while consecutive cross-sections still offer an overlapping component; `offroad` is zero on it by construction and `wrong_carriageway` is zero because it charges only opposing surface. Reported in entry-gap bands rather than against a threshold, because a cut-off would be a choice of how much `offroad` an ego may pay to enter — a parameter this work is not allowed to add. **In the band where the ego crosses at most one of its own widths of non-drivable surface: 318 records (9.1 %) have such a corridor, median length 15.0 m at a median offset of 10.70 m, and exactly 1 of the 318 is part of the assigned route.** **130 records — 3.7 % of the index — have one covering at least half the mission**, all Waymo, split 67 train / 43 test / 14 validation, so it is present in evaluation. **Two cover the whole route**: 158.6 m at offset −26.0 m and 139.7 m at +22.0 m, entered across 0.888 m and 0.872 m. In channel units the corridor is worth `length / D_REF`: median 6.75, **maximum 71.36 against a mean mission of 40.58**, so on the worst record an ego can bank more than a whole average mission's progress on a legal parallel street and arrive outside the gate. **Spacing control**: refining the walk from 5 m to 1 m moves the figures the other way — the band grows 303 → 318 and the at-least-half subset 124 → 130, median length ratio 1.000, maximum unchanged — so the coarse figure was a mild under-report rather than an artefact of bridging short gaps. **A preliminary reading over the first 60 records was wrong and is withdrawn**: there every corridor was `LANE_SURFACE_UNSTRUCTURE`, PG junction interior, and a station-by-station trace showed one ending because the surface ended. That holds for PG and not for Waymo — 1,137 of 1,266 corridors are `LANE_SURFACE_STREET` and the at-least-half subset is entirely Waymo — and the 60 records were PG-heavy only because they are sorted by `scenario_uid`. **Still not shown**: geometry rather than behaviour, since whether a trained policy goes there is what `route_fully_outside_max_run` answers and no `eval_episodes.csv` exists on disk; and the continuity test is a lower bound in two ways, so the true exposure is at least this. **The hierarchy is not what blocks the remedy, and an earlier reading of this said otherwise.** A sub-rule *below* progress provably cannot oppose it, as this row records — but the remedy wants to sit *above* progress, and A7 is the architecture that puts it there: under the shipped six-level order the natural home is L5, relaxable lane compliance, below L4, so the off-route trajectory wins at L4 and L5 is never consulted; under A7 the same channel is `K4`, above `K5`, and in the ordered arms an in-corridor trajectory has `K4 = 0` exactly and wins before progress is compared. A7 does not shrink the remedy space, it shows the space was already empty under the shipped order (dispute raised and adjudicated with the A7 ExecPlan session, 2026-09-09). **What forecloses the remedy is the specification, in two clauses**: §1's "not … a way to put legality, heading, or lateral offset into `R4`" covers exit (b), and §8's **"runtime authority of any final lateral envelope"** covers the `K4` shape, because any such rule needs that envelope. **All three remedy shapes are foreclosed by one sentence**, `driving_mission_v1.1_specification.md:104`, which lists in order "continuity/clamp/freeze/recovery/accumulated-travel/HMM protocols" (which forecloses `C50`'s cursor bound), "off-route `R4` zeroing" (exit (b)) and "runtime authority of any final lateral envelope" (the `K4` shape). So the position is not that no remedy has been found but that **every shape that would work was withdrawn by one approved document**, which is the case `AGENTS.md`'s Scientific Argument Standards names: when the only argument against something is the specification, that is a finding and usually the specification is the thing to fix. **And the scalar arm cannot be fixed this way at any admissible weight** — priced by the A7 session and **verified independently** from the A7 block and `_validate_six_level_weights`: a violated `K4` step costs `w₅(1 + σ)` against `λ₄ Δq` of progress, so the crossover is `w₅ = λ₄ Δq / (1 + σ)` and §5.4 caps `w₅` at `(a − λ₄ ΔQ_MAX)/(1 + σ)`. Cap **0.384615**; crossover **0.313983** at the expert's mean `Δq = 0.204089`, admissible; **1.538462** at the clip, inadmissible. So a weight exists that opposes a *slow* off-route drive and none that opposes a *fast* one, which is the wrong way round since speed banks the exposure sooner, and at the recommended `w₅ = 0.15` a fully violated step costs 0.195 against 2.000 of progress at the clip. **The factor of four is algebraic**: the ratio is `λ₄/(a − λ₄ ΔQ_MAX)`, in which `σ` cancels, so it is exactly 4 for every admissible severity and no re-tuning of `σ` reaches it. Corroboration of the relayed pace from an independent source: 40.58 channel units per mean mission at that `Δq` implies 198.8 steps against a median episode of 199. That makes it a third measured asymmetry in the same direction as the collision one. **The decision is unchanged in kind and now has its magnitude**: 3.7 % of missions, up to a whole mission's worth of progress. |
| D15 | **`γ = 0.996` (ADR-081) spends ordering O3, and neither the ADR nor the specification recorded it.** ADR-075 chose `γ = 1` partly because it is the unique value for which `Σ_t Δq_t` telescopes: two trajectories reaching the same place then tie at L4, and L5 decides — which is what makes O3 a consequence of the *placement* of relaxable lane compliance (ADR-076) rather than of a calibration. Its own table showed O3 failing at every `γ < 1`. ADR-081 moved `γ` to 0.996 on two arguments that are not in dispute (no Bellman contraction at `γ = 1` while ~2/3 of episodes end in a bootstrapped truncation; hierarchy inversion unless `ln(a)/−ln(γ) > 199`), but did not restate that table, and `RULEBOOK-V5.1` §1.1 and `AC-RB5.1-12` continued to report O3 as passing. | 2026-09-08 (measured while auditing the mission-gate/reward link) | **Open decision, two exits.** Measured at the shipped value against the §4.6 reference shortcut, `λ₄ = 2.0`, `η = 1.0`: the scalar margin is **−3.5789** where it was **+0.2000**, and under the ordered arms the comparison resolves at **L4** rather than L5 — so the level ADR-076 introduced to separate an illegal shortcut is never consulted, which is a structural loss rather than a degraded margin. Full table in `RULEBOOK-V5.1` §4.4, executed by `TEST-RB5.1-16c`; the shipped row is guarded by `TEST-RB5.1-16b`, which reads `γ` from the configuration rather than a literal. The mechanism is algebra, not calibration: a discounted sum weights the same increments by recency, so a trajectory delivering them sooner scores strictly more, and no `(λ₄, η, λ₆)` restores the tie. Two things bound how surprising this is: O3 held at `γ = 1` by only **+0.20 on a return of 78**, because `λ₆` sits just under its §4.6 O3 bound and the scalar arm was already spending nearly all the time preference the ordering can afford; and the three obvious remedies were falsified in ADR-075 (a threshold on L4, potential-based shaping, raising `η` against `λ₄ + 0.1·η < a`). **The exits are (a) restate O1–O6 as properties of the undiscounted channel sums and report the discounted ordering as a measured result, or (b) reopen `γ` against ADR-081's two arguments.** Recorded as `RULEBOOK-V5.1` §11.12 and `REQ-RB5.1-O3-DISCOUNT`. Not decided here. |
| C46 | **The ADR register in `docs/project_index.md` omits 15 of the 82 ADR files, and two unrelated ADRs share the number `ADR-025`.** Missing rows include `ADR-005`, `ADR-006`, `ADR-029`, `ADR-030`, `ADR-033`, `ADR-034`, `ADR-035`, `ADR-054`, `ADR-055` and `ADR-082`, all `APPROVED`; several are cited by number from the specification rows of the same file, so the omission is inconsistent rather than uniform. `ADR-025-r2-lateral-rss-clearance-replacement.md` (registered) and `ADR-025-vehicle-yield-latch-cleanup-decoupled-from-zone-selection.md` (unregistered, `Approved`, same date) both claim `ADR-025`. | 2026-09-08, documentation audit | Filed as a **register gap**, not as stale text: no row currently asserts anything false, and an absent row is not a claim. Renumbering the duplicate is not a documentation-only change — the identifier is referenced from prose elsewhere — so it needs a decision on whether identifiers may be reassigned when neither side has been published, which is the same question the `C13`/`C40` collision note in this file already answers for open-item identifiers but not for ADRs. |
| C47 | **The retained legacy `td3`/`sac` presets carry `batch_size: 128`, which matches neither `RL-BASELINES` (`256`) nor ADR-078 (`512`).** `conf/agent/planner/algorithm/td3.yaml:7` and `sac.yaml:7` set `128`, with no `update_to_data_ratio` and a legacy `auto` that resolves to the transitions collected in the last rollout (`src/thesis_rl/agent/planners/algorithms/sac.py`, `td3.py`), while ADR-078's affected configuration is the `_sb3` pair only. Both files declare themselves in their own header as "the temporary legacy baseline while fork-backed SB3 paths are consolidated". | 2026-09-08, documentation audit | No approved decision is on record for `128`. Recorded rather than reconciled, because the two candidate fixes point opposite ways: either the legacy presets are aligned (a configuration change to a path the thesis does not train on), or they are declared out of the specification's scope (a scope statement in `RL-BASELINES`). Amendment `RSA-4` states which configuration the specification's numbers describe and points here. |

## Deferred — postponed on purpose

| # | item | source | notes |
|---|---|---|---|
| F2 | **Scalarizer binarisation (F0)** deferred to a separate ExecPlan. | Rulebook v4.10 ExecPlan | |
| F3 | **Calibrated stochastic tracking** deferred; ideal semantic tracking remains an explicit baseline limitation. | `OBS-V1.2` | |
| F4 | **`MOTORCYCLIST` collision class** deferred. | ADR-027 (`DEC-R1-07`) | **Restated 2026-09-01: not deferred pending a measurement — the measurement cannot be taken.** `DEC-R1-07` deferred this "pending an empirical check of motorcycle frequency in the selected scenario pool", but the frozen Waymo records carry exactly three object types (`VEHICLE`, `PEDESTRIAN`, `CYCLIST`; sampled over 12 records: 634/113/1), and **no motorcycle label exists in the source taxonomy**. You cannot count what the data does not label, so the gating check is unanswerable and the item cannot close on its own terms. Convert to a **declared limitation**: motorcycles, where present, sit inside `VEHICLE` and are priced with the car-occupant injury curve, which under-estimates their risk. Adding the class would require a classifier the dataset does not support. |
| F5 | **ACL implementation stage v3 not started, v4 deferred**; runtime learner smoke, live ScenarioNet/Rulebook wiring and resume validation pending before `VERIFIED`. | ACL ExecPlan | |
| F7 | **The PER sum tree is rebuilt leaf-by-leaf in Python at load.** `__setstate__` restores the leaves and walks the tree one leaf at a time. | 2026-09-07, code audit (block B, mechanical list item d) | **Deferred, with the measurement, because the blast radius is currently zero.** Measured **3.34 µs per leaf**. The audit called it "O(minutes)"; it is not. At the **production** allocation it is about **1.0 s**, not the 20 s first recorded here: every profile configures `buffer_size: 300000` and SB3 divides that by `n_envs`, so the tree holds `15 000 x 20 = 300 000` leaves, and the 20 s figure was measuring a 6M-leaf tree twenty times larger than anything the repository builds. (The same inflated size briefly inflated `D13`; both are corrected.) A bottom-up build (write the leaf slice, then vectorised pairwise sums) is milliseconds. It is not urgent because `replay_persistence: false` on every non-smoke profile (`C19`), and the smoke buffers carry 24 000 rows, about 0.08 s. **One property to carry into that change**: the current rebuild is **already not bit-exact** with the tree it replaces — building one tree in insertion order and one in index order from identical priorities gives `array_equal → False`, total mass differing by 5.5e-12 — so bit-exactness is not an obstacle to vectorising it, but neither should the change be sold as preserving it. Sequence it with `V2`/`D11`, whichever turns replay persistence back on. |
| F8 | **`RoutePolyline.project` is a pure-Python scan over every segment, and the drivable-surface filter calls it once per lane, several times per step.** `rulebook/v2/geometry/route.py:147` builds a `RouteProjection` for each of the ~360 segments of a Waymo centerline and then reduces them in Python. `drivable_surface_for_ego` (`rulebook/v2/geometry/drivable.py:85`) calls it on **every** lane of the scenario (~180 on the profiled Waymo record) only to read `z_m` for the vertical-compatibility gate, and it is itself invoked about six times per step on identical inputs: `scene_context.is_physically_out_of_road` from `done_function`, `_is_out_of_road` and the reward path, plus `evaluate_transition`. | 2026-09-08, cProfile of one case of `test_reward_return_ordering_on_validation_panels` (`a_native`, `validation_waymo_empirical`, single env, `env.vectorized.enabled=false`) | **Deferred, measured, not fixed: it is production numerics and needs an approved equivalence test.** Profile (three episodes, 593 steps, profiler overhead ~3x included): `project` called **890 952 times — about 1 500 per step**, 601 s of 891 s cumulative (67 %); 322 M `planar_distance` calls; callers 644 903 from `drivable_surface_for_ego`, 107 333 from `carriageway_surfaces_for_ego`, 76 288 from `_build_static_v12`. Env build is 16–17 s per behaviour and reset 2 s, so the episode *step loop* is the cost, not construction. It is also why the panel split is asymmetric: **the Waymo cases take 588 s and 584 s, the PG cases 41 s and 41 s** (gate log `20260908T145526Z`), so after `pytest-xdist` the gate floor is one Waymo case, ~10 min. Two semantics-preserving changes would remove most of it, in this order: (1) memoise the drivable surface per `(step, ego pose)` so the six calls per step compute once; (2) vectorise `project` with NumPy, keeping the vertical filter, `GEOMETRY_EPSILON_M` ties, `previous_s_m` continuity and `max_s_jump_m` exactly, verified by an equivalence test against the current implementation on the frozen panels. Both touch the training step too (the same code runs in every vector worker), so the effect is not confined to the test. Profile artefact: `outputs/profile_return_ordering_a_native_validation_waymo_empirical.prof` on `gh200-1` (git-ignored). **Fixed 2026-09-08** on `worktree-f8-route-projection-hotspot`, ExecPlan `implementation/route_projection_hotspot_f8_v1_exec_plan.md`, in the two steps above and with the equivalence criterion the user chose the same day: **not bit-exactness but a declared tolerance** — the NumPy `project` must select the **same `segment_index`** as the pre-fix Python implementation on every probe point and return `s_m`, `z_m`, `lateral_distance_m` and `tangent_xy` within **1e-9 m** (`DEC-F8-001`; coordinates are O(1e2) m, so ~1e-11 relative, six orders below `GEOMETRY_EPSILON_M`). Step (1) became two semantics-preserving gates rather than a cache of the surface: a lane's projected `z_m` is a convex combination of two consecutive centerline elevations, so the centerline's closed z-range decides the vertical gate whenever it clears the tolerance by a 1e-6 m guard band and only straddling lanes are projected (`drivable.py::_vertical_compatibility_from_range`), and `SceneContextAdapter` memoises the full-footprint exit per `(adapter, ego pose)` so MetaDrive's native done/reward/cost paths and the thesis `done_function` build it once (`scene_context.py`). Step (2) runs the reference arithmetic once over per-polyline NumPy arrays built lazily (`route.py::project`); the reference algorithm is kept verbatim in `tests/test_route_projection_equivalence.py::_reference_project`, which checks synthetic routes covering each rule (straight, zigzag, roundabout self-approach where the continuity preference decides, ramp, seeded random walks) and the routes plus lane centerlines of frozen validation-panel records of both sources, tens of thousands of probe/option pairs, plus argument-validation parity. Pre-fix evidence executed by temporarily shelving `src/` (technique 3 of `docs/workflows/agent_operations.md`): the memo test failed with 3 computations against 1, the pre-classification test with extra lanes projected. **Measured** (same case as the profile, `a_native` × `validation_waymo_empirical`, 593 steps, no profiler, step loop only): baseline **1135 ms/step** (673 s); after step (1) alone **929 ms/step** (551 s, −18 %, measured by swapping the reference `project` back in); after both **312 ms/step** (185 s, **3.6×**; wall 710 s → 217 s). Per call on the 357-segment route of `sd_waymo_training_20s_141f284b8bb5d418`: 743 µs → 23 µs (×33). The three behaviours' returns and route completion are identical to every printed digit before and after, so runs A and B of the learnability screening started before this fix are comparable with later ones up to the declared tolerance, which this case did not even exercise. Step (1) alone would not have been enough, and the caller count did not tell the story: the 644 903 lane projections were only 18 % of the step. Remaining cost after the fix is profiled and recorded in the ExecPlan §11 and in `F9`. |
| F9 | **After `F8`, the Waymo step is the semantic observation's static-feature tangent, not the Rulebook.** `envs/observations/causal_semantic.py::_local_tangent_rad` builds one Shapely `LineString` per boundary segment of every local map feature and calls `distance` on each to find the nearest segment's orientation; `_build_static_v12` calls it ~128 times per step. | 2026-09-08, cProfile of the `F8` case after the fix (`a_native`, `validation_waymo_empirical`, 593 steps, 283 s of step loop under the profiler) | **Deferred, measured, not fixed.** Semantic observation 153 s (**54 %** of the step), of which `_build_static_v12` 123 s and `_local_tangent_rad` 105 s: 75 904 calls, 5.7 M `LineString` constructions, 6.5 M `distance` calls. Second: Rulebook transition 100 s (35 %), with `lanes.py::associate_route_lane` 36 s — 61 calls per step (one per actor) reading `polygon.bounds` on every route lane, 6.9 M `bounds` calls in total, because Shapely recomputes `bounds` on each access. `project` is 4 %. Both have the same shape as `F8` — a Python loop over geometry primitives that NumPy (the nearest-segment search is the same arithmetic as `project`) or a one-off cache (`bounds` per lane at reset) removes without changing the result — and the same equivalence obligation: the tangent feeds `OBS-V1.3` `REQ-006`, so it needs an equivalence test against the current implementation before it is touched. Not done here because it is observation code under its own specification, not Rulebook geometry. Expected gain, from the profile: the Waymo case from 312 ms/step to roughly 150 ms/step. |

## Known defects — identified, not fixed

| # | item | source | notes |
|---|---|---|---|
| C50 | **The mission-progress clip is symmetric while the magnitudes it bounds are not, and its negative side is an unbounded ratchet.** `Δq = clip(Δs / D_REF, −1, +1)`. Forward, vehicle physics already caps `Δs` at `v_max · Δt = 2.2222 m`, so the positive clip protects against nothing real. Backward, a route-projection branch jump is unbounded and the clip charges it `−1` instead of its true magnitude. **Executed, not deduced** (`docs/audits/rulebook_architecture_2026-09-09/g6_ratchet.py`, real `RoutePolyline`, hairpin of 84.03 m whose legs are one lane apart, 1 m per step, projecting sequentially as the tracker does): driving the route honestly pays 37.81 against a telescoped 37.81; a **closed loop with net displacement zero pays +36.00, then +72.00, then +108.00** for one, two and three laps, telescoping to 0.00 each time, with a worst single `Δs` of **−84.03 m** charged −1. **At `λ₄ = 2.0` that is +72 reward units per lap, linear in the number of laps and bounded by nothing in the reward.** | 2026-09-09 (`docs/audits/rulebook_architecture_2026-09-09/`) | **What blocks it today is the geometry of the frozen panels, not the reward**, and that is a weaker guarantee than it sounds. The 2026-09-05 read-only audit over all 3,500 frozen missions, sampling each canonical route every 1 m, found **zero** routes containing two portions with `|Δs| > 15 m` closer than 6 m in plane, and zero closer than 3.5 m (`docs/implementation/route_coordinate_driving_mission_v1.1_exec_plan.md:262-268`). Three reasons that does not close this: the same paragraph states the standing constraint itself — *"the equivalence above is a property of the frozen population, not of the code. Any regenerated or extended frozen index must re-run the near-revisit audit"*; **the re-audit script does not exist** (`V3`), so the standing constraint is not executable; and the audit's threshold is `|Δs| > 15 m` while **the ratchet needs only 2.2222 m** per step, so revisits between one clip width and 15 m — tight hairpins, roundabout entries and exits — are not covered. **A candidate remedy already exists and does not bind**: `ROUTE_CONTINUITY_JUMP_FACTOR = 2.0` gives the post-state projection a plausibility bound and ADR-035 documents it as **"a preference, not a gate"** — *"when no candidate is plausible the bound is abandoned"* (`ADR-035…:171, 180, 269`). Promoting it, or defining what happens when no candidate is plausible, adds no rulebook level, no weight and no observation field, because the constant is already there. Note that **removing the negative clip instead would not touch §5.4**: that condition bounds the maximum *gain* below a level, and an unbounded negative `Δq` cannot overturn anything — but it would make the per-step reward unbounded below, which is its own cost. **Raised in priority by the architecture review, and A7 was approved on 2026-09-09**, so progress is the last unthresholded objective: inside the safety budgets it is the *only* gradient, and this channel's integrity is the whole signal. Not decided here. **2026-09-09, two things measured and one premise corrected.** *Measured*: `V3`'s audit now exists and the frozen population is clean at the threshold that matters. With the ego on the route, 1 of 3500 routes has a positive under-charge and it is **+0.028 channel units**; zero pairs anywhere in the index exceed twice the clip width at one step of travel. At the lateral reach this exploit actually needs — `g6`'s two legs are one lane apart, so 1.75 m off route — the largest fold excess in the index is **0.247 m** against the hairpin's ~80 m, and at 5 m off route it is 3.226 m. A fold excess of a quarter of a metre cannot close a loop, so the worst case here is a one-off of 0.677 channel units rather than a per-lap ratchet. **This makes `C50` robustness for a future panel, not a live exposure.** *Premise corrected*: `ROUTE_CONTINUITY_JUMP_FACTOR` does **not** exist as a constant in the code, so there is nothing to "promote from preference to gate" in production. The bound was wired in on 2026-07-30 (`b30763a`, `be7c694`) through `evaluate_progress`, and **`e63e0bf` removed it on 2026-08-03** with the driving-mission v1.1 refactor: `mission/tracker.py::project` now documents itself as projecting "without a jump envelope or clamp", `transition.py` passes `previous_s_m=None`, and no production call site passes `max_s_jump_m` at all. The fix is therefore to *re-introduce* the bound at the tracker, plus the no-plausible-candidate semantics, plus the value 2.0 as a real constant — still no rulebook level, weight or observation field, but not free either. See `C52` for the documentation half. **Two further facts, both verified 2026-09-09.** (a) **A bound at ADR-035's factor of 2.0 would not close the ratchet**: the cursor could then move `4.44 m` in a step while `Δq` saturates at `2.2222 m`, so a retreating step still banks one channel unit and the g6 loop nets about `+18.8` per lap instead of `+36`. The bound that *does* close it is one clip width exactly, at which `clip(Δs/D_REF, −1, +1)` becomes the identity, the telescoping identity `AC-RB5.1-07` asserts becomes true by construction, and no closed trajectory can bank progress — algebraically rather than as a property of a population. ADR-035 rejected `k = 1` as one that "would deprioritize legitimate motion", an argument made without a measurement; `V3` measures the largest one-step station advance any route in the index offers an on-route ego at **2.284 m = 1.028 clip widths**, so `k = 1` truncates one route's one step by 6.2 cm. (b) **But `DRIVING-MISSION-V1.1` §8 lists "continuity/clamp/freeze/recovery/accumulated-travel/HMM protocols" among the obsolete elements to remove**, and a per-step station cursor is a continuity protocol by any reading, so that candidate re-introduces a mechanism the approved specification withdrew rather than filling a gap. **Removing the negative clip instead is not a one-line change either**: `_canonicalize_bounded` (`reward/scalarization.py`) range-checks the progress margin to `[−1, +1]` and raises `ScalarizationEvaluationError` outside it, and boundedness is the property SCAL-V1.1 through V1.4 rest their admissibility arguments on — so it violates the margin contract at the scalarization boundary, not merely the reward's lower bound. §5.4 is untouched by a negative increment, as recorded above: verified in `_validate_six_level_weights`, whose `tail` term is built from `DELTA_Q_MAX = 1`, the maximum *positive* progress. **Recommendation, not a decision**: change nothing in the reward while the measurement says the exposure is not armed, and keep the cursor bound at one clip width as the fix if a regenerated index fails `V3`'s audit. |
| C52 | **ADR-035 documents a route-projection continuity bound that production has not applied since 2026-08-03, and `RoutePolyline.project`'s docstring promises the opposite of what its own code does.** ADR-035 §"Route-projection continuity bound (`ROUTE_CONTINUITY_JUMP_FACTOR = 2.0`)" states in the present tense that *"the post-state projection is now given a plausibility bound of `2 * v_max * delta_t`"*. It was: `evaluate_progress` passed `max_s_jump_m = ROUTE_CONTINUITY_JUMP_FACTOR * cap * delta_t_s` from 2026-07-30 (`b30763a`, `be7c694`). **`e63e0bf` removed it on 2026-08-03** with the driving-mission v1.1 refactor, which moved `R4` onto the mission tracker's `delta_s`; `mission/tracker.py::project` documents itself as projecting *"without a jump envelope or clamp"*, `transition.py` passes `previous_s_m=None`, and **no production call site passes `max_s_jump_m`** — `grep` finds it only in `geometry/route.py` and three test files. The constant `ROUTE_CONTINUITY_JUMP_FACTOR` no longer exists anywhere in `src/`; two code comments still cite it by name. Separately, `RoutePolyline.project`'s docstring says *"When no candidate is within the bound the projection fails closed rather than selecting an implausible branch (v4.7 §3.4 forbids silent recovery)"*, while the inline comment ten lines below and the code both keep the unbounded selection — the two statements contradict each other in one function. | 2026-09-09 (`docs/audits/rulebook_architecture_2026-09-09/`) | **A documentation defect, not a behavioural one**: the removal was deliberate and the docstring of the code that replaced it is accurate. What is wrong is that three documents describe a bound the runtime does not have, and `C50`'s candidate fix was scoped against them — "the constant is already there" is false. Classification per `AGENTS.md`: a defect of the repository, since it misleads any contributor on any machine. The `project` docstring is a one-line fix. ADR-035 is approved, so amending its claim is a user decision rather than a correction to make unilaterally; the minimal honest form is a dated note recording that `e63e0bf` withdrew the bound and that ADR-053 and DRIVING-MISSION-V1.1 are where the replacement lives. |
| C48 | **`FINAL_GATE_BUILDER_EPSILON_M = 0.01 m` splits physically contiguous carriageway, so the frozen final gate stops a strip short of the road it is meant to span.** The gate builder merges cross-section intervals only when they are within 0.01 m (`src/thesis_rl/mission/builder.py:26,335-341`) and then keeps **the single merged component containing offset 0** (`:342-349`). Where two adjacent same-direction lane polygons leave a centimetre of source-geometry slack between them, they become separate components and the further one is discarded. Measured on all 3500 frozen records: the six smallest gaps between the gate and an uncovered same-direction component are **PG at 0.010-0.011 m** — i.e. the tolerance itself — beside gates that are *already* 11.5 to 34.7 m wide (`PGMap-19921647`: gate `[-2.09,+11.48]`, discarded strip `[+11.49,+29.61]`, gap **0.010 m**; `PGMap-14920532`: gate `[-14.91,+19.75]`, strip `[+19.76,+24.71]`, gap **0.010 m**). Nothing here is a *near miss* of an intended exclusion: an 18-metre strip of the same carriageway is dropped over one centimetre. | 2026-09-08, `docs/audits/final_gate_carriageway_coverage_2026-09-08/` | **Open, not fixed, and the fix is not obvious enough to guess.** Consequence bound: mission success needs the swept front bumper to intersect the *finite* segment (`mission/gates.py:35-71`), so an ego finishing on the discarded strip fails a mission it drove correctly. Frequency is unmeasured — the audit counted gaps, not how often an ego ends up there. **Raising the tolerance is the obvious move and is not safe on its own**: it is the same constant that keeps a genuinely separate carriageway from being merged into the gate, so a larger value trades this defect for the opposite one, and `builder.py:346-348` raises `final gate anchor component count is N` when the merge produces an ambiguity — meaning a careless change turns silent narrowing into a dataset-wide build failure. The measurement to size it before choosing is cheap and specified: the gap distribution is already in `coverage.json`, so the question "is there a tolerance that closes every intra-carriageway gap without merging any distinct carriageway" is answerable by inspecting that column against each record's lane membership. Affects the frozen artifacts, so a change means regenerating all 3500 gates and a new builder identity. |
| C2 | **`SIGNAL` never selected at runtime on 209/828 (25.2 %)** of Waymo scenarios carrying `has_route_traffic_light=true`, after the route-membership fix reduced it from 57.1 %. | ADR-051 | A traffic-light rule that never fires on a quarter of the scenarios that have traffic lights. **25.2 % is an upper bound, not the defect size**: ADR-051 splits the 209 into 52 lost at adapter construction and 157 that are *"genuinely unrelated approaches **or** route ends more than one lane short"* — a mix of correct exclusion and recoverable shortfall that the single-hop version cannot separate. **Decomposed 2026-09-01** (`M8b`) on the 1100-record panel: `SIGNAL` 313 of 744 controls dropped by route reachability (42.1 %), **`STOP` 400 of 536 (74.6 %)**, 122 records with no selectable signal, and 13 569 controls lost earlier at adapter construction. So the route filter accounts for 122 records — the part `M9`'s multi-hop extension could recover — and adapter construction for the rest, a different defect with a different fix. **`STOP` is the worse case and was never measured before; this item should be restated to cover it.** Matters because `signal` sits at L3, above progress. **Parked by explicit user decision 2026-09-01**: revisit only if training runs and test animations show traffic lights are still not being identified. The corrective reading that made parking reasonable: `M8b`'s drop counts are *mostly correct* drops -- a four-way junction carries a control per approach and the ego uses one, which is why `waymo_static_adapter.py` calls them "correctly absent, not a data defect" -- and the selectable counts corroborate it (744-313 = 431 selectable signals against 409 records where the rule fires; 536-400 = 136 against 115). Only the **122** records that carry signals but can select none are suspect, and ADR-051 says even those mix correct exclusion with recoverable shortfall. |
| C3 | **M0 control-drop diagnostic counters** are collected but aggregated into no cross-episode report table. | Rulebook v4.10 ExecPlan | **This is `C2`'s missing reporting, not a separate defect.** `control_line_diagnostics(cache)` (`src/thesis_rl/rulebook/v2/transition.py:556`) already computes the split and is already static per scenario; only the cross-episode aggregation is absent. **Closed 2026-09-01**: the aggregation exists and its output is in the report (`control_line_coverage`). |
| C7 | ([issue #2](https://github.com/EmanueleRsp/thesis-metadrive/issues/2)) **The typed data abort works only on the vectorized path.** With `env.vectorized.enabled=false` a `RuntimeScenarioNotEvaluableError` is fatal instead of being handled as an ordinary scenario data abort, and the run then exits **139** (SIGSEGV) during teardown with the MetaDrive engine still alive. | 2026-09-02, diagnostic run during `AB-LEARN` `M2` | **Cause located, not fixed.** The conversion machinery lives entirely inside the subprocess vector environment: the worker catches the error (`deterministic_subproc_vec_env.py:145-147`) and hands the parent a `RuntimeScenarioDataAbort` marker (`:630`). `RuleRewardWrapper` only *annotates and re-raises* (`rulebook/v2/wrapper.py:277`), explicitly deferring the handling to "the parent-side truncation bootstrap". Remove the worker/parent boundary and nothing performs that bootstrap, so the exception reaches `train_loop.py:1565` and terminates training. Observed on `evaluate_signal_transition` (`components/controls.py:222`) rejecting a signal whose pre/post states are `UNKNOWN` -- an ordinary data condition that ADR-024 / `RSA-1` classifies as a typed abort, not a failure. **Does not affect the production runs**: every official configuration is vectorized, and the `AB-LEARN` smoke passed on both arms with 20 environments. It bites anyone who sets `vectorized.enabled=false` to get a cleaner traceback while debugging -- which is exactly how it was found, and it cost a diagnostic run. Two candidate fixes, neither attempted: hoist the abort conversion above the execution boundary so both paths share it, or declare the single-environment path unsupported for `env=scenarionet` and fail fast with a clear message at construction. The exit 139 is a secondary symptom of tearing down the engine after an unhandled exception, not an independent defect. |
| C8 | **A run's own provenance artifacts mislabel which rulebook produced it, and two artifacts of the same run disagree with each other.** `artifacts/run_metadata.yaml` recorded `rulebook.implementation_family: v1`; `checkpoints/*.reward_semantics.json` recorded `4.7-final-implementation-complete` for the same run; the run actually drives the **v2** machinery on `RULEBOOK-V5.1`'s six-level vector, and `specification_id` was `not-applicable` in both. | 2026-09-05, launch of `AB-LEARN` `M3` seed 0 | **Mostly closed 2026-09-06** by `C8-PROV` (`docs/implementation/rulebook_provenance_identity_bugfix_exec_plan.md`). *Cause*: `conf/config.yaml` declared only `rulebook.version`, and the two readers substituted **different** values for the undeclared keys -- `runtime/io/metadata.py:207` the literal `"v1"`, `contracts/reward_semantics.py:47` the version string -- while a third classifier, `runtime/wiring/builders.py:349-358`, routed that same string to the **v2** adapter. Three classifiers of one fact, three answers, and a missing key silently becoming a *wrong* record rather than an absent one. *Fix*: all three fields declared explicitly in `conf/config.yaml` as `v2` / `RULEBOOK-V5.1`, both writers routed through one shared resolver `rulebook_provenance`, and an undeclared field now reads `not-declared`. Verified by reproducing the divergence against the pre-fix tree before fixing it; 1616 tests pass. **Never behavioral** -- the instrument was always v5.1, which `_canonicalize_bounded` enforces by fail-closing on any margin vector that is not length 6. **Open half still open after an attempt on 2026-09-08. `DEC-C8-004`'s stated reason is wrong; its conclusion is right, for a reason it did not name.** `RULEBOOK_V2_VERSION` reads `4.7-final-implementation-complete`, claiming a specification version two releases behind the six-level vector implemented since `RB51`. `DEC-C8-004` deferred the rename because the string "is also stamped into cached rulebook-catalog provenance, where a change invalidates the cache" — **that is false**: `filter_rulebook_v2_catalog.py` decides cache reuse on `relative_path` and `scenario_fingerprint` (`:95-97`) and only reads the version through (`:102`). On that basis the rename was made, and **the full suite caught the real constraint**: `geometry_config_hash()` (`rulebook/v2/config.py`) hashes a payload containing this string, and `scripts/verify_rulebook_eligibility_stability.py:152` compares that hash for **equality** against the one frozen into the dataset's eligibility provenance — `71d5cdb8…208d6` in `docs/audits/dataset_construction_2026-09-05/rulebook_eligibility_stability.json`. Renaming the string therefore moves a **dataset provenance identity**, which `AGENTS.md` reserves for explicit user approval, so the change was **reverted**. **To close it, decide what happens to that frozen hash**: re-verify the eligibility pass and re-record it, or accept and document a mismatch. Neither is a cleanup. **What was kept from the attempt**, because it stands on its own: the router's accepted spellings moved from an inline literal to `RULEBOOK_V2_FAMILY_ALIASES` in `runtime/wiring/builders.py`, and `tests/test_rulebook_v2_config.py` grew from one literal assertion into six that tie the three copies together — the constant against both YAMLs, both YAMLs against the router's alias set, the legacy spellings still routing, the specification being declared separately, that `version` and `implementation_family` deliberately *disagree* (with a message saying to update the test if they ever agree), and that the current geometry hash still matches the frozen one. That last test is the constraint made executable: the next attempt at this rename discovers it there instead of from a hash comparison twenty-eight minutes into a suite. **Merge timing**: `DEC-C8-002` accepts that the change alters the fail-closed checkpoint-compatibility identity, so checkpoints written before it stop being resumable; merge after the in-flight screening runs, not during. **Check not run**: `make smoke`, because the GPU is saturated by the two in-flight runs; the change touches no reward, environment or learner path and the full suite covers both writers, so the residual risk is low. Run `make smoke` before merging. *Operational note for whoever tests in a worktree next*: `third_party/` are git submodules, so a fresh worktree has them empty and the suite fails to collect on `No module named 'metadrive'`; mount the primary checkout's `third_party/` read-only. |
| C9 | **The constrained-triangulation coverage check budgeted an aggregate against a per-piece constant, and killed a screening run.** `_constrained_components_after_ear_exhaustion` discards triangulation slivers at or below `AREA_EPSILON_M2`, then required the **sum** of what it discarded to sit inside that same constant. N slivers each individually inside the per-piece bound sum to as much as N times it. | 2026-09-06, `AB-LEARN` `M3` arm B | **Fixed the same day.** Killed arm B at **100 000 steps after 7 hours**, inside `vehicle_yield` conflict-zone occupancy prediction (`predict_conflict_zone_occupancy_intervals` -> `deterministic_convex_decomposition`). **Measured, not guessed**: the original message reported no number, so the magnitude was added first -- the `C6` lesson. Reproduced three times, shortfall **1.2x-1.3x** the bound from **two to four** discarded slivers on polygons of 111-261 m2, with the shortfall equal to the discarded area **exactly** and the decomposition never escaping the polygon. In absolute terms the rejected shortfall was **0.13 mm2 on 261 m2**, a relative error of **5e-7**. Fix: budget the shortfall **relatively** -- at most one part in 100 000 of the polygon's own area, scale-free from a 1 m2 to a 1000 m2 zone. Overshoot stays a hard failure at any magnitude: claiming area the polygon does not have is not a tolerance question. Both messages now carry the measured magnitude. **The per-triangle filter is untouched, so the change is inert on every input the pre-fix code could already decompose**: verified over **1107** generated polygons, 1107 identical decompositions, zero differences -- it converts crashes into results and cannot alter a number the previous code could produce. That property is what let the fix be applied to the live tree under the running arm A without contaminating it (user decision, 2026-09-06). Regression test `tests/test_constrained_decomposition_coverage.py`, two-sided: it raises the exact pre-fix error on the pre-fix tree, and separately pins that the guard **still** rejects a material shortfall and that overshoot stays fatal. **Residual risk**: the relative bound is a new constant chosen to clear the measured cases with margin, not calibrated from a specification; a decomposition losing more than one part in 100 000 now fails loudly rather than silently, but the threshold itself is engineering judgement. |
| C10 | **A failed run leaves its container hanging for hours, so `tmux` liveness and the tail of `train.log` both report it as still running.** Arm B recorded `run_failed` at 09:47:44 on 2026-09-06; its `tmux` session was still listed at 11:19 and the last line of `train.log` was a stale `chunk_started`, which reads exactly like progress. The container exited and flushed its log only at ~11:47, two hours later. | 2026-09-06, `AB-LEARN` `M3` arm B second failure | **Not fixed; the monitoring practice is.** Directly caused a **false status report to the user**: the run was described as progressing through chunk 2 while it had been dead for ninety minutes. Same family as `C7` -- teardown after an unhandled exception with the MetaDrive engine still alive, which there produced exit 139 and here a long hang. **The authoritative liveness signal is `"event": "run_failed"` in `logs/events.jsonl`**, not the `tmux` session and not the last log line; the supervision watcher was changed to key on it the same day. The underlying teardown hang is unfixed and its cost is bounded -- a dead run wastes GPU-free wall clock until someone notices -- but it makes every liveness check that relies on process or session presence unreliable, which is worth knowing before writing another one. **Bounded 2026-09-06 by `C12`**: after `run_failed` the failure handler now closes the evaluation manager and the training environment, flushes the logs and arms a 120 s watchdog that forces the exit (`run_forced_exit` event) if teardown hangs anyway; the hang's root cause in engine teardown is still not fixed, only its duration. |
| C11 | **Arm A of the learnability screening is not a valid control: MetaDrive's native reward, under the thesis termination convention, makes a standstill the return-maximizing behaviour within reach of the learner.** Measured on arm A's own `eval_episodes.csv` (seed 0, 25k-175k, 1 887 episodes, the Rulebook attached as instrument): on `validation_pg` 853 of 1 050 episodes are stationary (`route_completion` 0, every episode a timeout, identical mean length 244.77 at five of six evaluations, longitudinal acceleration within +-0.25 m/s^2); on both panels the episodes that do not move score about 0 and the episodes that move score -9 to -40. The worst episodes are -198.9 over 200 steps and -452.8 over 465 steps, that is -1 per step: MetaDrive's `on_lane_line_penalty` (line or `crash_sidewalk` contact) is applied every step after `no_negative_reward` has clamped the dense term at 0, and ADR-058's physical-exit termination never ends such an episode, so the penalty is unbounded while standing still in lane scores exactly 0. Arm A's Waymo `route_completion` is largely inherited momentum: the logged ego starts at 5.1 m/s on average (0.0 on every PG record), the policy brakes at -7 to -9 m/s^2 at every evaluation, and the median completion of 0.09 is the stopping distance. Consequences in training: the chunk return is flat at -11 to -19, `route_completion` falls 0.11 -> 0.08, and between 150k and 175k the critic loss grows 22 -> 506 with returns spanning 0 to -450. | 2026-09-06, `AB-LEARN` `M3` seed-0 readout | **Not fixed; instrumented.** It is a design error of the screening, not of MetaDrive nor of ADR-058: the native reward was calibrated for MetaDrive's route-relative termination (4 m lateral), where the per-step penalties last a few steps, and was adopted as a control without checking that it stayed well posed under a termination that lets line and boundary contact continue to the end of the scenario. The Rulebook is not exposed to the same optimum: L6 `progress_rate` charges a stopped ego 1 per step, and on the same episodes its raw channel sum ranks standing still *worst* (PG: -247 still, -229 moving). §7.3's learner-versus-reward reading is therefore not available from arm A as configured. Two instruments added: `tests/test_reward_return_ordering_runtime.py` (integration) replays the logged expert in the live runtime and asserts `still < partial < full` on the first scenario of each validation panel for both arm rewards -- a necessary condition the native reward *passes* (Waymo: 9.5 < 198.7 < 386.4), which is why it is not sufficient; and `scripts/evaluate_constant_action_baseline.py`, which evaluates `brake`, `coast` or `random` through the same panels and CSV schemas as a run, so every curve has its floor and the neighbourhood pathology (`random` far below `brake`) is measured before a launch. Floors measured 2026-09-06 (`seed=0`, full validation panels, native / scalar mean return): brake Waymo 3.33 / -1.06, PG 0.006 / -4.90; random Waymo -36.37 / -37.00, PG 1.18 / -3.16. On PG both rewards rank random above brake, so the standstill that arm A converged to on PG is *not* a local optimum of the native reward at the random-policy level there; on Waymo both rewards rank random far below brake, the Rulebook included, because a random driver collides in 13.5 % of the episodes, which is the intended ordering. The native-specific pathology is therefore the unbounded line-contact penalty on the *trained* policy's trajectories, not a property visible from a uniform-random policy alone; the per-episode `eval_episodes.csv` of the baseline runs is retained for that reading. Whether to make arm A a valid control (switch off the native per-step penalties, or terminate arm A on line/boundary contact) was decided 2026-09-06 as `DEC-AB-007`: arm A is relaunched **unchanged and relabelled** as the descriptive baseline "MetaDrive native reward under the thesis episode contract"; it is not a control, and arm B's frozen reward is not revisited on this evidence. |
| C12 | **A run launched through a pipe has no step counter anywhere, and a failed run has no bounded end.** `docker compose run ... \| tee` allocates no TTY, the Rich `Live` training monitor renders nothing on a non-terminal console, and its step counter, fps and EMA losses exist in no artifact (`AB-LEARN` §10 M3, 2026-09-05): between two evaluations the only exact step figure is the chunk boundary. Independently, `C10`'s hang after `run_failed` kept container, `tmux` session and GPU memory alive for two hours. | 2026-09-06, `AB-LEARN` `M3` seed-0 relaunch (user request) | **Fixed the same day, observability and lifecycle only; no training semantics touched.** (1) `train_vectorized` emits a TTY-independent progress snapshot every 1 000 collected steps and at chunk end (`runtime/training_progress.py`); `train_loop` appends it to `logs/events.jsonl` as `"event": "training_progress"` with `chunk_id`, `stage`, `run_env_steps`, `chunk_env_steps`, `fps`, `elapsed_seconds`, `episodes`, `ep_len_mean`, `ep_rew_mean`, `ema_actor_loss`, `ema_critic_loss`, `update_calls`, `gradient_steps`; when the console is not a terminal the same snapshot is printed as one plain line, so the `tee` log and the pane show progress. About 350 events per `medium` run. The non-vectorized `Agent.train` path is **not** instrumented. (2) `runtime/failfast.py`: the `run_failed` handler closes the async evaluation manager and the training env on a best-effort basis, flushes logging, and arms a daemon watchdog that calls `os._exit(1)` after 120 s, recording `run_forced_exit` first; the original exception is still re-raised, so nothing that observed it before observes anything different. Tests: `tests/test_training_progress_failfast.py` (11 tests: bucket/due semantics, plain-line format, `train_vectorized` emission through a two-slot fake env under a non-terminal console, disabled channel, close/flush/watchdog behaviour). **Verified before the fix**: the 2026-09-05 pair's `events.jsonl` carry no intra-chunk event and the 2026-09-06 arm-B run stayed alive two hours after `run_failed`. **Residual**: the forced exit is exercised by unit tests with an injected exit function, not by a real hung teardown; the 120 s grace and the 1 000-step interval are engineering constants, not configuration. |
| C40 | **ADR-071's not-at-fault truncation was dead code: `done_function` read an actor-less snapshot.** `_install_mission_runtime` installs a snapshotter with `actors=()` and `contact_onset_records=()` so the mission tracker can run without the Rulebook; `_install_rulebook_v2_adapter` built the actor-complete snapshotter for the Rulebook but never replaced `_mission_snapshotter`, so in `done_function` the onset list was always empty, `_collision_is_not_at_fault` returned `False` on every step, and **every** collision terminated with a zero bootstrap while R1 charged nothing for the not-at-fault ones — the exact "worse than the status quo" combination ADR-071's own comment warns about. | 2026-09-06, code audit (block A1) | **Fixed the same day.** The adapter installation now reassigns `_mission_snapshotter` and `_mission_pre_snapshot` to the actor-complete snapshot. No test covered the real installation path (the `done_function` tests inject the snapshotter by hand). Regression: `tests/test_audit_block_a_episode_contract.py::test_rulebook_adapter_installation_replaces_the_actor_less_snapshotter`, fails on the pre-fix tree. Consequence for past runs: the 14–19 % `crash_vehicle` rate in `train_chunks.csv` includes rear impacts on a stopped ego, all terminated. |
| C14 | **The env and the Rulebook classified fault on different states.** `done_function` committed the post-snapshot into `_mission_pre_snapshot` *before* calling `_collision_is_not_at_fault`, which then read the post-state ego and the post-state actors, while `evaluate_collision_impact` reads the pre-state ego and `pre_actors_by_id`. An agent crossing the 0.05 m/s stopped threshold or the ego's rear half-plane within one control step made one side truncate and the other charge, or vice versa. Separately, `_is_agent_ahead` took `max` of the axis-aligned bounds, i.e. half the **length** (~2.4 m) as the frontal band, so lateral impacts inside it were classified frontal and unconditionally at fault. | 2026-09-06, code audit (block A6) | **Fixed the same day.** The genuine pre-snapshot is captured before the commit and passed explicitly; actors are read from it; an actor with no pre-record but present post is treated as R1 does (appeared, not at fault); the frontal band is half the shorter side of the minimum rotated rectangle. Regression: `test_fault_is_classified_on_the_pre_state_like_the_rulebook`, `test_front_band_is_the_ego_half_width_not_half_length`, `test_front_band_does_not_inflate_with_the_ego_heading` (same file), all fail on the pre-fix tree. |
| C15 | **A geometry abort in vectorized ACL mode killed the run.** The ACL episode-end callback recognised only `runtime_scenario_data_abort`; a geometry abort also has no `acl_episode_id`, so `int(payload["episode_id"])` raised `TypeError`. | 2026-09-06, code audit (block A2) | **Fixed the same day.** `aborted_acl_slots()` classifies both typed aborts and any payload without an `episode_id`. Regression: `tests/test_audit_block_a_acl_geometry_abort.py`. Never observed in a run because no ACL run has hit a geometry abort yet (`GEOM-ABORT` landed 2026-09-06). |
| C16 | **`best_*`, `latest` and periodic checkpoints written after an asynchronous validation held the live learner's weights, not the evaluated ones.** `_record_async_evaluation` called `save_intermediate_checkpoints`, which did `agent.save(...)` on the live agent; a validation takes ~35 min, so the file labelled with step `g`'s metrics contained weights from `g + Δ`. The evaluated snapshot was deleted right after. | 2026-09-06, code audit (block A3) | **Fixed the same day.** `save_intermediate_checkpoints` takes `source_checkpoint_stem` and copies the evaluated snapshot (zip and every sidecar) via `_copy_checkpoint_snapshot`; the callback passes `job.checkpoint_stem`, which is still on disk because `on_complete` runs before `_release_snapshot`. Regression: `tests/test_audit_block_a_checkpoint_and_resume.py`. **Residual**: `latest_rng_state.pkl` is still the live RNG state, so a resume from `latest` is coherent in weights and step counter but not bit-exact; with `replay_persistence: false` it is refused anyway (`C19`). Every `best_*` checkpoint of runs before this fix is mislabelled by up to one validation of training. The ACL driver path saves `latest` synchronously and was not affected. |
| C17 | **Every chunk boundary in the baseline path reset all workers mid-episode.** `train_loop` called `train_vectorized` without `initial_observations`, so `env.reset()` ran at every chunk start. The last transition of each slot was already stored with `done=0`, so the n-step sampler chained it into the next episode's rewards and bootstrapped it from another scenario's observation (`2·n_envs` corrupted transitions per chunk, sampled early under `new_transition_priority: current_max`); `n_envs` partial episodes also vanished from `train_chunks.csv`. The ACL driver already passed `summary["last_observations"]`. | 2026-09-06, code audit (block A4) | **Fixed the same day.** The baseline path carries `last_observations` across chunks (and into the PPO overshoot collection); the staged-curriculum path still resets because stage transitions change env overrides. `tests/test_audit_block_a_chunk_boundary.py` pins that `train_vectorized` honours `initial_observations` without a reset; the loop wiring itself is a closure and is not unit-tested. **Extended 2026-09-08 by `OBS-AUDIT-FIX-001` (`REQ-AF-05`, `DEC-AF-004`), merged from a branch that had fixed the same defect independently as its own `C17`:** the carry now goes through `chunk_carry_kwargs`, which also forwards `last_episode_lengths` and refuses the carry when the environment object changed, on the chunk boundary and on the PPO overshoot alike; the `curriculum_manager is None` gate is kept. Regression `tests/test_train_vectorized_chunk_continuation.py`. |
| C18 | **`context_history` covered ~10 steps instead of 21.** MetaDrive builds the observation before the Rulebook wrapper publishes the committed context, and the wrapper rebuilds it afterwards, so `_append_timestamped_context_row` ran twice per step and appended two rows to a `deque(maxlen=21)`. The stale row also mixed the previous context with the live ego. | 2026-09-06, code audit (block A5) | **Fixed the same day.** A repeated build for the same `step_index` reuses the committed row. Regression: `tests/test_audit_block_a_context_history.py`, fails on the pre-fix tree. **Checkpoint note**: the observation layout is unchanged, but the *content* of the 21-row trace differs, so policies trained before the fix saw a different feature distribution. The double `observe()` itself (≈2× observation cost) is untouched and is a design item. **Extended 2026-09-08 by `OBS-AUDIT-FIX-001` (`DEC-AF-002`), merged from a branch that had fixed the same defect independently as its own `C15`:** one batch per `(scenario, step)`, a repeated `build()` returns the batch already built, so the second observation build per step is gone as well and the row guard above is now a second line of defence. Regression `test_v13_repeated_build_for_the_same_step_does_not_duplicate_the_context_row` in `tests/test_perception_bounded_semantic.py`. |
| C19 | **Resuming an off-policy learner without its replay buffer silently trained on ~20 transitions.** `num_timesteps` is restored from the checkpoint, so `learning_starts` was already spent; with `replay_persistence: false` (every non-smoke profile) the first vector step ran 10 gradient steps of batch 512 on a buffer of `n_envs` rows, without any error. Present in **both** resume paths: the baseline loop and the scenario-ACL driver, which resumes through its own code (`train_loop.py:917` returns into it before the loop's own resume block). The ACL path is the one that can actually resume from a periodic checkpoint, because it writes `latest_checkpoint_pair.json` itself. | 2026-09-06, code audit (block A8) | **Fixed the same day** as a fail-fast: `require_replay_buffer_for_resume` (in `sb3_extensions/replay/config.py`, so both callers share one implementation) refuses the resume of a replay learner past step 0 when persistence is disabled. Whether to persist the buffer periodically (~7 GB per checkpoint) remains the `V2` decision. Regression: `tests/test_audit_block_a_checkpoint_and_resume.py`, including that both modules bind the same function object. |
| C20 | **PER could emit `inf`/`NaN` importance weights.** A prefix search can land on a zero-priority leaf (a masked data-abort slot, or float drift of the incrementally maintained sums) and the active-range wrap can move an index onto one; probability 0 gives weight `inf`, the max-normalisation turns the batch into `NaN`/0, and the critic parameters become NaN with no exception. | 2026-09-06, code audit (block A7) | **Fixed the same day.** Zero-probability addresses are redrawn from the positive mass (bounded retries, then a `ValueError` naming the inconsistent tree), and the weights are asserted finite. Regression: `tests/test_audit_block_a_per_zero_leaf.py` (forced zero-leaf hit and inconsistent-tree cases fail on the pre-fix tree). Not observed in a run; the stratified draw makes it rare, which is why it would have been a mysterious NaN rather than a reproducible failure. |

| C21 | **The horizon truncated on an emptied world, and the critic bootstrapped from it.** `scenario_time_limit_reached` stopped the episode at `episode_steps >= scenario_length`, but `ScenarioTrafficManager.after_step` despawns **every** replayed participant as soon as `episode_step >= current_scenario_length` (`third_party/metadrive/.../scenario_traffic_manager.py:111`, cleanup at `:142`), and it runs before `BaseEnv._get_step_return` builds the observation. Both counters hold the same value there (`before_step` increments `episode_step`, `_get_step_return` increments `episode_lengths`, `done_function` runs between them). So the final observation of a horizon-truncated episode — which is exactly the state its value target bootstraps from under partial-episode bootstrapping — was the only state in the whole training distribution with zero actors, and systematically the easiest one in it: no actor can violate L1 or L2 and the road ahead is clear. With `n = 3` the contamination spans the last three transitions, and with `gamma = 1` it propagated to `t = 0` undamped. The same step also earned a free maximally-safe reward. | 2026-09-07, code audit (block B1a) | **Fixed the same day.** The horizon is now `scenario_length - 1 + extra_steps_after_scenario`, i.e. the last step carrying logged data; `extra_steps_after_scenario` keeps its documented meaning (steps taken *after* the data ends) and now actually delivers it, where the frozen 0 previously still spent one step in the empty world. Regression: `tests/test_thesis_scenario_env.py::test_truncation_happens_before_upstream_despawns_the_traffic`, which pins the **ordering** against upstream rather than the constant. Pre-fix evidence: executed, the contract fails for every scenario length tried (L = 2, 10, 91, 199 all truncate at exactly the despawning step). **Episodes are one step shorter**, so returns are not numerically comparable across the fix. |
| C22 | **The terminal observation was preprocessed twice.** `Agent.train` preprocessed each worker's `terminal_observation` into `info`, `normalize_vector_transition_boundary` copied that value into `final_observations`, and the collection loop then ran the preprocessor over it again. The doubly-transformed value is what reached the replay buffer as the truncated transition's `next_obs`, i.e. precisely the bootstrap state of C21. | 2026-09-07, code audit (block B) | **Fixed the same day.** The two stages are now `_preprocess_terminal_observations` and `_attach_terminal_observations`, and the second takes **no preprocessor argument**, so the defect cannot be reintroduced by an edit that does not also change a signature. Regression: `tests/test_audit_block_b_terminal_observation.py` with a deliberately non-idempotent preprocessor; pre-fix evidence executed (the stored bootstrap row came out at 4x the raw value instead of 2x, with three preprocessor calls instead of two). **Latent, not active**: `conf/config.yaml` selects `agent/preprocessor: identity`, which is idempotent, so no completed run is affected. It would have become a silent numerical error the moment any normalizing preprocessor was selected. |
| C23 | **A typed abort was reported as a timeout.** An aborted episode has no success, no collision and no off-road, so the trailing `else` of the reason chain — spelled out inline at three sites — filed it under `timeout`. The timeout rate is the statistic used to judge how much of the training distribution reaches the horizon, and the abort rate sat at exactly zero beside it, so both were wrong in the same direction and neither showed it. | 2026-09-07, code audit (block B, promoted from non-blocking) | **Fixed the same day.** One `_episode_termination_reason` helper, `aborted` tested first, used at all three sites; the vectorized sites pass `idx in aborted_indices`. Regression: `tests/test_audit_block_b_termination_reason.py`. **Residual**: the numeric `timeout` column still means `truncated`, which an abort is; the abort count is now readable from `termination_reason` instead. Nothing was added to the CSV schema. |
| C24 | **Six encoder settings were configurable in YAML and connected to nothing.** `conf/agent/planner/encoder/lq_v3.yaml` ships `activation`, `dropout`, `attention_dropout`, `type_embedding`, `time_embedding` and `slot_embedding`; the factory passed none of them and `lq_encoder.py` hard-codes every one (ReLU in `_TokenProjection`/`_FeedForward`, `dropout=0.0` on both `MultiheadAttention` modules, embeddings constructed and added unconditionally). An override parsed, logged, reached the checkpoint's configuration record and changed nothing, so an ablation driven from those keys would have reported "no effect" for a knob that was never connected. | 2026-09-07, code audit (block B, promoted from non-blocking) | **Fixed the same day.** `_reject_non_core_latent_query_settings` refuses any of them that differs from the frozen value, in all four latent-query branches — the same discipline the constructor already applies to `num_latents` and `depth`. Regression: `tests/test_audit_block_b_encoder_frozen_settings.py`, which also pins the guard list against the shipped config and against what the module implements, so making a setting genuinely configurable forces the guard to be removed rather than left refusing a value that now works. **This is a false-negative risk in a results table, not a performance one**; no completed ablation is known to have used these keys. |
| C25 | **`train_reset_seed_unique_count` reported `0` under ScenarioNet.** The counter stays empty whenever no reset-seed function drives resets, which is the normal ScenarioNet case: the provider selects records by UID and MetaDrive's seed never enters the choice. A reproducibility column reading "zero distinct seeds" is worse than an empty one, because it looks like evidence. | 2026-09-07, code audit (block B, promoted from non-blocking) | **Fixed the same day.** `_unique_reset_seed_count` returns `None` — not applicable — instead of `0`. Regression: `tests/test_audit_block_b_termination_reason.py`. **Residual, deliberate**: the statistic that *would* be informative under ScenarioNet is the number of distinct scenario UIDs visited per chunk. It is collected nowhere; adding it means extending the runtime-stats counters in `envs/thesis_scenario_env.py`, `runtime/wiring/builders.py` and the CSV schema, and was left out of this change rather than done badly. |
| C26 | **The ACL bandit divided a per-step quantity by a per-episode one.** `compute_td3_learning_potential` returns the *mean* positive-part TD residual over an episode's transitions; `update_reward_scale` fed the EMA `abs(ep_return)`. `LP_scaled` therefore had units of 1/steps, so an arm whose episodes end early — the hard arms, where the ego crashes in the first seconds — scored a systematically higher learning potential for no reason connected to learning. Worse than the bias: an episode whose progress nearly cancels its violations has `abs(G) ~ 0`, and the clamp at `_MIN_REWARD_SCALE = 1e-3` then multiplies its LP by up to a thousand, so arms sitting near the cancellation point would have dominated the teacher on arithmetic alone. | 2026-09-07, code audit (block B5) | **Fixed the same day.** `update_reward_scale` takes `episode_steps` and stores `abs(G)/T`; both driver call sites pass it, and the vectorized ACL payload now carries `episode_length` inside `metrics` so both paths read it from one place. The state schema moves to `acl_ema_v4` because the field names are unchanged and a v3 checkpoint would otherwise load without complaint and normalize by roughly two hundred times too much. Regression: `tests/test_scenario_acl_mab.py::test_reward_scale_is_a_per_step_magnitude_not_an_episode_return` plus the extended legacy-schema rejection. **Verified separately and *not* a defect**: the LP attribution itself is correct — for TD3/SAC the residuals are computed at collection time and accumulated per `(slot, episode_id)` in `planners/core/lifecycle.py`, so they belong to the episode they are credited to. |
| C27 | **Any evaluation failure terminated training.** `AsyncEvaluationManager._fail` set `_fatal_error` unconditionally, so a single evaluator fault — an OOM while the learner happened to peak, say — discarded every hour that produced the checkpoint being measured. | 2026-09-07, code audit (block B4), user-approved | **Fixed the same day.** A failure is recorded and skipped, releasing its snapshot so the checkpoint directory does not grow by one evaluated model per failure; `max_consecutive_failures` (default 2) still aborts, because a persistently broken evaluator produces no comparable metrics and continuing would only spend the remaining budget. A success clears the streak. Regression: five cases in `tests/test_async_evaluation.py`; the original `test_evaluation_error_is_fatal` is preserved verbatim under the explicit `max_consecutive_failures=1`. |
| C28 | **The asynchronous evaluator had no device of its own.** It rebuilds from `job.cfg` and therefore inherited `device`, loading a second copy of the policy onto the training accelerator *while* the learner was mid-update — the observed out-of-memory. | 2026-09-07, code audit (block B4), user-approved | **Fixed the same day.** New `eval_device` key, null by default so no existing run changes behaviour; `resolve_evaluation_device` is applied in the worker before anything is built. Because the evaluator is concurrent with training rather than on its critical path, `eval_device: cpu` usually costs nothing in wall-clock. Regression: three cases in `tests/test_async_evaluation.py`. |

| C29 | **The checkpoint reward identity carried `priority_base` but none of the weights beside it in the same formula.** `build_reward_semantics_identity` recorded the mode, the schema id, the base, the sigmoid sharpness and the tolerance, and omitted `severity`, `flat_tie_breaker`, `progress_weight`, `relaxable_weight`, `progress_rate_weight`, `step_dt_s` and `reference_time_s`. Harmless while every one of them was frozen; ADR-081 turns `severity` into a selected parameter, at which point a checkpoint trained at `σ = 0` would resume against `σ = 0.30` with no complaint and one set of curves would carry two different rewards. | 2026-09-07, found while applying ADR-081 | **Fixed the same day**, as part of the change that made it matter. All seven fields join the identity; an undeclared key stays `None` rather than defaulting, which is the `C8` rule. Regression: `tests/test_audit_block_b_reward_identity.py`, parametrized over every field plus a separate case for `severity` (the parametrization alone would still pass if someone removed `severity` and left the other six). Pre-fix evidence is structural rather than executed — the keys did not exist, so no value of them could change the identity — and visible in the diff. **Consequence**: the identity of *every* run changes, so checkpoints written before this commit are not resumable. That break is already unavoidable through `priority_base`, and `DEC-C8-002` accepted the same kind of break for the same reason. |

| C30 | **RULEBOOK-V5.1 §7's episode counters were never cleared at `reset()`.** `_l4_clip_binding_steps`, `_l5_reached_steps`, `_ego_speed_sum_mps` and `_ego_speed_steps` were initialised in `__init__` and nowhere else, while one `RulebookV2MonitorWrapper` instance serves **every** episode of its slot — the reset path rebuilds memory, cache, adapter and snapshot but left the counters alone. Each of the three §7 diagnostics is then unreadable in a different way: `l4_clip_binding_steps` exists to be **zero** (a non-zero value says the 80 km/h cap was overridden and the Test A figures no longer bound what the agent can earn), so one override reports forever; `l5_reached_steps` grows with the slot's age rather than with the episode, which destroys exactly the "does L5 ever decide" reading §1.1 needs; and `mean_ego_speed_mps` averages scenarios the episode never visited, against a requirement (limitation 13) to report the mean **per source**. | 2026-09-07, code audit (block B, mechanical list item a) | **Fixed the same day.** Both `__init__` and `reset` now call one `_reset_episode_counters`, so a counter added to one of them cannot silently survive an episode boundary — the `C22` discipline, applied to state instead of to a signature. Regression: `tests/test_rulebook_v51_diagnostics.py::test_reset_clears_the_episode_counters`, which drives three steps, resets into an episode with a different ego speed, and asserts all four counters restart; pre-fix evidence executed, `assert 4 == 1` on `l4_clip_binding_steps`. **Latent, not active**: verified 2026-09-07 that no writer in `runtime/` or `analysis/` reads any of the three keys — they are published in `info` and consumed by tests alone, so no artifact or reported figure is wrong. The §7 aggregation that *would* consume them, `mean_ego_speed_by_source`, is not implemented anywhere; `REQ-RB51-19` is met at the wrapper and unmet in the report, which is a separate gap and is **not** closed by this fix. |

| C31 | **The vectorized ACL chunk boundary did not persist `recent_usefulness`, so a resumed teacher normalized against nothing.** The two chunk-end writers of `scenario_acl_state.json` had diverged: the non-vectorized one wrote the window, the vectorized one — **the production path**, since every serious profile runs 20 environments — wrote eight keys and omitted it, and the loader's `[float(v) for v in state.get("recent_usefulness", [])]` turned the omission into an empty window with no complaint. `_normalize_learning_potential` returns `1.0` whenever it has nothing to rank against (`len(rank_set) <= 1`), so the **first** post-resume episode fed the bandit a maximum whatever its true learning potential, and episode *k* ranked on a grid of step `1/(k-1)` until `recent_window_size = 100` episodes had refilled. At `mab.alpha = 0.10` each spurious 1.0 moves the pulled arm's EMA by about a tenth and decays over roughly ten updates, so the teacher's first decisions after a resume were noise on top of arm scores that had themselves survived correctly. | 2026-09-07, code audit (block B, mechanical list item c) | **Fixed the same day.** One `_scenario_acl_resume_state` builds every field the loader reads, and both writers now call it — the non-vectorized site keeps its extra diagnostic keys by spreading the shared payload and adding them, and the three fields it used to repeat after the spread (`mab`, `coverage`, `rng_state`) are gone, since repeating them is the drift this fix removes. Regression: `tests/test_scenario_acl_resume_state.py`, which round-trips a distinctive value through **every** field the loader reads rather than the window alone, plus a second case pinning why the window must survive (an empty window scores the first episode as a maximum by construction). Pre-fix evidence executed: the exact eight-key payload the vectorized path wrote restores `recent_usefulness == []`, length 0, with no error raised. **Confirmed end-to-end**, not only by the unit test: the production-path smoke of 2026-09-07 (`curriculum=scenario_acl_scenarionet`, `env.vectorized.enabled=true`, two chunks, driver `scenario_acl_vectorized`) wrote a `scenario_acl_state.json` carrying a **40-value** window, matching the 40 MAB updates its own log reports; on the pre-fix tree that key would have been absent from the file altogether. That run directory (`outputs/audit-c30-c36/td3_sb3/seed_42/20260907_160733`, 601 MB) is **not retained** — `/scratch` sits at 98 % — so the figure above is the record of it. **No resume-time warning was added** for a state file that lacks the key: such a file can only come from a run written before this branch, and `C29`'s reward-identity change already refuses those checkpoints, so the branch would be unreachable. **Narrower than the audit claimed** — the audit said "an ACL resume loses the window"; the non-vectorized path never did. |

| C32 | **A masked data-abort slot evicted the running maximum without releasing it, and the stale value was permanent.** The masked branch of `PrioritizedNStepReplayBuffer.add` wrote `raw_priorities.flat[i] = 0.0` and the leaf directly, bypassing `_set_raw_priority` and therefore `_update_current_max`. The sum tree stayed correct; only `_current_max_count` missed its decrement when the overwritten row was the one holding the maximum. The error is **sticky, not transient**: `_update_current_max` decrements only when the overwritten value equals the recorded maximum, so once no row holds it the count can never reach zero and the rescan that would repair it never runs — and `_set_raw_priorities_batch`'s `retained_max_count` keeps the stale value alive for the same reason. `_insertion_priority` then primes **every** unseen transition at a maximum the buffer does not contain, over-weighting it in sampling by `(stale/true)**alpha` with `alpha = 0.6`, for the rest of the run. It also survives a resume, because `__setstate__` restores both counters from the pickle. | 2026-09-07, code audit (block B, mechanical list item b) | **Fixed the same day.** The masked branch reads the previous value and calls `_update_current_max(previous, 0.0)`, so both branches share one bookkeeping path. A second boundary had to be closed to make that safe: the rescan now treats "no positive priority left" as the empty-buffer state (`1.0`, count 0) instead of recording a maximum of `0.0`, which `_set_raw_priority` would reject on the next valid insertion — reachable when a whole buffer cycle aborts, the condition `C7` describes. Regression: `tests/test_transition_replay_per.py::test_per_masked_overwrite_releases_the_maximum_it_evicts`, two-sided (the eviction case and the all-aborted cycle). Pre-fix evidence executed: `_insertion_priority()` returned **9.000001** against a true maximum of **1.0**. **The audit's direction was wrong** — it reported that the accounting "silently under-counts"; it over-states the maximum. **No completed run is affected**: the defect needs a ring wrap to overwrite a priced row, which at capacity 300 000 over 20 environments is 300 k env steps, and the `AB-LEARN` screening stopped near 175 k. Every serious run (`D4`) would have been. |

| C33 | **Every reset built the static adapter result twice and discarded the second.** `_install_causal_observation_builder` called `_build_static_adapter_result(scenario, record)` eagerly, though `_install_mission_runtime` had already built it from the same scenario and record and kept it in `_mission_static_result` — and `_get_reset_return` runs the mission installer, then the Rulebook adapter, then this builder, unconditionally and in that order. The second result was used only as the `else` fallback of the `route_lanes` choice, and on the production path that branch is never taken, because the Rulebook's `initial_cache` is installed by then and wins. So the work was not merely duplicated, it was **thrown away**. | 2026-09-07, code audit (block B, mechanical list item e) | **Fixed the same day, throughput only; no number changes.** Both fallbacks now read `self._mission_static_result`, which is the raw result the fallbacks want — `_mission_episode_cache` is the elevation-aligned one and is a different object on purpose. An explicit precondition error replaces the silent rebuild, because this file has a history of installation-order defects (`C40`). **Measured, not estimated**: 61 ms per reset on `sd_waymo_training_20s_104202f4f2590dff` (234 map features, 109 lanes) on the persisted-route branch, 254 ms on the map-matching branch. With 20 slots and ~200-step episodes at least one slot resets on ~9.5 % of vector steps, and a vector step waits on its slowest worker, so about 5–6 ms of a ~179 ms step, ≈3 %. Regression: `tests/test_thesis_scenario_env.py::test_causal_builder_reuses_the_mission_static_adapter_result`, which counts constructions and requires **zero**; the existing installer test keeps its assertion and gains the stored result in its fixture. Pre-fix evidence executed: the counter recorded one construction. **Residual risk**: the fallback now shares one object with the mission runtime and the Rulebook cache instead of holding a private copy. That is exact only because the two functions between them are purely functional — `build_episode_cache` constructs a new `EpisodeCache` that *shares* `route_lanes` by reference, and `align_episode_cache_to_live_elevation` builds shifted `RoutePolyline`s and `replace`d lanes rather than mutating them, verified by inspection when this fix was written. An edit that made either mutate in place would silently give the fallback elevation-aligned geometry where it wants raw. Note the production path is unaffected either way, because the Rulebook cache wins the choice; only a Rulebook-disabled configuration reads the fallback at all. |
| C34 | **The latent-query tokenizers asserted an invariant that holds by construction, and paid a GPU synchronisation per forward for it.** `tokenize_structured` ended with `if not valid_mask.any(dim=1).all(): raise ...` in both the v2 and v3 encoders. `valid_mask` is a `torch.cat` in which three of the concatenated blocks are literal `torch.ones((batch_size, 1))`, so every row carries at least three valid tokens whatever the data masks contain: the branch cannot be taken. Reading the result into Python nevertheless forced a device-to-host synchronisation on the critical path. | 2026-09-07, code audit (block B, mechanical list item f) | **Fixed the same day, throughput only; the branch was unreachable, so nothing behavioural changed.** Both guards removed. **Measured on the GH200**: the host read costs **14 µs** on top of the device-side reduction, and identically at batch 20 and batch 512 — the signature of a fixed synchronisation overhead rather than work. `td3_sb3.yaml` sets `share_features_extractor: false`, so SB3 runs about six extractor forwards per gradient step, and at 20 environments with `update_to_data_ratio` 0.5 that is ten gradient steps per vector step: **~61 forwards, ~0.85 ms per vector step** of a ~179 ms budget, about 0.5 %. End-to-end forward timings were **not** usable as evidence — the GPU was saturated at 100 % by the user's live run and the deltas swung by ±1.7 ms, negative included — so the isolated synchronisation cost is what is claimed here. Coverage: the invariant was **already** pinned on an all-zero observation by `tests/test_encoders_v10.py` and `test_encoders_v11.py`, which is the executed proof that the guards were unreachable; both now assert `mask.any(dim=1).all()` explicitly, tying the invariant to the removed check. **Left in place deliberately**: the second synchronisation per forward, `torch.isfinite(flat_obs).all()` in `encoders/base.py`, is the NaN/inf check AGENTS.md requires. Removing or gating it is a user decision, not a cleanup. |

| C35 | **Seventeen of the twenty keys in `conf/obs/semantic_v3.yaml` were connected to nothing.** Only the three signal-camera keys have a reader (`envs/factory.py`, the deliberate `C5`/ADR-045 threading). The token counts and history lengths are frozen inside `SemanticObservationSchemaV12` (`flat_dim = 3011`, frozen by `OBS-V1.3.1`); the radii and the prediction horizon are literals in the builder kwargs of `envs/thesis_scenario_env.py`; the LiDAR geometry is a literal the factory itself writes into `vehicle_config`. `interaction_prediction_horizon_s` is even spelled differently from the kwarg it corresponds to (`prediction_horizon_s`), which is part of why the disconnect went unnoticed. Three of the seventeen are the **causality declarations** of `OBS-V1.2` — `future_ground_truth`, `other_agent_navigation`, `future_disambiguation`, all `forbidden` — so the configuration could request a leak the contract forbids and be silently ignored. | 2026-09-07, code audit (block B, mechanical list item h) | **Fixed the same day**, as `C24` one layer down and with `C24`'s remedy: `_reject_divergent_semantic_v3_settings` refuses any of the seventeen whose value differs from the frozen one, so the YAML keeps documenting the observation accurately and a divergent override fails loudly instead of reporting "no effect" for a knob that was never connected. The three ADR-045 keys stay overridable and a test asserts the guard does not cover them. Regression: `tests/test_audit_block_b_semantic_v3_frozen_settings.py`, which loads the **shipped file** rather than a copy of it and pins four things — every frozen key rejects a divergent value, the shipped config still installs the observation, every shipped key is either threaded or guarded (so a new key cannot be added unnoticed), and no frozen key reaches the environment configuration (so if one ever becomes threaded the guard must be dropped rather than left refusing a value that now works). Pre-fix evidence executed: overriding `dynamic_radius_m`, `context_history_length`, `route_tokens` **and** `future_ground_truth: allowed` together raised nothing and produced a **byte-identical** environment configuration. **Two corrections during review, both recorded because the second is a lesson.** (1) The comparison was `int(value) == frozen` for integer-valued keys, so `history_length=5.4` truncated to the frozen 5 and was accepted — silent acceptance by the guard built to refuse it; it now compares as `float`. (2) A first attempt also **required** the three causality declarations to be *present*, on the `C8` reasoning that an absent one reads as compliant. That was the wrong layer and the full suite caught it: `tests/test_semantic_state_v3.py::test_factory_exposes_v3_as_an_explicit_observation_type` calls `_configure_agent_observation(env_cfg, {"type": "semantic_v3"})`, a legitimately partial config, and a new precondition on the runtime function broke it. The risk that actually exists is a *config file* omitting a declaration, so it is pinned over `conf/obs/*.yaml` instead — every file selecting v3 must declare all three as `forbidden` — which covers more than the runtime check did, since it inspects every config rather than the one a given run selects. **The focused test selection missed it**: `test_env_factory.py` was run and `test_semantic_state_v3.py` was not, which is precisely the coverage argument for the full suite being the gate. |
| C36 | **A payload that is static per scenario was pickled through the worker pipe on every step, and the route that was supposed to carry it was dead.** `rulebook_control_line_diagnostics` is computed once at `reset()` from the episode cache, and the wrapper put it into **every** step's info dict. The evaluation loop's `metadata_keys` reads the key from both the reset info and the last step info, and only the second ever fired, because `RulebookV2MonitorWrapper.reset` returned the inner info unmodified — so the mechanism the exec plan describes ("same mechanism as `scenario_uid`", and `scenario_uid` does arrive in reset info) never worked, and the terminal-step read saved the feature by accident. | 2026-09-07, code audit (block B, mechanical list item g) | **Fixed the same day.** Published in `reset()`'s info and removed from the step info; the only consumer keeps working, now through its intended path. **The audit filed this as throughput and that part is negligible** — measured at 5 integer keys, 195 bytes pickled, 3.9 KB per vector step across 20 workers, noise beside `rulebook`, `rule_components` and `scalarization` in the same dict. The finding worth having is the dead read. Regression: `tests/test_rulebook_v51_diagnostics.py::test_control_line_diagnostics_are_published_at_reset_not_on_every_step`, two-sided (present in reset info, absent from step info); it fails on the pre-fix tree on both halves. **Residual risk, and it is the mirror of the defect**: the key now travels *only* in reset info, and an SB3-style `VecEnv.reset()` returns observations without info. The one consumer (`agent/agent.py`'s evaluation loop) uses a raw `gym` env — it unpacks `obs, reset_info` from `reset()` — so nothing is affected today, and `control_line_coverage` in the §7 report is built by `scripts/measure_expert_rulebook_transition.py` from its own accumulators rather than from this key, checked when this fix was written. But if evaluation ever moves onto the vectorized path the key disappears silently, and **no test covers that boundary**: exercising the wrapper through the deterministic subproc vec env was judged disproportionate for a consumer that does not exist. |

| C38 | **Five of the ten `transition_replay.per` keys have no reader, and one of them currently declares a rule the code does not follow.** `td3_sb3.yaml` and `sac_sb3.yaml` ship `alpha`, `beta_initial`, `beta_final`, `beta_anneal_steps`, `epsilon`, `priority_aggregation`, `new_transition_priority`, `duplicate_update_reduction`, `sampling` and `tree_dtype`. The planners read exactly the first five (`td3_sb3.py:208`, `sac_sb3.py:203`); `resolve_transition_replay_config` reads only `beta_anneal_steps` from the block and **validates no unknown key**. The other five describe what the code hard-codes — and describe it *accurately*: the twin-critic mean of absolute residuals, `np.maximum.at` reduction of duplicate updates, `np.linspace` strata, a float64 sum tree — so they are documentation masquerading as configuration. This is the third instance of the same family in one day, after `C24` (six encoder settings) and `C35` (seventeen observation settings). | 2026-09-07, found while investigating `C37` | **Fixed 2026-09-08 on its own branch (`worktree-per-config-guard`), by user decision, stacked on the audit branch** — stacked rather than branched from `main` because this register file would otherwise conflict between the two. `_reject_divergent_per_settings` refuses any of the five whose value differs from what the implementation does, and `_reject_unknown_keys` now names an unrecognised key in the `transition_replay` block, in `persistence` and in `per` — the resolver reads every value with a defaulting `.get`, so a misspelling was indistinguishable from an absent key: `keep_last` mistyped silently restored the default the explicit value existed to override, and a mistyped `periodic_frequency_steps` silently disabled the guard that refuses it. Regression: `tests/test_transition_replay_per_frozen_settings.py`, which loads the **shipped** blocks of both algorithms rather than copies, and pins five things — each frozen key rejects a divergent value, an unknown key is named at all three levels, both shipped blocks still resolve, every shipped `per:` key is either forwarded or guarded (so a new key cannot be added unnoticed), and the frozen values match what the module implements rather than what the YAML says (`_SumTree` really allocates float64, and so on), so making one genuinely configurable forces the guard to be dropped instead of left refusing a value that now works. PPO's `{enabled: false}` block keeps resolving. Pre-fix evidence executed: four frozen keys given values the implementation cannot honour, plus `beta_annel_steps` and `keep_lastt` misspelled, produced a **byte-identical** `TransitionReplayConfig` with nothing raised. **The sharp edge is `new_transition_priority: current_max`**: it has been accurate for as long as the code implemented the prose of `REQ-013`, and `C37`'s floor made it **false on this branch** — so every `hydra/config.yaml` and checkpoint configuration record written from here would describe a priming rule the run did not use. That is an independent argument for `C37`'s revert, and it is why this item is recorded next to it rather than merged into the general guard work. The missing unknown-key validation in the resolver is the same defect one level up and should be closed in the same change. |

| C39 | **Two tests passed only because a sibling had leaked shared state into them.** `tests/test_rulebook_v2_metadrive_live.py` builds its fakes as module-level classes, and `_Traffic.obj_id_to_scenario_id` is a **class attribute**: three tests add `other-runtime -> other-scenario` to it and none removed it, so every test running later in the same process inherited the addition. `::test_metadrive_contact_recorder_accepts_node_only_callback_without_manifold_query` and `::test_metadrive_contact_recorder_clears_per_control_step` both assert the mapped identity `other-scenario` without establishing the mapping, so what they actually verified was that *some earlier test* had seeded it — a green result that says nothing about the recorder. | 2026-09-08, exposed by a `pytest-xdist` trial | **Fixed the same day** on `worktree-test-order-isolation`. An autouse fixture restores the mapping after each test in the module, which closes the whole class of leak rather than the two instances — a test added later can neither silently depend on an entry nor silently provide one — and both dependent tests now establish the precondition they read. **The fixture is what found the second one**: containing the leak made `::test_metadrive_contact_recorder_clears_per_control_step` fail, and a deterministic scan of the module (which tests read `other-scenario`, which set it) then proved those two were the only ones. Pre-fix evidence executed, and it is as sharp as this kind gets: `::..._accepts_node_only_callback_without_manifold_query` **fails when run on its own**, in 0.22 s, with `assert 'other-runtime' == 'other-scenario'`. Verified after the fix under `pytest-xdist`'s **default** distribution, which splits a file's tests across workers — 23 passed — so the dependency is removed rather than hidden by `--dist loadfile`. **No production code is touched**, and the module's fakes are test-local, so nothing outside this file can be affected. **The trial that exposed it is not being adopted, and the reason is worth recording.** `pytest-xdist` at `-n 16` was measured against the sequential gate: on the full suite it took 21m07s against 28m00s — only 1.33x, because two `integration`-marked tests take **632 s and 626 s** and landed on the same worker, and `632 + 626` is the 21 minutes. Deselecting those seven tests with the **already-declared** `integration` marker takes the suite from 28m00s to **3m54s** with no code change and no dependency, and xdist then saves a further 84 s (2m30s) — not worth a dependency. And it is **not sound on this suite**: under xdist's default distribution the fast set fails 4 tests — `test_pg_validation.py::test_bundled_export_validation_can_be_read`, `test_scenario_features.py::test_bundled_waymo_feature_extraction_is_route_aware`, and two in `test_scenario_catalog_build.py` — a *different* class from this item, sharing fixed filesystem paths rather than module state, one of which spawns its own parallel loaders. Those are unfixed and recorded here rather than chased, since nothing depends on them: the sequential gate is green. **Superseded 2026-09-08 (PR #9): `pytest-xdist` is adopted** after `test_reward_return_ordering_on_validation_panels` was split by panel; the four order-dependent tests and the reproduction attempts are `C43`, and the reason the gate floor is still ten minutes is `F8`. |

| C41 | **The two habitual liveness checks are both wrong, and nothing committed read the one that is right.** `C10` records the cost: a run recorded `run_failed` at 09:47, its `tmux` session was still listed at 11:19, and the last line of `train.log` was a stale `chunk_started` that reads exactly like progress — so the run was reported to the user as advancing while it had been dead for ninety minutes. `C10` names `"event": "run_failed"` in `logs/events.jsonl` as the authoritative signal and says the watcher "was changed to key on it the same day", but that watcher was never committed: no script or module in the tree read those events. | 2026-09-08, after being fooled twice in one session by a buffered log tail | **Fixed the same day, two independent halves.** (1) `PYTHONUNBUFFERED=1` in `compose.yaml`'s shared environment: a run started as `docker compose run … \| tee train.log` has no TTY, so Python block-buffers stdout and the tail lags. Measured this session on a live full suite: the log read 43 % while the run was in fact minutes from finishing, and separately read "unchanged for 14 minutes" while the container sat at 99.9 % CPU. This does not make the tail a liveness signal, it stops it being a misleading one. (2) `scripts/run_liveness.py` reads the event log and reports `completed` / `failed` / `running` / `stalled` / `unknown`, with exit status 0/1/2 so a supervision loop can branch without parsing prose. **The staleness threshold is measured, not chosen**: it is a multiple of the longest gap in the run's *own* event history, so a run with long evaluations is judged against its own long gaps. Whole-history rather than a recent window, and zero gaps kept rather than filtered, both deliberately: `log_event` stamps to the second and the ACL driver emits bursts inside one second, so a recent-window maximum is routinely 0–1 s and multiplying that would call a healthy run dead during any ordinary pause. The error this design can make is being slow to notice a stall, which costs idle GPU time, rather than reporting a working run as dead, which costs the run. **Two terminal signals, because neither path writes both** — the finding that `C42` records: the baseline loop writes `run_completed`/`run_failed` as events, the ACL driver writes neither, and every path updates `status` in `artifacts/run_metadata.yaml`. The reader consults both, events first, because `failfast.py` writes `run_failed` on a path where the metadata update may not be reached. Without the second signal a *successfully finished* ACL run reads as `stalled` once its silence outgrows its cadence, which is exactly the false alarm the design is meant to avoid. Tests: `tests/test_run_liveness.py`, 15 cases including the `C10` case (a terminal failure outranks a recent event), the finished-ACL case, that the initial `running` status does not mask a stall, and the refusal to guess when no cadence is measurable. **Validated against reality three times**: the post-merge smoke reported `running: last event 21s ago, within the measured cadence of 141s (tolerance 564s)` while live and `completed: run_metadata.yaml records status='completed'` once finished, and a genuinely failed run from 2026-07-29 reported `failed`. |
| C42 | **The scenario-ACL driver — the production path — emits neither `C12`'s progress events nor any run-lifecycle event.** `C12` added a TTY-independent `training_progress` snapshot every 1 000 collected steps and notes that the non-vectorized `Agent.train` path is not instrumented; the ACL vectorized driver is not either, because it collects through its own loop rather than `train_vectorized`. **And it goes further than progress**: `run_completed`, `run_failed` and `run_forced_exit` are emitted by `train_loop`, not by the driver. Measured on the 2026-09-08 post-merge smoke (`obs=semantic_v3`, `curriculum=scenario_acl_scenarionet`, vectorized, **finished successfully, exit 0**): **81 events, zero `training_progress` and zero terminal events** — all `scenario_acl_episode_ended`, `scenario_acl_reset_timing`, `scenario_acl_slow_step` and the evaluation pair. Two consequences. `C12`'s stated benefit, "a run launched through a pipe has a step counter somewhere", does **not** hold for an ACL run. And `C10`'s remedy is worse off than it reads: it names `"event": "run_failed"` as *the* authoritative liveness signal, and on the path every serious run uses **that event is never written**. | 2026-09-08, found while validating `C41` against a live and then a finished run | **Fixed 2026-09-08, and the entry above overstated the defect — corrected here.** The claim that `C10`'s `run_failed` "is never written" on the ACL path is **wrong**: `train_loop`'s `try` (`:880`) spans the delegation to the driver (`:929`), so an exception raised inside it reaches the loop's `except Exception` and is recorded there as `run_failed` with `status: failed`. The driver also already emitted `run_interrupted` itself. The real gaps were narrower: (a) `agent.train_vectorized` was called without `progress_callback` — the channel existed and the call site simply did not connect it; (b) none of the **four** completion branches emitted `run_completed`, because the driver returns to `train_loop` before the loop's own emission. Both are now fixed, with the event paired to each `status: completed` update the way the interrupt path already paired them. **The non-vectorized path keeps no progress channel**: `Agent.train` has none at all, and instrumenting a second collection loop is out of scope; it does now state its own end. Regression: `tests/test_scenario_acl_lifecycle_events.py`, structural on purpose — the defect was a call site forgetting an argument and four branches forgetting an event, which a behavioural test could only catch by running the whole runtime. It counts completion branches against `run_completed` events, so a fifth branch added without its event fails; asserts the driver does *not* emit `run_failed`, so nobody "fixes" a gap that is not one and creates two writers of one fact; and pins the two signature facts that make passing the callback sufficient — the kwarg name, and that `progress_interval` defaults to the enabled value. Pre-fix evidence executed: **all four structural tests fail against `origin/main`'s driver**. **No end-to-end smoke, deliberately**: the emission itself is already exercised by `tests/test_training_progress_failfast.py` through a two-slot fake environment, `ruff` excludes an undefined name in the four branches, and the signature assertions close the "passed but not received" gap — so a 25-minute GPU run would have confirmed nothing the fast checks leave open. One residual in the record rather than in the code: `test_the_callback_reaches_an_enabled_channel` was added after the full-suite gate had started and passes standalone; it is a signature assertion with no shared state. No result changes: the driver's own events are frequent and timestamped, so `C41` measures cadence from any event rather than from `training_progress` (an earlier version keyed on the latter and was blind here), and it reads the terminal state from `artifacts/run_metadata.yaml`'s `status`, which every path does update — without that second signal it would have called this successful run `stalled`. What is genuinely lost is the *step counter, fps and EMA losses* a human wants while watching an ACL run, and a terminal event for anything keying on the event log alone. The fix is to emit both from the ACL loop, which is bounded work but on the path the A/B runs will use, so it belongs before a long run rather than inside a merge. |
| C43 | **Four tests are order-dependent under `pytest-xdist`'s per-test distribution, and the failure is not reproducible on demand.** `C39` records them from a 2026-09-08 trial: `tests/test_pg_validation.py::test_bundled_export_validation_can_be_read`, `tests/test_scenario_features.py::test_bundled_waymo_feature_extraction_is_route_aware`, and two in `tests/test_scenario_catalog_build.py` (`::test_waymo_loader_parallel_matches_serial`, `::test_waymo_loader_deduplicates_reprocessed_batch_uid` are the two that read the shared fixture). A second trial in a separate session saw one failure on the full suite at `-n 16`; its node id and message were not recorded. | 2026-09-08, `pytest-xdist` trials | **Open, not reproduced, no fix applied.** With `pytest-xdist` adopted (PR #9) the reproduction attempts are: the `-m "not integration"` set three times at `-n 16 --dist load`, **1838 passed, 5 skipped** each, 94–95 s; the full suite at `-n 16 --dist load`, **1847 passed, 5 skipped**, 19m50s (gate log `20260908T145526Z`); the full suite at `-n 16 --dist worksteal`, **1847 passed, 5 skipped**, 19m36s (`20260908T151619Z`); the full suite at `-n 16 --dist load` with the spread collection order of `tests/conftest.py`, **1855 passed, 5 skipped**, 10m01s (`20260908T154213Z`). Static reading of the four tests: all of them only *read* `third_party/metadrive/metadrive/assets/waymo/` through a cwd-relative `Path("third_party/...")` (`test_pg_validation.py:17`, `test_scenario_features.py:204`, `test_scenario_catalog_build.py:81,94`), nothing in `tests/` or `src/thesis_rl/` writes there, and `test_waymo_loader_parallel_matches_serial` spawns two `spawn`-context loader processes (`scenarios/parallel.py:53`). So the shared resource is a fixed path, as `C39` says, but no writer has been found and the two candidate mechanisms — a sibling test changing the working directory inside the same worker, or concurrent first-compilation of `__pycache__` by freshly spawned interpreters — are hypotheses, not observations. **Rule applied**: a fix for a failure that has not been reproduced is a guess, so none is made; a green parallel gate is evidence that the suite passes, not that its tests are independent. Next time it fails, keep the node id and the assertion text, and run that file alone with `-n 4 --dist worksteal` several times to confirm. `--dist loadfile` is deliberately not used in the gate because it would hide exactly this. **Reproduced 2026-09-08 with a trigger identified: the first full-suite run in a freshly created worktree.** Three of the four failed together — `test_scenario_catalog_build.py::test_waymo_loader_parallel_matches_serial`, `::test_waymo_loader_deduplicates_reprocessed_batch_uid` and `test_scenario_features.py::test_bundled_waymo_feature_extraction_is_route_aware` — in gate log `20260908T214505Z-163d18f`, at `-n 16` on a worktree whose submodules had been cloned nine minutes earlier. **Evidence for hypothesis 2 (concurrent first-compilation of `__pycache__`), which was previously a guess:** `find third_party -name '*.pyc'` restricted to the run's own five-minute window returns **229 files**, spread across `metadrive/component/pgblock` (17), `metadrive/manager` (15), `metadrive/engine/core` (14) and `stable_baselines3/common` (20+) — i.e. the whole vendored tree was being byte-compiled for the first time *while* the suite ran, and `test_waymo_loader_parallel_matches_serial` spawns its own `spawn`-context interpreters that import metadrive independently. **Confirmed by the obvious control**: each test passes alone, the two files pass together at `-n 16` (17 passed), and the immediately following full gate on the same worktree with warm caches is green — **1896 passed, 5 skipped**, log `20260908T215022Z-163d18f`. The assertion text C43 asked to be kept was not captured, because `gate.sh` runs pytest with `-q` and the log carries only the short summary; **capturing it needs `-q` relaxed or `--tb=short` added for a diagnostic run**, which is the concrete next step rather than another reproduction attempt. Still not fixed, and the classification is unchanged: this is a **local gap on a cold worktree**, not a repository defect — a contributor with a warm checkout never sees it, and the gate is green on the second run. **Reproduced again 2026-09-09 in a second fresh worktree, and the mechanism is now better evidenced and larger than `__pycache__`.** Ten tests failed, not four, and the set is exactly the bundled-Waymo family: the two `C43` tests in `test_scenario_catalog_build.py` plus `test_pg_validation.py`, `test_scenario_features.py`, `test_scenario_validation.py`, `test_scenario_waymo.py`, both in `test_scenarionet_smoke.py`, `test_scenario_catalog_build.py::test_build_catalog_cli_reports_rich_progress_and_writes_artifacts` and `test_rulebook_v2_live_integration.py::test_collision_hook_preserves_waymo_dynamics_for_fixed_seed_and_actions` (gate log `20260909T061058Z-10d0980`). All 33 tests in those seven files pass on an immediate re-run at `-n 16`. **The new evidence: it is not only bytecode.** Restricting `find third_party` to the run's own window returns **431** files, of which 173 are `.pyc` — and the remainder includes `third_party/metadrive/metadrive/assets/waymo/*.pkl`, i.e. **the very fixtures those tests read were being written one minute into the gate**. A reader opening a pickle while it is being rewritten fails non-deterministically, which explains both the varying failure set (3 then 10) and the non-reproducibility on a warm tree. **Candidate mechanism, evidenced but not proven**: MetaDrive pulls its own assets when `AssetLoader.should_update_asset()` is true (`third_party/metadrive/metadrive/engine/asset_loader.py:93-103`, comparing `asset_version()` against `VERSION`), so the first engine start in a freshly checked-out submodule can rewrite that directory — concurrently, across several xdist workers, while sibling tests read it. **To confirm, and this is the cheap next step**: in a fresh worktree, record `asset_version()` and the assets' mtimes before and after a single-process engine start, before running any suite. If the pull is the writer, the fix is a warm-up step in the worktree recipe rather than anything in `tests/`. |
| C48 | **Four documentation sites were verified stale by the 2026-09-08 audit and deliberately left unreconciled.** (a) `docs/protocols/csv_evaluation_objectives.md` freezes the literal `evals.csv` header and an `analysis/` file list, while the real writer field names in `src/thesis_rl/runtime/io/csv_recorder.py` include the comfort aggregates, the data-abort and GIF-timing columns, and the analysis code lives in subpackages under `src/thesis_rl/analysis/`. (b) `docs/engineering_workflow.md` §"Quality Tool Adoption" and §"Definition Of Done" predate `make gate`, `make check` and AGENTS.md's requirement that the review come from a session that did not write the code, and its ADR threshold omits the proportionality clause — two policy documents disagreeing on what Completion requires. (c) `docs/implementation/evaluation_protocol_v1.0_exec_plan.md` still says no `make analysis`/`make evaluate` target exists, which its own Milestone 8 then created as `make analyze RUN_PROFILE=<profile>`. (d) Suite-size counts quoted as completion evidence ("1528 tests", "1598/1598") in `docs/project_index.md` and in this file age at every merge and were not re-measured. | 2026-09-08, documentation audit | Left out on purpose, and the reasons differ: (a) and (b) are larger than a value correction — (a) is a column contract that should be regenerated from the writer rather than hand-patched, (b) is repository-wide policy, which AGENTS.md reserves for the user; (c) sits inside a dated ExecPlan narrative, the category the audit treated as legitimately historical; (d) needs the suite executed on the project machine, so it is verification work, not editing. Recorded so the audit's own coverage boundary is visible rather than implied. |

## Verification debt — checks not run

| # | item | notes |
|---|---|---|
| V4 | **The O1–O6 ordering fixtures are asserted at a weight pair production has not used since ADR-081.** `tests/test_rulebook_v51_orderings.py` calls `measurement.v51_reward(...)` without `base` or `severity`, so it inherits `FINAL_PRIORITY_BASE = 2.2` and `FINAL_SEVERITY = 0.0`. Those constants are pinned **deliberately and correctly**, documented at `scripts/measure_expert_rulebook_transition.py:249-255`, so that the `v51_weight_grid` baseline row keeps reproducing the published figures — production moved to `a = 2.5, σ = 0.30` on 2026-09-07. What is missing is a second assertion at the shipped pair. | **Checked 2026-09-09: every ordering still holds at `a = 2.5, σ = 0.30`** (O3 excepted, which is the failure `D15` already records), so this is a verification gap and not a behavioural one. `tests/test_scal_v14.py` does guard the shipped pair, but for the scalarization arithmetic and §5.4 admissibility, not for the orderings. The exposure: a future change to `a` or `σ` would move production without moving the fixtures. Fix is to parametrise the fixtures over both pairs, or to read the shipped pair from `conf/scalarization/default.yaml` as `_shipped_gamma()` in the same file already does for the discount. |
| V1 | **`make smoke` not run** since ADR-058's episode-contract change, and not run after `γ = 1`. The discount change alters value-target scale, which is what an end-to-end smoke test would surface. | **Partly retired 2026-09-01**: `make smoke` passed under `γ = 1` and the six-level reward after `RB51`/`M6` fixed the scalarization config. It could not have passed before — the four-margin default would have raised on the first step. **Closed 2026-09-01**: re-run after `M3`, `M4`, `M5`, `M7` and the §7 diagnostics landed — `make smoke` exit 0 on the finished six-level rulebook under `γ = 1`. **Reopened 2026-09-08**: the discount that closure certifies is no longer the production value. ADR-081 (approved 2026-09-07) set `γ = 0.996`, `a = 2.5` and `σ = 0.30`, and the reason this row existed in the first place — a discount change alters value-target scale — applies to that change exactly as it applied to `γ = 1`. `σ = 0.30` additionally widens the expert return's dynamic range (p1 −61.8 → −99.4 per ADR-081), which is the other thing a smoke run would surface. Follow-up: `make smoke`. |
| V2 | **Transition replay**: source-backed learner smoke and checkpoint/resume remain pending. | `TRANSITION-REPLAY` ExecPlan | **Half closed, half converted into a finding, 2026-09-01.** The *source-backed learner smoke* is **PASS**: `make smoke` runs `td3_sb3` with `transition_replay.enabled=true, prioritized=true, n_steps=3` against frozen ScenarioNet records, and completed exit 0 with both evaluations. The *checkpoint/resume* half was run and **failed for a structural reason worth recording**: `TRANSITION-REPLAY` v1 hard-codes `persistence.trigger = final_or_manual` and forbids `periodic_frequency_steps` (`sb3_extensions/replay/config.py:73-75`), so the replay buffer is paired only with the **`final`** checkpoint. The periodic `latest` checkpoint has no buffer, and resuming from it raises *"Replay continuation requires a checkpoint pair manifest"*. **A crashed run never reaches `final`, so it can never resume with replay continuation** -- which is almost certainly the `sac-0`/`ppo-0` failure recorded in `D4`. **The mechanism itself is sound**: resuming the same run with `checkpoint.resume.checkpoint_name=final`, which does have a paired buffer, completed **exit 0**. So the defect is not the resume path but *which* checkpoint carries a buffer. The code fails closed with a clear message rather than resuming on an empty buffer, so this is a design limitation, not a bug. Note the ACL driver *does* write `latest_checkpoint_pair.json` periodically (`curriculum/scenario_acl/driver.py:1256`), so runs with the curriculum enabled are not affected; the smoke ran with the curriculum disabled. **Decided 2026-09-01 (user): park it.** Mid-run replay continuation is a convenience, not a requirement — a crashed run restarts, and with the ACL enabled the periodic pair is written anyway. Revisit only if crashes prove frequent enough that losing the buffer costs real time. The failure mode is now documented rather than mysterious, which is what `D4` was missing. **Re-examined 2026-09-06 as `C13` / issue #3** and **resolved the same day**: the user approved `DEC-RES-003` (ADR-079), so the replay buffer is now paired with every periodic checkpoint on every profile and a crashed run resumes with `checkpoint.resume.checkpoint_name=periodic`. The checkpoint/resume half of this item is thereby closed; the source-backed learner smoke half was already `PASS`. |

---

## Closed

| # | item | closed | how |
|---|---|---|---|
| C51 | **`telescoping_max_error` is published as an absolute value, so it cannot distinguish an over-payment from an under-payment.** `measurement.v51_telescoping_max_error = max(…, abs(v51_episode_delta_q − v51_episode_delta_s / V51_REFERENCE_ADVANCE_M))` (`scripts/measure_expert_rulebook_transition.py:2637-2639`). The audit reports **62.294** channel units against a mean mission of 40.58, and `AC-RB5.1-07` records `PASS`, "inexact on the expert panel only where the clip binds". | 2026-09-09 (`docs/audits/rulebook_architecture_2026-09-09/`) | **On the expert panel the sign is recoverable by argument and the error is a deficit, i.e. conservative.** A forward jump of `Δs = 3.609 m` gives `Δq_raw = 1.624` clipped to 1.0, so the channel *loses* credit; and the negative clip never binds there, because `result.rulebook_v51.behind_peak` reports `max_m = 0.053` and `steps_beyond_1m = 0` — the largest backward step over 217,189 transitions is 5.3 cm against a clip threshold of 2.2222 m. All 62.294 units are therefore deficit over 1298 clipped steps (0.60 %). **But the statistic as published cannot say that**, and the same number would report `C50`'s surplus identically. Emitting it signed is a one-line change at the site above and it is the detector for `C50`, which is why the two are filed together. An earlier analysis called this figure "the largest number in the system" and treated it as a reward-hacking exposure; that reading is withdrawn — the exposure is `C50`, on the other side of the same clip. | 2026-09-09 | Emitted on both sides instead of as one absolute maximum: `Measurement.observe_telescoping_residual` keeps `telescoping_max_surplus` and `telescoping_max_deficit`, the merge routes each worker's two extremes through the same method, and the report publishes both. A single extremum taken by magnitude would not have been enough — on the expert panel the deficit is 62.294, so any surplus below that would still have been invisible, and the surplus is the side `C50` produces. The absolute figure earlier revisions published is recoverable as `max(surplus, -deficit)`, so runs stay comparable with the 2026-09-07 calibration. Regression `TEST-RSEC-029` in `tests/test_measure_expert_rulebook_transition.py`. **Pre-fix evidence** (technique 2, `docs/workflows/agent_operations.md` — the fix introduces a method, so the test cannot fail before it for an honest reason): the pre-fix accumulation was reconstructed inline and run over three materially different populations — deficits only, the same plus a `+0.75` surplus, and the exact mirror image with surpluses — which report the identical `62.294`. **The per-episode distribution ships with it**, which closes the "only the maximum is recorded, so the distribution is unknown" gap the architecture review's `F14` list carries: `telescoping_residual` publishes p01/p10/p50/p90/p99, the mean, the mean absolute, the total absolute, and `concentration_in_worst_episode` — the worst episode's share of the total absolute residual, which is the one scalar that separates a single outlier from a defect every episode carries. Two extremes could not: a mean deficit of a fraction of a channel unit against a maximum of 62.294 is violently skewed, and `fraction_below_standstill` is a bottom-tail statistic measured exactly where such skew bites. Requested by the A7 ExecPlan session for that reason; it is the same defect as this row one level up, since reducing a population to one extremum hides its shape whether the reduction is `abs()` or `max()`. `TEST-RSEC-030` pins both readings of the concentration scalar and that the merge carries episodes rather than only extremes. Residual risk: none in the runtime, which never reads this statistic; the distribution itself is unmeasured until a panel run produces it. |
| V3 | **The near-revisit re-audit that a standing constraint requires does not exist as a script.** `docs/implementation/route_coordinate_driving_mission_v1.1_exec_plan.md:270-278` makes it permanent: *"Any regenerated or extended frozen index must re-run the near-revisit audit (route portions with `|delta_s| > 15 m` within one lane width) before the runtime is trusted on it; if any route fails the audit, the contiguous cursor must be implemented and `AC-RCM-003` extended with a self-intersecting/U-turn case before that index is used."* Nothing under `scripts/` matches. | **Now load-bearing rather than hygienic**, because `C50` shows what the audit was protecting against and shows it is exploitable when the geometry admits it. Two things the script should do beyond restating the 2026-09-05 run: report at a threshold of **one clip width (2.2222 m)** rather than 15 m, because that is all the ratchet needs and the original threshold leaves tight hairpins and roundabout entries uncovered; and emit the distribution rather than a pass/fail, so the exposure can be read rather than asserted. It is read-only over the frozen index and needs no simulation. **Cheapest thing that decides the priority of `C50`**: a clean result at 2.2 m makes the ratchet a robustness matter for future panels; a dirty one makes it urgent. | 2026-09-09 | `scripts/audit_route_near_revisits.py`, read-only over the committed index, plus five acceptance tests in `tests/test_route_near_revisit_audit.py`. **The instrument is cross-validated in both directions**: it reproduces the 2026-09-05 result exactly — 0 of 3500 routes with two portions more than 15 m apart in arc length within 3.5 m or within 6 m, closest approach 10.581 m — and its positive control is the `g6` hairpin itself, on which it recovers the arc separation that script's return jump charges `-1`. **The naive threshold substitution was rejected as degenerate**: arc length is never shorter than its chord, so "portions more than 2.2222 m apart in arc length within one lane width" fires on every route in the index while describing nothing. The criterion pairs the arc separation with the planar one instead — `a/D_REF - max(1, ceil(d/D_REF))`, the arc-length change the clipped channel cannot express over the steps the traversal takes — and reports the distribution over planar bands rather than a verdict. A second regime is reported beside it, because the on-route figure alone would overstate safety: an ego near the medial axis between two portions switches branch in one step whatever their separation, at the price of a lateral excursion of half of it, and that is the regime `g6` executes. **Result: clean.** See `C50`. |
| C44 | **The semantic observation never contained another road user.** The OBS-V1.2 LiDAR admission gate compared the sweep's MetaDrive object ids (random names under ScenarioNet replay, `force_reuse_object_name` never set) with `ActorSnapshot.actor_id` (the source actor id via `obj_id_to_scenario_id`). Disjoint by construction, so `dynamic`, `interactions` and live-actor `static` were empty in every `obs=semantic_v3` episode (about 2100 of 3011 dimensions dead) and collision/yield costs were unlearnable noise for the policy. Every unit test mocked the sweep with ids matching the snapshots. | 2026-09-07 | `OBS-AUDIT-FIX-001` (`implementation/observation_signal_audit_fixes_v1_exec_plan.md`), `DEC-AF-001`: the sweep resolves ids through the same traffic-manager mapping as the live adapter. Reproduced before the fix in the dev container (sweep `{UUID}`, snapshot `{"77"}`, intersection empty); regression `tests/test_perception_actor_identity.py` and integration `tests/test_perception_first_hit_scenario_ids.py` (real `ScenarioOnlineEnv`, synthetic descriptor) fail on the pre-fix tree and pass after. The dynamic block's station difference was also re-anchored at the committed mission station (`REQ-AF-02`), because re-enabling the block without the anchor would snap to the far branch on self-approaching routes. **Consequence**: no policy trained on `semantic_v3` before this date saw traffic; results before and after are not comparable. |
| C45 | **A flashing red light aborted the episode instead of being priced.** MetaDrive simplifies `LANE_STATE_FLASHING_STOP` to `TRAFFIC_LIGHT_UNKNOWN` (and `FLASHING_CAUTION` to `YELLOW`) before the light object is readable, so the approved mapping's `FLASHING_STOP -> RED` branch was dead code, `evaluate_signal_transition` raised `RuntimeScenarioNotEvaluableError`, the episode was truncated with zero signal cost, and a flashing yellow lost its `NOT_APPLICABLE` treatment. Offline eligibility only rejects raw `UNKNOWN`, so such scenarios entered training. | 2026-09-07 | `OBS-AUDIT-FIX-001`, `DEC-AF-003` / `DEV-AF-001` (user approved): the live provider reads the source-recorded state at the current frame from the light manager's per-episode sequence (same index rule as `after_step`, frozen past the scenario length, current frame only), falling back to the object state when no sequence exists. Regression tests in `tests/test_rulebook_v2_metadrive_live.py`. Reproduced before the fix with a synthetic flashing-red descriptor. |
| C13 | ([issue #3](https://github.com/EmanueleRsp/thesis-metadrive/issues/3)) **An abruptly killed run could not be resumed as a valid continuation**: only SIGINT reached the save handler; `latest.zip`, the training state, the RNG state and the ACL state files were written in place; the periodic `latest` carried no replay buffer and persistence was off on every production profile (ADR-017), so a resume failed closed or continued silently on an empty buffer; Ctrl+C recorded a stale step counter and dropped `beta_progress_env_steps`; best keys were never restored. | 2026-09-06 | `RESUME-ABRUPT-001` (`implementation/resumable_training_after_abrupt_interruption_v1_exec_plan.md`, `IMPLEMENTED`), `TRANSITION-REPLAY` v1.1 amendment, ADR-079 (supersedes ADR-017). Atomic publication of every resume artifact with the training state as commit marker; replay buffer paired with every periodic checkpoint on every profile (`trigger=periodic_and_final`, cadence `periodic_interval_steps` with crossing semantics, `keep_last=1`, deleted after `final`); `checkpoint.resume.checkpoint_name=periodic`; torn-snapshot and seed checks; REQ-025 empty-segment resume gated by `allow_replay_reset` and logged as `replay_reset=true`; SIGTERM handled like Ctrl+C; interrupt handlers no longer write a mid-chunk snapshot (`DEC-RES-007`). Found on the way: the async-evaluation callback wrote `latest` mid-chunk with the evaluated step; the ACL loops wrote no periodic checkpoints; periodic checkpoints were skipped whenever `periodic_interval_steps` was not a multiple of `eval_interval`. Evidence: full suite `1704 passed`; kill -9 smoke resumed twice from `periodic` snapshots, ran to the full budget and completed (`status: completed`, final panels recorded), with a strictly monotone `checkpoint_index.csv`, self-consistent counters and the post-`final` replay cleanup observed. |
| D2 | **L6 rewards speed, and PG admits no speed limit.** | 2026-08-20 | Decided: **accept**. PG blocks are synthetic, so there is no traffic law to encode and MetaDrive's design speeds are simulator parameters, not norms; inventing a PG limit would be the unmotivated calibrated constant this project rejects elsewhere. `offroad` at L3 constrains reactively, and it is a difference between *sources*, not between *arms*, so it does not confound the comparison. Recorded as `RULEBOOK-V5.1` limitation 13, with a new §7 diagnostic `mean_ego_speed_by_source` so the divergence is measured rather than assumed away. |
| D3 | **Whether to raise MetaDrive's `max_speed_km_h = 80`.** | 2026-08-20 | Decided: **do not raise**; declare the ceiling. The reason is quantitative: the episode ceiling is `a · distance / longest single step`, and the cap *is* the longest step, so it sits in the denominator of what a mission can be worth. A 110 km/h cap — the least that covers all nine records — costs **−27 %** of every mission's worth while every per-step penalty stays unchanged, and `λ₄` cannot compensate because `λ₄ + 0.1·(λ₅+λ₆) < a`. Completing those records *would* be legal (posted limits 65–70 mph), so the obstruction is the vehicle, not the rulebook. Recorded as `RULEBOOK-V5.1` limitation 14 with the full table. |

| B1 | **ADR-071 not implemented** — R1 at-fault classification and truncation. | 2026-09-01 | Implemented as `RB51` `M7`. `components/collision_fault.py` carries nuPlan's taxonomy; `evaluate_collision_impact` charges only at-fault contacts; `done_function` truncates a not-at-fault-only step through the existing `MAX_STEP` channel, calling the same classifier so cost and episode contract cannot drift. Undeterminable lane containment resolves to at-fault. |
| B3 | **`speed_limit` requires amending `OBS-V1.3` and `OBS-LIDAR-V2.0`.** | 2026-09-01 | Both amendments written and applied: `OBS-V1.3.1` (`D` 3009 -> 3011) and `OBS-LIDAR-V2.0.2` (frame 308 -> 310, stacked 6489 -> 6531). Checkpoint compatibility intentionally broken, as `DEC-RB51-001` approved. The observation resolves the limit through the reward's own `associated_speed_limit_mps`, so the "unavailable" encoding fires under exactly the condition that makes the sub-rule inapplicable. |

| F6 | **PG coverage measurement** for the rulebook. | 2026-09-01 | Run as `RB51` `M8a`. It was one call site, as limitation 5 said. Result: 1089/1100 PG records replay, and **five of the six L3 sub-rules never apply** — `signal`, `stop`, `crosswalk` and `speed_limit` are inapplicable on every PG record, as is `clearance`, so L3 on PG is `offroad` alone and PG carries no traffic controls at all. The two sources are graded by substantially different rulebooks, not merely under different speed regimes as limitation 13 records. Bears on `D5`. |

| B2 | **`RULEBOOK-V5.1` not implemented in `src/thesis_rl/`.** | 2026-09-01 | `RB51` `M0`-`M8` complete. Six levels, `SCAL-V1.4` selected by config, the inherited sub-rule redefinitions, `speed_limit`, the at-fault gate, R1 at-fault classification with truncation, and the §7 diagnostics. 1528 tests, lint and format clean, `make smoke` exit 0; production reproduces the measurement instrument with zero divergence and the 1100-record panel reproduces §5.5 to the decimal. |
| D5 | **Arm/source confounding in the frozen train split.** | 2026-09-01 | Decided: **accept, it is the design.** PG is deliberately simpler and covers a gap logged scenarios do not, per ScenarioNet's own rationale, and the ACL exists precisely to move from simple to complex — so arms ordered by source is the curriculum, not a confound. `M8a`'s finding that five of six L3 sub-rules never apply on PG is a *sharper statement of the same intent*: "fewer active rules" is a concrete, measurable notion of "simpler". **One reporting rule survives**: never publish a single score pooled across sources, because the expert's +142.06 on PG against +70.70 on Waymo is largely rule count, not driving quality. Report per source. |

| C1 | **RSS standstill-exit transient** identified but not fixed. | 2026-09-01 | **Retired by ADR-063, not fixed.** `rss` longitudinal now carries `contributes_to_channel=False` (`rulebook/v2/registry.py`), so it is evaluated and published but never reaches a channel. The transient therefore cannot affect any cost, reward or termination — it is a defect in a diagnostic. Verified by inspection of the registry entry. |
| C4 | **Mission artifact regeneration pending** a provisioned `pyarrow`. | 2026-09-01 | **Verified obsolete.** The `pyarrow` gap ended 2026-08-06 (pyarrow 25.0.0 in the `dev` image) and `scenario_catalog_driving_mission_v1_1_1.parquet` is produced reproducibly by `scenarionet-v1-2-rebuild` (`Makefile:354`). The one artifact the mission ExecPlan still lists as pending, `scenario_selection_mission_v1_1_1.json`, is **referenced by no code path** — `grep` over `src/`, `conf/`, `Makefile` and `scripts/` returns only the *schema string* `scenarionet_frozen_selection_mission_v1_1_1`, which `frozen.py:461` accepts as an alternative to the plain schema. The runtime reads `scenario_selection_index.json`, which exists and works. |
| D6 | **Should `EVAL-PROTOCOL v1.3` record the feasibility ceiling?** | 2026-09-01 | Decided: **yes, record it; change nothing else.** Written as `docs/specifications/evaluation_protocol_v1.3.1_amendment.md` (`EVAL-PROTOCOL-V1.3.1`): three requirements — report the ceiling alongside every Waymo success figure, re-measure it if the panel or the vehicle changes, and exclude no record. §3 records *why* no remedy was adopted: raising the cap to 110 km/h would cover all nine records but costs **−27 %** of every mission's worth, because §4.1 puts the cap in the denominator of the episode ceiling, and `λ₄` cannot compensate under §5.4's bound. Nine unreachable records out of 1805 against a 27 % devaluation of all 1805 — the first is a declared ceiling, the second reopens the degeneracy the rulebook exists to close. |
| D7 | **The git-tracked frozen index is not the index in use.** | 2026-09-01 | **Was never true; raised in error from a stale ExecPlan note and withdrawn the same day.** The mission ExecPlan's 2026-08-04 entry said the tracked copy was still the pre-mission snapshot, but the sync happened on 2026-08-08 (commit `aa30e40`) and the note was never updated. Verified by hash: `data/scenarionet/frozen/scenario_selection_index.json` and the host copy at `/scratch/.../frozen/` are both `sha256 2829a1e26f50d65f4bfda1cb2945691ccad7fee8c81b515631d01898bf499b39`, same 196 461 242 bytes, same `created_at` 2026-08-04T21:23:53Z and same `selection_hash`. Reproducibility is intact. |

| F1 | **M5: solid-line penetration deadband (audit `P5c`) and signal persistence (audit `P7.3`).** | 2026-09-01 | **Both halves superseded, neither needs its own work.** `P5c` asked for a minimal-penetration deadband on `solid_line`; ADR-065 went further and replaced the binary cost outright with **graded lateral penetration, zero below tolerance 0.3**, detected against the marking's real half width 0.075 m instead of a 1 cm epsilon — a deadband is a special case of what was implemented, and it was calibrated by a reported sweep rather than chosen. `P7.3` asked about the red-light persistence latch; ADR-064 **removed** the traffic-control persistence latches. Both landed in `RB51` `M3` on 2026-09-01. |

| C5 | **Every `obs=semantic_v3` run was unstartable for a month.** `envs/factory.py:128-133` injected `semantic_v3_signal_range_m`, `semantic_v3_signal_fov_degrees` and `semantic_v3_signal_camera_height_m` into the environment configuration; `ThesisScenarioEnv.default_config()` declared none of them; MetaDrive's `BaseEnv.__init__` merges with `allow_add_new_key=False` and raises `KeyError` before the first reset. | 2026-09-02 | **Found and fixed the same day.** Introduced 2026-08-01 (`52b8ad4`) and invisible for a month, because the repository's only end-to-end check, `make smoke`, selects `obs=lidar_state` and `encoder=none` -- a different code path from `make run-train`, which passes `obs=semantic_v3`. Surfaced by the `AB-LEARN` screening smoke, which existed precisely to exercise that unverified path. Fixed by declaring the three keys at the `OBS-V1.2` SS6.2 baseline `80.0/65.0/1.2`, identical to the defaults the readers already applied, so no behavior changed: the environment either failed to construct or now constructs with the values the code expected. Regression test parametrized over `conf/obs/*.yaml` and asserting MetaDrive's own construction merge rather than a key-set comparison, because the rejection recurses into nested dictionaries and the passthrough also writes into `vehicle_config.lidar`. Verified to fail on the pre-fix code. The **coverage gap that hid it is not closed by the fix**: see `SMOKE-COV` `M2`, deferred by `DEC-COV-001`. |

| C6 | **The evaluation path cannot start under `obs=semantic_v3`.** `_ego_local_projection` (`envs/observations/causal_semantic.py:79`) requires the re-projected ego station to equal the mission tracker's committed station within `1e-6 m`; at episode reset it does not, and the first asynchronous evaluation dies in a vector worker (`reset_slots`). | 2026-09-02, `AB-LEARN` smoke | **Measured, not guessed** (diagnostic added to the error message the same day): projected `s = 0.022900 m` against committed `s = 0.0 m`, divergence **0.0229 m** on a 23.17 m route -- four orders of magnitude above the tolerance and three below anything semantically meaningful. **Cause identified**: `mission/runtime.py:281` constructs `RouteCoordinateMissionTracker(mission, route, 0.0)` with a hard-coded initial station, so at reset the guard compares a zero *by definition* against a geometric projection of the actual spawn pose. They agree only if the vehicle spawns exactly on the route origin. The magnitude is consistent with the projection jitter ADR-073 already measured (`sigma - s` non-zero on 20.99 % of expert steps, never above 0.053 m). **Not floating-point noise and not a stale snapshot** -- both were excluded by the magnitude. Blocks the `AB-LEARN` screening and therefore the freeze of rulebook and scalarization. Never caught before because the whole `obs=semantic_v3` path was unstartable since 2026-08-01 (`C5`), and **no test covers this invariant**. Resolution is an open decision: seed the tracker from the projected spawn pose, widen the tolerance to a measured bound, or exempt the reset observation, each with different consequences for `route_completion` at `t=0` and for L4's first increment. **Closed 2026-09-02**: resolved as the user chose -- the consistency check is enforced from the first tracked step (`step_index > 0`), the `1e-6 m` tolerance is unchanged, and a two-sided regression test in `tests/test_causal_semantic_batch.py` pins both the reset exemption and the fact that the same divergence still raises once the tracker has advanced. Both smoke arms then completed `exit 0`. |

| D8 | **Should TD3 receive ADR-078's learner setting (batch 512, `update_to_data_ratio` 0.5)?** Opened when ADR-078 changed SAC only, because the 179 ms/step measurement was taken on SAC. | 2026-09-06 | **Closed the same day: yes, same setting, same ADR.** The TD3 backend has the same update structure (`auto -> n_envs` gradient steps of batch 256, four `lq_v3`-backed networks, update overlapped with the workers), and an asymmetric update-to-data ratio would be a second varied factor in the `EVAL-PROTOCOL` algorithm comparison. `td3_sb3.yaml` and the six non-smoke profiles now carry 512 x 0.5; `policy_delay=2` unchanged. The `REQ-013` documentation debt noted here is recorded in ADR-078 §Consequences. |

| W1 | **"`PER.add()` runs a Python loop"** — the audit's mechanical list item d, first half. | 2026-09-07 | **Withdrawn on measurement: the vectorised version already exists and is four times slower.** `add()` does loop over `n_envs`, each iteration walking the depth-23 tree in `_SumTree.set`, and that costs **64.8 µs per vector step** at production geometry (20 leaves x 23 levels). The alternative is `set_batch`, which `update_priorities` already uses: it costs **256 µs** on the same input, because it runs `np.unique` plus `np.add.at` at each of 23 levels on 20-element arrays. Both produce bit-identical trees. The loop is the faster of the two at this width, so the "fix" is a 4x regression. Do not touch it. |
| W2 | **"`required_observation_schema` is declared in the encoder YAML and read by exactly one test, nothing else"** — the audit's mechanical list item i. | 2026-09-07 | **Withdrawn as framed: the pairing it would guard is already enforced, and more strongly.** `agent/planners/encoders/factory.py` refuses the encoder outright unless the observation schema *is* `SemanticObservationSchemaV12` and `input_dim` equals its `flat_dim` — at three sites, one per latent-query variant, plus a fourth check inside `lq_encoder.py`. So the key is neither a dead knob (as `C24`'s six were) nor an unimplemented guard: it is descriptive, and `tests/test_audit_block_b_encoder_frozen_settings.py` classifies it as such deliberately. **Residual, not a defect**: the YAML string is a third copy of the schema version, which a bump already has to update by hand in four files. Hardening it (refusing a declared value that differs from the selected schema's `.version`) is about four lines, but it buys nothing today and was left out rather than added as unrequested scope. |

| D12 | **"PPO's 96x63 geometry is incompatible with 20 envs and wants a fail-fast"** — the audit's mechanical list item j, opened as a decision on 2026-09-07 and **withdrawn the same day**, before any code was written. | 2026-09-07 | **Withdrawn: `63` is correct, `64` is the value that would break it, and the fail-fast already exists.** The constant is not a typo. Commit `57269b8` (2026-07-23) changed `n_steps` 2048 → 96 and `batch_size` 64 → 63 *together with* `docs/implementation/ppo_vectorized_21_env_exec_plan.md`, whose `REQ-001` states the geometry outright — "21 env × 96 steps = 2016, batch 63, 32 minibatches, 320 optimizer steps/update", sourced to **User request** — and §52 restates "2016/63 = 32 minibatches". The arithmetic settles it: `2016 / 63 = 32` exactly, while `2016 / 64 = 31.5`, so changing 63 to 64 would *introduce* the non-divisibility the audit thought it was fixing and destroy the 32-minibatch figure `D9` quotes as the intended geometry. The second half was equally wrong: `_validate_rollout_geometry` (`agent/planners/algorithms/ppo_sb3.py:128`) already raises `"PPO global rollout size must be divisible by batch_size"` at construction, and `validate_rollout_geometry` defaults to `True` in both `runtime/wiring/builders.py:314` and `ppo_sb3.py:44`, passed `False` only on evaluation, video-render, async-evaluation and ACL checkpoint-load paths where no update runs. So the audit's "SB3 warns rather than crashing" never applies to the training path — this repository's own validator refuses the 20-env pairing before SB3's warning is reachable. **The one real residual is documentation**: no shipped preset sets PPO to 21 environments, so nothing points a reader at the geometry these constants serve, and a 20-env launch is refused rather than guided. **Method note**: this is the third claim on that mechanical list to invert on checking, after item (b)'s direction and item (d)'s 4x regression — the handoff's warning that the list is "an audit's output, not verified fact" earned its place. |
| D13 | **"The full-array rescan of the priority maximum is a hot-path cost worth a decision"** — opened 2026-09-07 while fixing `C32`, **withdrawn the same day** on provenance and measurement. | 2026-09-07 | **Withdrawn three times over: the rescan is specified, it is the approved design's own fallback, and my figure for it was 25x too large.** (1) *Specified*: `REQ-013` requires the **current** maximum, so the monotone "maximum seen so far" — option (c), the textbook alternative — is not a test-weakening question at all but a specification violation, and would need an amendment. (2) *Approved as the design*: `docs/implementation/transition_replay_v1.0_exec_plan.md` records that `add()` originally ran `np.any` and `np.max` over the whole allocation on **every** vector insertion, that the user "explicitly approved a semantics-preserving optimization", and that the approved contract "does not require a full-array scan on every insertion … rescan only if replacement removes the last leaf at the maximum". The incremental counter *is* that optimization, and the rescan is the fallback it was approved with — so `C32`, which made a masked eviction go through the same bookkeeping, restored the approved design rather than deviating from it. (3) *Mis-measured*: the 4.1 ms figure assumed a 6M-element array, but SB3 divides the configured capacity by `n_envs`, so `buffer_size: 300000` over 20 environments allocates `15 000 x 20 = 300 000` elements. Re-measured minimum-of-300: **80.6 µs** at the production allocation (2011 µs at 6M, confirming the earlier number was measuring the wrong size). Worst case, firing on all ten `update_priorities` of a vector step, is **0.81 ms of a ~179 ms step — 0.45 %**, under the step-timing noise floor. Option (a), accept it, is therefore the answer, and (b)'s max-tree would be unjustified complexity. The investigation also surfaced `C37`, a real divergence from the same `REQ-013`. |
| W1 | **"`PER.add()` runs a Python loop"** — the audit's mechanical list item d, first half. | 2026-09-07 | **Withdrawn on measurement: the vectorised version already exists and is four times slower.** See the entry below; kept adjacent to `D12` because both were withdrawn the same day for the same reason. |

*(move further items here with the date and the change that closed them)*
