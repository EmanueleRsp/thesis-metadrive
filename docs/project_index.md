# Project Document Authority Index

## Purpose And Current Status

This index prevents an apparently newer, exploratory, or implementation-tracking
document from being mistaken for an approved scientific contract.

- Last repository inspection: 2026-08-20
- Index status: `CURRENT_WITH_DOCUMENTED_GAPS`
- **Open work is registered in [`open_items.md`](open_items.md)**, not in this
  file's Notes cells. That register was created on 2026-08-20 because items had
  been accumulating in those cells for weeks without surfacing; when an item is
  closed, close it there.
- Approval evidence: explicit user confirmations recorded on 2026-07-16,
  2026-07-17, 2026-07-21, 2026-07-24, and 2026-08-09
- Rule: repository evidence establishes paths, versions, links, and reported
  implementation status; explicit user approval establishes scientific authority.

## Status Vocabulary

- `AUTHORITATIVE`: explicitly approved by the user and selected for use.
- `CANDIDATE`: relevant document exists but authority is not established.
- `MISSING`: no matching document was found.
- `IMPLEMENTED`: implementation is reported complete, without implying full
  reconciliation.
- `VERIFIED`: mandatory validation and specification reconciliation are recorded.
- `SUPERSEDED`: an approved authority record explicitly identifies a replacement.

## Scientific And Functional Documents

| Area | Exact authoritative or candidate document | Authority | Implementation record | Remaining gap |
|---|---|---|---|---|
| Rulebook v2 | `specifications/rulebook_v4.7_specification.md`; version `4.7-final-implementation-complete` | `AUTHORITATIVE`; explicit user approval 2026-07-17 | `implementation/rulebook_v2_implementation_plan.md`; F11 `COMPLETATA`; causal CTRV wiring, conformance suite and live Waymo smoke implemented | Final document/diff reconciliation required before `VERIFIED` |
| Rulebook v2, R2 clearance amendment | `specifications/rulebook_v4.8_specification.md`; version `4.8`; amends only v4.7 §6.4 (vehicle/static clearance → scoped lateral-RSS / diagnostic-only) | `AUTHORITATIVE` for the amended §6.4 subset; explicit user approval 2026-07-24, recorded by ADR-025 | `implementation/r2_lateral_rss_clearance_v4.8_exec_plan.md`; `VERIFIED`; `rss_lateral` component, VRU-only clearance, aggregation/registry wiring, and REQ-R2-06 regression test implemented and tested (306-test rulebook suite and `make rulebook-v2-check` pass) | Known limitation: no R2 sub-metric other than TTC catches a static obstacle whose geometry converges beyond the TTC horizon (AC-R2-06, accepted by design) |
| Rulebook v2, R1 injury-risk amendment | `specifications/rulebook_v4.9_specification.md`; version `4.9`; amends only v4.7 §5.4/§5.6/§5.8 (R1 collision cost: configured-speed-cap normalization → MAIS3+F injury-risk curve) | `AUTHORITATIVE` for the amended R1-cost subset; explicit user approval 2026-07-26, recorded by ADR-027 | `implementation/r1_injury_risk_collision_cost_v4.9_exec_plan.md`; `IMPLEMENTED`; `injury_risk.py` component, `collision.py` wiring, updated diagnostics, and 12-test suite incl. the v4.7 severity-inversion regression; full suite (1008 tests) and scoped ruff pass | Known limitations (spec §9): normal component instead of closing-speed magnitude, `STATIC_COLLIDABLE` mapped to the car-driver curve by analogy (both under-estimate), fixed age 65, German 1999-2020 data, no `MOTORCYCLIST` class. `make smoke` not run in the implementing session |
| Rulebook v2, R2/R3 cost-activation corrections | `specifications/rulebook_v4.10_specification.md`; version `4.10`; amends v4.7 §6.2.1, §6.2.4, §7.2.1, and adds new §7.3-bis | `AUTHORITATIVE` for the amended subset; explicit user approval 2026-08-01, recorded by ADR-046..ADR-049 | `implementation/rulebook_v2_cost_activation_corrections_v1_exec_plan.md`; M1-M4 `IMPLEMENTED` (drivable-surface seam closing, ego-braking physical bound with regenerated calibration artifact, RSS standstill applicability, new `wrong_carriageway` sub-rule) plus M3 marking-coverage adapter fix (PG/Waymo, incl. DEC-RBCOST-006 eligibility-safe degenerate-geometry diagnostic, verified with a read-only 1805-record Waymo dry-run: zero new validation errors); M0 partial (GIF status overlay, plus post-approval follow-up: `wrongway` status deadband `ADR-050`, and control-drop diagnostic counters `dropped_control_line_off_route_count`/`control_line_diagnostics`); full rulebook suite (316 tests), full repository suite (except 9 pre-existing unrelated failures), and `make smoke` (end-to-end `td3_sb3` training) all pass | M0's control-drop counters not yet aggregated into any cross-episode report table; a 828-record read-only dry-run over the frozen catalog's `has_route_traffic_light=true` Waymo scenarios initially found 57.1% (473/828) never have a `SIGNAL` control selected at runtime, root-caused to the route-membership predicate (see `ADR-051` row below) and largely fixed (down to 209/828, 25.2%, after the fix); M5 (deferred solid-line penetration deadband) not yet implemented, gated on its own measurement; RSS standstill-exit transient identified but not fixed, pending frequency/duration measurement; scalarizer binarisation (F0) deferred to a separate ExecPlan |
| Rulebook v2, traffic-control route-successor extension | `specifications/rulebook_v4.11_specification.md`; version `4.11`; amends v4.7 §2.9.5's operational predicate for "movimento ego pertinente" | `AUTHORITATIVE` for the amended predicate; explicit user approval 2026-08-02, recorded by ADR-051 | `implementation/rulebook_v2_cost_activation_corrections_v1_exec_plan.md`; `IMPLEMENTED`; `route_reachable_control_lane_ids` extends control selection to the route's unambiguous single successor lane, mirroring `derive_lane_movement_key`'s existing exit-lane disambiguation; 3 new regression tests; verified on the frozen catalog (working-signal rate 42.9% -> 74.8%, 828 Waymo signalised records) | Single-hop only (a route ending 2+ lanes short of a light is not covered); the remaining 209/828 (25.2%) unselectable scenarios are unaffected (52 lost at adapter construction, 157 genuinely unrelated or beyond one hop) |
| Rulebook v2, wrong-way cost physics-noise deadband | `specifications/rulebook_v4.12_specification.md`; version `4.12`; amends v4.7 §7.3.2's `wrongway` cost formula; supersedes `ADR-050`'s "cost is out of scope" scope conclusion | `SUPERSEDED` by `rulebook_v4.13_specification.md` on 2026-08-09 (ADR-060 removes the deadband's subject entirely); historical authority from explicit user approval 2026-08-06, recorded by ADR-056 | `implementation/rulebook_v2_cost_activation_corrections_v1_exec_plan.md`; `IMPLEMENTED`; `cost` now shares the `0.1 m/s` standstill noise floor already used by `status` (`ADR-050`) and RSS applicability (`ADR-048`), via a continuous reparameterization; `status` simplified to derive from `cost > 0.0`; found while auditing tolerance ownership for the in-review `SCAL-V1.1` scalarization amendment; 4 new + 1 updated regression test, scoped ruff/format clean, full `test_rulebook_v2_road.py` (23 tests) green | Episode returns from any run before this version are not directly comparable for scenarios with stops (most scenarios); other candidate subrule tolerances flagged by the same audit (TTC, stop dwell, crosswalk/yield temporal gap) remain unverified and out of scope for this version |
| Rulebook v2, unified direction sub-rule and control-reference polyline | `specifications/rulebook_v4.13_specification.md`; version `4.13`; amends v4.7 §7.3 and §2.9.5/§2.9.6, absorbs v4.12 in full and v4.10 §7.3-bis | `AUTHORITATIVE` for §3-§4 of that document; explicit user approval 2026-08-09, recorded by ADR-060 and ADR-061 | `implementation/reward_scale_and_episode_contract_v1_exec_plan.md`; `IN_PROGRESS`; M2/M3 not started | Replaces `wrongway`+`wrong_carriageway` with one memoryless `wrong_direction` (`2`/`6 m/s` anchors, nuPlan DDC reinterpreted for Markov observability, declared in §3.5) and adds a prefix-identical bounded control-reference polyline for `route_s_m`. Supersedes `ADR-050` and `ADR-056`. `D` is fixed from measurement, not a priori; residual signal coverage must be re-measured on the 828-record dry-run |
| Rulebook v5.0, falsified redesign and rank-preserving scalarization | `specifications/rulebook_v5.0_UNDER_REVIEW_specification.md`; ID `RULEBOOK-V5.0`, version `5.0`; on approval supersedes v4.7 §6.4/§7.1-§7.5, v4.13 for the `wrongway` subject, and `SCAL-V1.1` §7.6 | **`SUPERSEDED_BY_DRAFT`** by `rulebook_v5.1_specification.md`; never approved, never an implementation contract. Retained as the evidence of record for the per-sub-rule Test A / Test B tables, which v5.1 inherits unchanged | `implementation/reward_scale_and_episode_contract_v1_exec_plan.md`; restructure pending | Falsifies every sub-rule against 1100 logged Waymo `train` records (217,189 transitions, 0 skipped). Production charges the human expert a mean episode return of **-203.35** with **46.55 %** of expert episodes below standstill; the specified rulebook measures **+31.14** and **7.45 %** (ADR-070 at-fault gate applied; **+29.09** / **8.82 %** without it). `rss` longitudinal leaves the reward for a reported diagnostic (18.29 % of applicable expert steps, not controlled-invariant); `wrongway` deleted (1 step in 217,189); traffic-control latches removed (86.9 % of their cost, unobservable); `offroad`/`solid_line`/`wrong_carriageway` redefined geometrically (the last to **0** expert violations); `clearance` scoped to VRU on the roadway; `ttc` to nuPlan's 0.95 s; `dashed_line` kept (already observable — the 21-frame window was derived from `DASHED_TCAP_S`); **`speed_limit` added** (0.000 % expert violations at nuPlan's 2.23 m/s), which requires amending `OBS-V1.3` and `OBS-LIDAR-V2.0` and intentionally breaks checkpoint compatibility. Scalarization `SCAL-V1.2`: `a=2.2, sigma=0, phi=0.25, lambda=1`, rank-preservation condition verified against exhaustive counterexample search. Known limitations: R1 `NOT_MEASURED` offline (the standstill comparison is therefore conservative), no longitudinal safe-distance rule survives even in its controlled-invariant form, calibration is Waymo-only |
| Rulebook v5.1, liveness-aware six-level hierarchy | `specifications/rulebook_v5.1_specification.md`; ID `RULEBOOK-V5.1`, version `5.1`; supersedes v5.0 in full, plus v4.7's macro grouping, `max` aggregation and R4 definition | **`AUTHORITATIVE`** for SS3 (hierarchy and aggregation), SS4 (L4, L6 and the discount), SS5 (`SCAL-V1.4` and its weights), SS9 and SS10; explicit user approval 2026-08-14, **amended 2026-08-20** (ADR-075 `gamma = 1`, ADR-076 `L6 progress_rate`), **amended 2026-09-07** (ADR-081: `a = 2.5`, `sigma = 0.30`, `gamma = 0.996`; SS4.4, SS5.4, SS5.5 callouts). `REQ-RB5.1-OBS-01` withdrawn 2026-08-14, `REQ-RB5.1-GAMMA` resolved 2026-08-20. **`REQ-RB5.1-O3-DISCOUNT` opened 2026-09-08**: `gamma = 0.996` breaks the exact L4 tie, so ordering O3 fails (scalar margin **-3.5789** against **+0.2000** undiscounted) and the ordered arms resolve at **L4** instead of L5; SS4.4, SS11.12, open item `D15`. **`AC-RB5.1-05` downgraded to `NOT ESTABLISHED`** the same day: the engine-force cap bounds the ego's travel, not its projection | `implementation/rulebook_v5.1_six_level_hierarchy_exec_plan.md` (`RB51`); **`M0`-`M8` implemented and verified 2026-09-01**: six levels, `SCAL-V1.4` selected by `conf/scalarization/default.yaml`, the inherited sub-rule redefinitions (ADR-064/065/067), `speed_limit` with `OBS-V1.3.1` and `OBS-LIDAR-V2.0.2`, the at-fault gate (ADR-070) and R1 at-fault classification with truncation (ADR-071), plus the SS7 diagnostics. 1528 tests, `ruff check` clean, `make smoke` exit 0. Production reproduces the measurement instrument with **zero** divergence on every redefined sub-rule, and the full 1100-record Waymo panel reproduces SS5.5 to the decimal (mean **70.70**, p1 -61.81, p5 +5.29, p50 +51.85, below standstill **3.36 %**) | Splits v5.0's R3: rules a competent driver may relax to complete a mission (`solid_line`, `wrong_carriageway`, `dashed_line`) move **below** progress as L5, leaving only non-relaxable rules above it. Buys exactly one ordering, **O2** ("completing with a brief violation beats standing still for ever"), which is the motivating pathology; v5.0 already passed O3/O4/O5/O6 and both fail O1, which no hierarchy can deliver and a thresholded comparison can. L4 becomes the **signed route advance normalized by a fixed `D_REF = v_ref · Delta t`**, after normalizing by each record's own route length was measured and rejected (the panel's shortest route, 19.96 m against a 171.98 m mean, dictated the weight ceiling for all 1100 records). A sixth level **`L6 progress_rate`** (ADR-076) sits **below** L5 and carries the time preference an undiscounted L4 removes, placed there so an illegal shortcut loses at L5 before it is reached — **which holds undiscounted only; at the shipped `gamma = 0.996` the comparison resolves at L4 and L5 is never consulted** (SS11.12, `D15`). `SCAL-V1.4` keeps `SCAL-V1.2`'s per-step dominance over L1-L3 and gives L4/L5/L6 a finite exchange, because no finite weight can dominate a continuous increment as it tends to zero. Measured on 1100 Waymo `train` records, 217,189 transitions, 0 skipped, 0 errors: expert mean **+70.70** and **3.36 %** below standstill at `lambda4=2.0, eta=1, lambda6=0.2`, against v5.0's +31.14 and 7.45 %, dominating on mean, p1, p5, p50 and below-standstill. Two consistency checks: at `lambda6=0` the panel reproduces **+73.84**, bit-identical to the pre-L6 measurement, so L1-L5 are untouched; and below-standstill is identical across every `lambda6` because the baseline moves with the channel. The Test A figures are a lower bound because the clip only ever truncates a positive advance downwards. *Corrected 2026-09-08*: the earlier reason given here — that the clip "never binds on an agent trajectory" because MetaDrive caps every vehicle at 80 km/h = `v_ref` — does not follow, since `s` is a projection and ADR-035 sizes the gap at a factor of 2 (`ROUTE_CONTINUITY_JUMP_FACTOR`, cutting the inside of a curve); the lower-bound conclusion is unaffected. | Admissibility (Test A) and the O1-O6 orderings are established by two separate instruments, because expert replay holds one trajectory per scenario and therefore no counterfactual: the orderings are covered by constructed fixtures in `tests/test_rulebook_v51_orderings.py`, all passing. `eta` is the one weight not pinned by measurement (the expert almost never relaxes, so the panel cannot discriminate). Calibration remains Waymo-only. **Measured 2026-09-01** (`M8a`): on the PG half of the training mixture **five of the six L3 sub-rules never apply** -- `signal`, `stop`, `crosswalk`, `speed_limit` and `clearance` are inapplicable on every PG record, so L3 there is `offroad` alone and PG carries no traffic controls at all. The two sources are graded by substantially different rulebooks, which is stronger than limitation 13's speed-regime statement and bears on the arm/source confounding of open item `D5` |
| Rulebook v5.2, five-channel hierarchy with progress last (`A7`) | `specifications/rulebook_v5.2_UNDER_REVIEW_specification.md`; ID `RULEBOOK-V5.2`, version `5.2`; **on approval** supersedes `rulebook_v5.1_specification.md` SS3, SS4, SS5, SS9 and SS10, and `SCAL-V1.4` in full, replacing it with `SCAL-V1.5`. Keeps the `_UNDER_REVIEW` filename until approved, the convention `rulebook_v5.0_UNDER_REVIEW_specification.md` set | **`CANDIDATE`** — status `UNDER_REVIEW`, `Authoritative: NO`, **not an implementation contract** (`AGENTS.md`, Sources Of Truth): `RULEBOOK-V5.1` SS3/SS4/SS5/SS9/SS10 stay authoritative until this document is approved. What is *already* approved and recorded rather than re-decided: the **five-channel architecture** and **`gamma = 0.9982`** (explicit user approval 2026-09-09) and every remaining gate of the `A7` ExecPlan SS6 (2026-09-10). New ADR: `ADR-083`, also `Proposed` | `implementation/rulebook_a7_five_channel_hierarchy_exec_plan.md` (`A7`); **`M1` measurement done 2026-09-10**, **`M2` document written 2026-09-11, independently audited and corrected 2026-09-12, awaiting approval**; `M3`-`M9` blocked on that approval. **No production file is touched by `M2`**. The audit's one blocking finding is resolved (`DEV-A7-010`, user-approved 2026-09-12): the SS5.4 rank-preservation predicate counted the progress swing **once** where `rulebook_v5.0` SS6.3 counts it twice, so it asserted an "iff" that a two-state counterexample refutes — a fully violated `K3` with `dq=+1` scores **-1.2500** against **-2.0000** for a clean trajectory at `dq=-1`. Corrected to `lambda4*(DQ_MAX - DQ_MIN)` with `DQ_MIN` declared at two values, and SS5.4 restated as a **conditional guarantee with a runtime falsifier** (`AC-A7-17`): the ordering holds while the compliant trajectory loses no more than **0.339 m of station per 0.1 s step**, which is 6.4x outside the expert's measured 0.053 m. **No weight moved.** The defect predates A7 — it is v5.1 SS5.4's and ADR-081 calibrated against it, and under the corrected form the six-level weight set in production today fails at all three levels | Five channels `K1..K5` with mission progress **last and unthresholded**, the `L6 progress_rate` level deleted, and negotiable lane compliance charged by a bounded satisfaction indicator **above** progress at `w5 = 0.15`: six free weights `(a, sigma, phi, lambda4, eta, lambda6)` become four `(a, sigma, lambda4, w5)`, and one per-step normalization disappears. Buys O3 on the scalar arm at **both** discounts (+0.581 at 0.996, +2.458 at 0.9982) where v5.1 fails it at the shipped one (-3.5789, `D15`); **loses O2 under strict lexicographic comparison** at `K4`, reported and not repaired, with the measured qualification that v5.1's pass there is a zero-traffic artefact. Takes the discount to its own criterion at the **measured** 500-step horizon (break-even 508.6 against 228.6), closing `C49`, at the declared cost of doubling the effective horizon to 556 steps. Budgets `tau1..tau4` fixed by the logged expert's per-episode maximum on the frozen Waymo `train` panel (1100 records, 217,189 transitions, 0 skipped, **0 exclusions declared**): `0.000000 / 110.379799 / 21.287443 / 23.386514`, evidence in `audits/a7_m1_measurement_2026-09-10/`. Measured under the A7 reward on that panel: expert mean **71.34** and below-standstill **4.64 %** against the 7.45 % ceiling. **Two things are declared open rather than resolved, deliberately.** **`D1`**: which of three candidate *objects* a threshold is enforced on (undiscounted realized, per-span, discounted expected) — all three are measured and recorded so the choice costs no second run, and the document declares the requirement on the undiscounted realized form without choosing a mechanism. **`K2 > K3`** is the hierarchy's least-supported adjacency: its only surviving argument is a specification of this repository, which `AGENTS.md` treats as a finding, and the co-occurrence that would price it is measured for the first time — **15 of 217,189 steps (0.0069 %)** across **5 of 1100 episodes**, Pearson **-0.0161** — which bounds how often the order can matter without deriving it. Also open on approval: the v5.1 row above must be moved to `SUPERSEDED` for SS3/SS4/SS5/SS9/SS10, and the ordering battery (`TEST-A7-15`/`-16`) and oracle agreement (`TEST-A7-14`) are `NOT_RUN` |
| ScenarioNet integration, zero episode tail | `specifications/scenarionet_integration_v1.4_specification.md`; version `1.4`; amends v1.1 §49/§18.1 only | `AUTHORITATIVE` for the frozen tail value; explicit user approval 2026-08-09, recorded by ADR-058 | `implementation/reward_scale_and_episode_contract_v1_exec_plan.md`; `IN_PROGRESS`; M-tail not started | `extra_steps_after_scenario: 50 -> 0`. Not independently releasable: requires `evaluation_protocol_v1.3` in the same release, otherwise a competent-but-slower-than-expert policy is scored zero. Episode returns from every prior run become incomparable |
| Evaluation protocol, continuous progress as primary metric | `specifications/evaluation_protocol_v1.3_specification.md`; ID `EVAL-PROTOCOL`, version `1.3`; amends v1.2 reporting only; the reporting of the binary success event further amended by `specifications/evaluation_protocol_v1.3.1_amendment.md` (`EVAL-PROTOCOL-V1.3.1`, `APPROVED` 2026-09-01: success is reported against a measured feasibility ceiling; the primary continuous progress-ratio metric and v1.0's statistical protocol are unchanged) | `AUTHORITATIVE` for the reporting contract; explicit user approval 2026-08-09, recorded by ADR-059 | `implementation/reward_scale_and_episode_contract_v1_exec_plan.md`; `IN_PROGRESS`; M-report not started | `route_completion` becomes the primary continuous metric (no threshold); the geometric gate stays the binary event; nuPlan's making-progress gate at `0.2` zeroes degenerate episodes in evaluation only. Benchmark comparability is claimed on the Waymo panel only, because `s/s_goal` is own-route completion whereas nuPlan's is expert-relative and PG has no expert |
| LiDAR observation, traffic-light blindness | `specifications/observation_lidar_v2.0.1_amendment.md`; ID `OBS-LIDAR-V2.0.1`, version `2.0.1`; amends `OBS-LIDAR-V2.0` §5 only | `AUTHORITATIVE` for the amended limitation list; explicit user approval 2026-08-09, recorded by ADR-062 | `implementation/reward_scale_and_episode_contract_v1_exec_plan.md`; `IN_PROGRESS`; M-lidar not started | Ego raycast mask drops `InvisibleWall`, removing an unintended signal-phase channel from the LiDAR point cloud. IDM light compliance and `vehicle.red_light` use different code paths and are unaffected. Schema, `D=6489` and `ENC-V1.4` unchanged; that the raycast currently returns the air wall is `INFERRED` until `TEST-RSEC-011` runs |
| Unified driving mission and route progress v1.0 | `specifications/driving_mission_v1.0_specification.md`; ID `DRIVING-MISSION-V1.0`, version `1.0` | `SUPERSEDED` by DRIVING-MISSION-V1.1 on 2026-08-04; historical authority from user approval 2026-08-02 and ADR-053 | `implementation/driving_mission_v1.0_exec_plan.md`; historical partial implementation through M5 | Retained for reproducibility and traceability. Its intermediate mandatory gates, bounded/clamped progress, and broad final-gate envelope are superseded by ADR-054. Historical code and artifacts remain unchanged as migration inputs until v1.1 milestones replace them. |
| Route-coordinate driving mission v1.1 | `specifications/driving_mission_v1.1_specification.md`; ID `DRIVING-MISSION-V1.1`, version `1.1` | `AUTHORITATIVE`; explicit user approval 2026-08-04, recorded in ADR-054; supersedes DRIVING-MISSION-V1.0 | `implementation/route_coordinate_driving_mission_v1.1_exec_plan.md`; `APPROVED`, implementation authorized | Unified mission with one immutable canonical XY-arc-length route, exact sequential cursor, pure `delta_s` R4, separate completion values, shared immutable snapshot, ten local route samples, and one offline anchor-based final gate. Final gate builder uses `0.01 m` offline merge/anchor coverage tolerance; runtime consumes one frozen segment passively. PG/Waymo offline derivation may differ, runtime is source-independent. Implementation, migration, tests, audit, and smokes proceed under the approved ExecPlan. |
| Route-coordinate driving mission v1.1.1 amendment | `specifications/driving_mission_v1.1.1_amendment.md`; ID `DRIVING-MISSION-V1.1.1`, version `1.1.1` | `AUTHORITATIVE`; explicit user approval 2026-08-04, recorded in ADR-055; amends v1.1 occurrence orientation and mission-local route representation | `implementation/route_coordinate_driving_mission_v1.1_exec_plan.md`; `APPROVED`, implementation authorized | Frozen `assigned_route_lane_ids` remain the sole topology. Offline sequence-constrained global two-state orientation resolves each occurrence, then trims one canonical mission-local route from reset to goal. SDC trajectory is offline-only evidence; runtime consumes no trajectory or source-station offset. Corrected builder audit: 3,500/3,500. Dataset generation now builds and validates missions after Rulebook filtering and before split selection; split and freeze require the validated mission schema. Artifact regeneration awaits the provisioned `pyarrow` environment. M8 (2026-08-06): semantic observation (OBS-V1.3) and the LiDAR 22D navigation block now consume the mission's canonical route and the shared per-step `MissionSnapshot.s_m` instead of independently reprojecting the ego (`AC-RCM-006`); Rulebook wrong-way/wrong-carriageway intentionally stays on the legacy source-declared-direction route per §6 legal-direction independence. Observation schema versions bumped (`DEC-MSN-004`); a pre-existing `MissionRuntime.snapshot` bug affecting every v1.1.1 mission was found and fixed alongside it. |
| Rulebook scalarization v1.0 | `specifications/rulebook_scalarization_v1.0_specification.md`; ID `SCAL-V1.0`, version `1.0` | `AUTHORITATIVE` for the three original modes and all sections not amended by `SCAL-V1.1`; explicit user approval 2026-07-17 | `implementation/rulebook_scalarization_v1.0_exec_plan.md`; implementation in progress | Configurable scalarizer for Rulebook margins and scalar RL baselines |
| Rulebook scalarization v1.1, priority-weighted rank amendment | `specifications/rulebook_scalarization_v1.1_specification.md`; ID `SCAL-V1.1`, version `1.1`; amends `SCAL-V1.0` §3.1, §5, §6, §7, §9-14 (adds `bounded_priority_weighted_rank` mode and optional post-hoc `symlog` reward compression); does not replace `SCAL-V1.0` | `AUTHORITATIVE` for the amended subset; explicit user approval 2026-08-06, recorded by `ADR-057` (`DEC-SCAL11-001`=`A`, `002`=`B`, `004`=`B`) and `ADR-056` (`DEC-SCAL11-003`, closed, companion `rulebook_v4.12` fix) | `implementation/rulebook_scalarization_v1.1_exec_plan.md`; `IMPLEMENTED`; new default mode `bounded_priority_weighted_rank` (`priority_base=3.0`, embeds each margin's severity in its own priority-weighted term instead of a diluted shared tie-breaker, per-mode frozen `priority_base` now strictly validated); optional `reward_compression.mode=symlog` (off by default, `DEC-SCAL11-002`); `raw_scalar_reward`/`scalar_reward` diagnostics split in `wrapper.py`/`ScalarizationResult`; `reward_compression_mode` added to the resume-compatibility identity (`reward_semantics.py`); `conf/scalarization/default.yaml` updated; 12 new/updated tests, scoped ruff clean, full repository suite (1297 passed, 47 pre-existing unrelated failures, zero new) | Full training ablation comparing the two modes with/without compression is out of scope (§2.3); the algebraic-guard test is not retroactively required for the three `SCAL-V1.0` modes (`DEC-SCAL11-004`=`B`); `make smoke` not run in the implementing session |
| Perception-bounded semantic observation v1.3 | `specifications/observation_v1.3_specification.md`; ID `OBS-V1.3`, version `1.3-perception-bounded`; supersedes OBS-V1.2 for the selected path; the `lane_road` group and the flat dimension `D` amended by `specifications/observation_v1.3.1_amendment.md` (`OBS-V1.3.1`, `APPROVED` 2026-09-01 under `DEC-RB51-001`: `lane_road` 12 -> 14, `D` 3009 -> **3011**, `ENC-V1.3`'s `lane_road` projection widened to match, checkpoint compatibility intentionally broken) | `AUTHORITATIVE`; explicit user approval 2026-07-29, recorded by ADR-033 | `implementation/semantic_observation_causal_correctness_v1.3_exec_plan.md`; `IMPLEMENTED`; 28 review findings corrected (geometry, causality, Rulebook consistency, ranking, dead dimensions); `D` 3064 -> 3009; amended 2026-07-31 (`REQ-028`..`REQ-030`): reachable static taxonomy, `compliance_history` -> `context_history`, `yellow_onset_memory` -> `signal_onset_state`, explicit justification of the 21-step context window; full suite 1174 passed, rulebook 368 passed, semantic_v3 smoke run end to end | Known limitations carried forward in OBS-V1.3 §10, chiefly the exact lane association of other actors |
| Perception-bounded semantic observation v1.2 | `specifications/observation_v1.2_specification.md`; ID `OBS-V1.2`, version `1.2-perception-bounded` | `AUTHORITATIVE`; explicit user approval 2026-07-21, recorded by ADR-022; SS6.2 amended 2026-08-01 by ADR-045 | `implementation/perception_bounded_semantic_observation_v1.2_exec_plan.md`; M0–M6 complete; physical first-hit/signal gates, causal history, source-limited unknowns, encoder/checkpoint path, and provider-backed smoke verified; signal camera range/FOV/height now configurable via `conf/obs/semantic_v3.yaml` (default 80 m/65°/1.2 m unchanged, ADR-045) | Ideal semantic tracking remains an explicit baseline limitation; calibrated stochastic tracking is deferred |
| Rulebook-aligned stacked LiDAR observation v2.0 | `specifications/observation_lidar_v2.0_specification.md`; ID `OBS-LIDAR-V2.0`, version `2.0-rulebook-aligned`; new mode alongside unchanged `OBS-V1.1`/`stacked_lidar_state`; the causal frame contract amended by `specifications/observation_lidar_v2.0.1_amendment.md` and then by `specifications/observation_lidar_v2.0.2_amendment.md` (`OBS-LIDAR-V2.0.2`, `APPROVED` 2026-09-01 under `DEC-RB51-001`: the frame widens 308 -> 310 for the posted speed limit and its availability flag, so the stacked dimension stated by that amendment becomes `310 * 21 + 21 = 6531`, checkpoint compatibility intentionally broken) | `AUTHORITATIVE`; explicit user approval 2026-07-30, recorded by ADR-036 | `implementation/lidar_arm_temporal_alignment_and_lq_tokenization_v2.0_exec_plan.md`; `VERIFIED` (M0–M6 all complete); `stacked_lidar_v2` 21-frame masked observation (`D=6489`), derived from `DASHED_TCAP_S`; full suite 1174 passed, lint/format-check/`make config`/`make config-gpu` clean, end-to-end smoke (`TEST-020`) passes for both `stacked_lidar_v2`+`lq_lidar` and the legacy `stacked_lidar_state` | Known limitations in OBS-LIDAR-V2.0 §5: `yellow_must_stop` unreconstructible (per ADR-033), neighbor slots not identity-stable, sector bearing is ego-relative under yaw rate. Three pre-existing bugs in `CausalLidarFrameBuilder`/`RayNoiseWrapper` were found and fixed with regression tests while validating this plan (two `isinstance(config, dict)` duck-typing bugs, one `Lidar.perceive()` return-shape bug) — none was reachable by the pre-existing unit-test mocks, so the LiDAR arm had never been exercised in a real training loop before this plan |
| LiDAR latent-query encoder v1.4 | `specifications/encoder_v1.4_specification.md`; ID `ENC-V1.4`, version `1.4-lidar-tokenized` | `AUTHORITATIVE`; explicit user approval 2026-07-30, recorded by ADR-036 | `implementation/lidar_arm_temporal_alignment_and_lq_tokenization_v2.0_exec_plan.md`; `IMPLEMENTED`; `lq_lidar` 38-token STECA-inspired tokenizer (sector tokens + circular PE only, no two-stage attention) reusing the frozen LQ core unchanged | No migration path from any other encoder checkpoint by design |
| Automatic curriculum learning | `specifications/automatic_curriculum_learning_v1_specification.md`; version v1 amended by §28 / ADR-014. For the ScenarioNet semantic-arm selector the authoritative contract is `specifications/automatic_curriculum_learning_v2.0_specification.md` (`ACL-SN-EMA-001`, `v2.0`, supersedes v1.3/v1.2/v1.1 in full) | `AUTHORITATIVE`; original approval 2026-07-16, ScenarioNet scalar amendment explicitly approved 2026-07-19, `v1.3` approved 2026-07-29 (ADR-032), **`v2.0` approved 2026-09-01 (ADR-077)** | `implementation/scenario_acl_implementation_plan.md`; its internal implementation stages v1/v2 are complete, v3 not started, and v4 deferred | Runtime learner smoke, live ScenarioNet/Rulebook wiring, resume validation, and final reconciliation before `VERIFIED` |
| ScenarioNet integration | `specifications/scenarionet_integration_v1.1_specification.md`; amended by `scenarionet_integration_v1.2_specification.md` and `scenarionet_integration_v1.3_specification.md` (v1.3 changes only evaluation-pool cardinality and frozen subset artifacts) | `AUTHORITATIVE`; v1.3 explicitly approved 2026-07-31 and recorded by ADR-042 | `implementation/scenarionet_multi_panel_evaluation_v1.2_exec_plan.md`; `IN_PROGRESS` | The materialized frozen index must be regenerated with the v1.3 complete/diagnostic panel artifacts before official runs. |
| RL baselines | `specifications/rl_baselines_v1_specification.md`; ID `RL-BASELINES`, version `1.0`; four amendments now recorded in the document's own Executive Summary as `RSA-1`..`RSA-4`: truncation/bootstrap by ADR-024, off-policy replay capacity unified at `300000` by ADR-036 point 8, `gamma = 0.996` by ADR-075 as amended by ADR-081 (2026-09-07), and SAC/TD3 minibatch `512` with `update_to_data_ratio 0.5` and the `gradient_steps=auto` resolution by ADR-078 (2026-09-06) | `AUTHORITATIVE`; explicit user approval 2026-07-20 | `implementation/run_profile_td3_diagnostics_exec_plan.md`; this narrow sub-plan (Hydra run-profile budgets, diagnostic defaults, replay-persistence default) is itself `VERIFIED` (re-confirmed 2026-07-26, `38 passed`) — the broader RL-BASELINES epic remains `IN_PROGRESS`, with no single ExecPlan yet covering it; SB3 deviation ledger recorded in `implementation/sb3_fork_deviation_ledger.md` | Resolved end-to-end presets, manifest save/load path, ACL formula reconciliation, GPU learner smoke, and full acceptance matrix remain — all require real training/learner runs, out of scope for a no-long-runs session |
| Perception-bounded encoder v1.1 | `specifications/encoder_v1.1_specification.md`; ID `ENC-V1.1`, version `1.1-perception-bounded` | `AUTHORITATIVE`; explicit user approval 2026-07-21, recorded by ADR-022 | `implementation/perception_bounded_semantic_observation_v1.2_exec_plan.md`; encoder, SB3 bridge, checkpoint schema identity, and provider-backed smoke complete | No migration path to legacy OBS/ENC checkpoints by design |
| Perception-bounded encoder v1.3 | `specifications/encoder_v1.3_specification.md`; ID `ENC-V1.3`, version `1.3-perception-bounded`; supersedes ENC-V1.1/ENC-V1.2 for the OBS-V1.3 path | `AUTHORITATIVE`; explicit user approval 2026-07-29 | `implementation/semantic_observation_causal_correctness_v1.3_exec_plan.md`; `IMPLEMENTED`; four token-projection widths and the MLP input adapted to `D=3009`; `LatentQueryEncoderV2` left at the OBS-V1.1 widths; default Hydra composition moved to `obs=semantic_v3` + `encoder=lq_v3` on 2026-07-31 | No migration path from ENC-V1.1/V1.2 checkpoints by design |
| Perception-bounded encoder v1.2, dynamic-slot-identity removal | `specifications/encoder_v1.2_specification.md`; ID `ENC-V1.2`, version `1.2-perception-bounded`; supersedes ENC-V1.1 for the latent-query (LQ) variant only, the MLP variant is unchanged | `AUTHORITATIVE` for the LQ variant; explicit user approval 2026-07-26, recorded by ADR-026 | `implementation/semantic_v3_dynamic_ranking_and_slot_embedding_removal_exec_plan.md`; `VERIFIED`; `dynamic_slot_embedding` removed from `LatentQueryEncoderV3` (spurious per-slot identity under sticky/first-fit assignment); slot-invariance regression test added; 39-test focused suite and ruff lint pass | No migration path from ENC-V1.1 LQ-variant checkpoints by design; restart of the 3 active production runs is a separate pending decision — `sac-0` and `ppo-0` already crashed on checkpoint reload during implementation and were left stopped, `td3-0` remains at risk |
| Transition-level replay v1 | `specifications/transition_replay_v1_specification.md`; ID `TRANSITION-REPLAY`, version `1.0`; **persistence and resume classification amended by `specifications/transition_replay_v1.1_amendment.md`** (`TRANSITION-REPLAY-V1.1`, REQ-023/024/024a/025/025a/033, §8.9, §9.1; approved 2026-09-06, ADR-079); **`REQ-013`/`AC-016` amended by `specifications/transition_replay_v1.1.1_amendment.md`** (`TRANSITION-REPLAY-V1.1.1`, approved 2026-09-07, ADR-082). The two amendments are disjoint: v1.1 governs what is persisted and how a resume is classified, v1.1.1 governs the priority given to a transition that has never been priced | `AUTHORITATIVE`; explicit user approval 2026-07-17; v1.1 amendment explicit user approval 2026-09-06; v1.1.1 amendment explicit user approval 2026-09-07 | `implementation/transition_replay_v1.0_exec_plan.md`; `IN_PROGRESS`; configuration, N-step, canonical collection, PER, persistence, and reward compatibility implemented; the v1.1 periodic snapshot is implemented by `implementation/resumable_training_after_abrupt_interruption_v1_exec_plan.md` | Focused replay matrix and current Hydra preset matrix pass; source-backed learner smoke and checkpoint/resume remain pending |
| Transition-level replay v1.1.1, priming-priority amendment | `specifications/transition_replay_v1.1.1_amendment.md`; ID `TRANSITION-REPLAY-V1.1.1`, version `1.1.1`; amends `REQ-013` and `AC-016` only (the priority given to a transition that has never been priced); every other requirement of v1.0 is unchanged | `AUTHORITATIVE` for the amended `REQ-013`/`AC-016` subset; explicit user approval 2026-09-07, recorded by ADR-082 | `implementation/transition_replay_v1.0_exec_plan.md`; no implementation required — the code already primes at the exact current maximum, and did so before the amendment | `REQ-013` had stated two different rules and called them one: its prose gave the current maximum with `1.0` as the *initial* value, its invariant gave `max(1, current)` as a permanent floor. The amendment keeps the prose rule, because the sampler is otherwise exactly equivariant to a rescaling of the priorities (verified bit-exact: 100.000 % identical addresses under `|TD|x100` without the floor, 0.641 % with it) and a constant in reward units would be its only scale-dependent term, in units ADR-081 moved the same day. **Open, deliberately**: the buffer maximum has never been logged during a run, so the converged light-tailed regime in which a floor would bind is simulated but unobserved; instrumenting it is deferred with `C38` |
| Lexicographic/distributional RL | No dedicated approved specification found | `MISSING` | No authority can be inferred from literature or exploratory documents | Approved algorithms, interfaces, and acceptance criteria |
| Evaluation and algorithm comparison protocol | `specifications/evaluation_protocol_v1.0_specification.md`; ID `EVAL-PROTOCOL`, amended by v1.1 and `evaluation_protocol_v1.2_specification.md` (v1.2 multi-panel full-pool execution/reporting) | `AUTHORITATIVE`; v1.2 explicitly approved 2026-07-31 and recorded by ADR-042 | `implementation/scenarionet_multi_panel_evaluation_v1.2_exec_plan.md`; `IN_PROGRESS` | Runtime, artifacts, analysis, and the terminal monitor are being reconciled to the v1.2 contract. |
| Experimental and reporting protocols | `protocols/algorithm_comparison_protocol.md`, `protocols/csv_evaluation_objectives.md`, and `protocols/live_eval_video_protocol.md`; no versions declared | `CANDIDATE`; normative statistical/seed content of `algorithm_comparison_protocol.md` (10-seed protocol, 95% CI formula, reward×curriculum ablation framing) superseded by `EVAL-PROTOCOL` v1.0; `csv_evaluation_objectives.md` retained as the subordinate implementation-level CSV schema `EVAL-PROTOCOL` references; `live_eval_video_protocol.md` retained as implementation guidance, amended by `EVAL-PROTOCOL` REQ-014's post-hoc qualitative-selection scheme. See `EVAL-PROTOCOL` `DEC-008`. | Operational commands exist in `setup/validation_commands.md` | Implementation reconciliation against `EVAL-PROTOCOL`; none of the three candidate documents is deleted, only superseded/consolidated/amended in normative scope |

Document paths in the document and implementation columns are relative to
`docs/`. Source, test, and configuration paths are relative to the repository
root.

## Historical Material

- Rulebook v4.13 (`specifications/rulebook_v4.13_specification.md`, explicit
  user approval 2026-08-09) supersedes v4.12 **in full**: v4.12's entire
  subject, the `0.1 m/s` physics-noise deadband on the `wrongway` cost, ceases
  to exist because v4.13 replaces the sub-rule with `wrong_direction`, whose
  `2 m/s` manoeuvre tolerance already sits twenty times above that noise floor.
  It also absorbs v4.10 §7.3-bis (`wrong_carriageway`), whose formula is
  unchanged but which is no longer a separately aggregated sub-rule. `ADR-050`
  and `ADR-056` are superseded by `ADR-060` on the merits, following the user's
  standing instruction that earlier approvals be re-evaluated against new
  evidence rather than treated as immutable. v4.12 is retained for
  reproducibility of runs performed under it.
- `scenarionet_integration_v1.4` and `evaluation_protocol_v1.3` form one
  indivisible release (`ADR-058`, `ADR-059`). Neither is scientifically
  acceptable alone: the zero tail without ratio reporting would zero-score a
  competent-but-slow policy, and ratio reporting without the zero tail would
  leave the empty-world exploit in place. Any partial adoption must be treated
  as a deviation.
- The R2 cost scale remains an **open** approval gate (`DEC-RSEC-001` in
  `implementation/reward_scale_and_episode_contract_v1_exec_plan.md`). No
  authority record selects a formulation yet; the decision is bound to the
  outcome of the expert-trajectory measurement recorded in that plan, and none
  of the 2026-08-09 approvals above may be read as pre-approving it.
- Rulebook version `4.7-final-implementation-complete` is the canonical
  approved identifier; it supersedes Rulebook v4.6 for the selected rulebook
  scope. Earlier archived Rulebook labels used a different
  increment convention, so their numeric relationship must not be interpreted as
  semantic-version precedence.
- `specifications/rulebook_v4.6_specification.md` remains retained historical
  material for reproducibility of v4.6 experiments and is not the current
  implementation authority.
- `archive/plans/rulebook_v1_specification.md` is archived historical material.
  No inspected authority record formally establishes its supersession chain.
- The temporary names `rulebook_v4.4_final_corrected(1).md`,
  `rulebook_v4.4_final.md`, `rulebook_v4.1_final_updated.md`, and
  `observation_spec_v1.0_final_implementation_complete.md` were not found.
- Do not create a supersession relationship from these names alone.
- ScenarioNet integration v1 is superseded by
  `specifications/scenarionet_integration_v1.1_specification.md` following the explicit
  user approval recorded on 2026-07-16. Its specification and implementation
  plan remain historical traceability records.
- OBS-V1.1 and ENC-V1.0 remain historical, reproducible contracts for their
  existing experiments. ADR-022 supersedes them only for the selected semantic
  observation/encoder implementation path; it does not change legacy runtime
  modes implicitly.
- Rulebook v4.8 (`specifications/rulebook_v4.8_specification.md`) amends only
  v4.7 §6.4 (R2 vehicle/static clearance), replacing the vehicle sub-metric
  with a scoped lateral-RSS metric and demoting static clearance to
  diagnostic-only per ADR-025, explicit user approval 2026-07-24. v4.7
  remains authoritative and unchanged for every other section (§2-§4, §6.1-
  §6.3, §6.5-§6.6, §7-§17), including RSS longitudinal, TTC, VRU clearance,
  and vehicle-yield.
- Rulebook v4.9 (`specifications/rulebook_v4.9_specification.md`) amends only
  v4.7 §5.4/§5.6/§5.8 (the R1 collision cost), replacing normalization by the
  configured speed cap — which made the cost depend on scenario configuration
  at equal physical impact severity — with the MAIS3+F injury-risk logistic of
  Lubbe et al. (2022), per ADR-027, explicit user approval 2026-07-26. v4.7
  remains authoritative and unchanged for the rest of R1 (§5.1-5.3 collision
  onset, normal definition, pre-state closing speed; §5.5 numerical floor;
  §5.9 contact hook) and for every other rule. v4.8 is an independent
  amendment on R2 and does not interact. The two amendments are disjoint and
  both apply.
- `EVAL-PROTOCOL` v1.0 (`specifications/evaluation_protocol_v1.0_specification.md`)
  is approved and authoritative, following explicit user approval on
  2026-07-24. It replaces the normative statistical/seed content of
  `protocols/algorithm_comparison_protocol.md` (10-seed protocol, the
  `1.96*s/sqrt(n)` 95% CI formula, and the reward-setting x curriculum
  ablation framing), consolidates `protocols/csv_evaluation_objectives.md`
  as a subordinate implementation-level CSV schema, and amends
  `protocols/live_eval_video_protocol.md` only where its REQ-014 post-hoc
  qualitative-selection scheme changes it. All three candidate documents
  are retained for traceability, not deleted. Approval of `EVAL-PROTOCOL`
  is a scientific-contract decision only; the repository implementation
  does not yet conform (no ExecPlan exists yet; see the ExecPlan Registry
  entry below and `EVAL-PROTOCOL` §11 for the specific verified gaps).

## Decisions

| ADR | Status | Approval evidence | Affected scope |
|---|---|---|---|
| `decisions/ADR-001-scenarionet-v1-1-dataset-policy.md` | `APPROVED` | Explicit user approval of ScenarioNet Integration v1.1 on 2026-07-16 | ScenarioNet v1.1 dataset, ACL arm, horizon, and eligibility policy |
| `decisions/ADR-002-semantic-observation-and-encoder-contract.md` | `APPROVED` | Explicit user approval of observation v1.1 and encoder v1.0 on 2026-07-16 | Semantic observation, encoder architecture, SB3 integration, and checkpoint compatibility |
| `decisions/ADR-004-assigned-route-metadata-for-pg-and-waymo.md` | `APPROVED` | Explicit user approval on 2026-07-17 | Offline assigned-route metadata and runtime anti-leakage boundary for PG and Waymo |
| `decisions/ADR-003-causal-ctrv-conflict-zone-prediction.md` | `APPROVED` | Explicit user approval on 2026-07-17 | Causal filtered CTRV for vehicle conflict-zone occupancy, frozen defaults, history lifecycle, and experimental separation |
| `decisions/ADR-007-waymo-batch-throughput.md` | `APPROVED; amended by ADR-010` | Explicit user approval on 2026-07-17 | Waymo acquisition throughput: 64 unseen shards per cycle, with cap amended to 256 |
| `decisions/ADR-008-pg-compositional-replenishment.md` | `APPROVED; amended by ADR-009` | Explicit user approval on 2026-07-17 | Bounded PG composition replenishment trigger and two-cycle limit |
| `decisions/ADR-009-pg-targeted-replenishment.md` | `APPROVED` | Explicit user approval on 2026-07-17 | Targeted 1,750-candidate PG budget allocated by observed arm deficits; no parameter calibration in this cycle |
| `decisions/ADR-010-waymo-cap-expansion.md` | `APPROVED` | Explicit user approval on 2026-07-17 | Waymo cumulative cap increased to 256 unseen shards; batch size remains 64 |
| `decisions/ADR-011-rulebook-scalarization-v1.md` | `APPROVED` | Explicit user approval of SCAL-V1.0 on 2026-07-17 | Scalarization modes, rulebook adapters, default, reward interface, future replay semantics, and compatibility |
| `decisions/ADR-012-stratified-source-arm-split-allocation.md` | `APPROVED` | Explicit user approval on 2026-07-18 | ScenarioNet primary-split source-arm stratification while retaining exact global source totals |
| `decisions/ADR-013-pytorch-cross-architecture-validation.md` | `APPROVED` | Explicit user approval on 2026-07-19 | PyTorch 2.9.1 cross-architecture pin and narrowly validated ARM64 cuSPARSELt checker exception |
| `decisions/ADR-014-scenarionet-acl-learning-potential-only.md` | `APPROVED` | Explicit user approval on 2026-07-19 | ScenarioNet ACL usefulness equals learning potential; mutation is prohibited; Rulebook curriculum inputs are diagnostic-only |
| `decisions/ADR-016-scenario-acl-vectorized-execution.md` | `APPROVED` | Explicit user approval of DEC-VEC-001--DEC-VEC-005 and deterministic resume restart policy on 2026-07-20 | Parent-controlled selective reset, fresh-batch uniqueness, per-episode LP provenance, deterministic commit ordering, PPO partial-rollout persistence, and explicit active-slot restart on resume |
| `decisions/ADR-015-r1-pre-state-centerline-normal.md` | `APPROVED` | Explicit user approval on 2026-07-20 | R1 derives every ego-to-other normal from pre-state canonical-footprint centers, independent of Bullet manifold geometry |
| `decisions/ADR-018-parallel-evaluation-and-test.md` | `APPROVED` | Explicit user instruction to implement synchronous evaluation/test parallelism on 2026-07-21 | Spawned evaluation/test workers, deterministic reset/reduction, ScenarioNet/ACL scheduling, and live video rendering |
| `decisions/ADR-019-asynchronous-evaluation-queue.md` | `APPROVED` | Explicit user confirmation of FIFO asynchronous ordinary/ACL diagnostics, staged barrier, fatal errors, and parent Rich UI on 2026-07-21 | Immutable evaluator snapshots, FIFO queue, fatal propagation, and training/final-test integration |
| `decisions/ADR-020-evaluation-video-diagnostics.md` | `APPROVED` | Explicit user approval of compact live/replay diagnostic overlays on 2026-07-21 | Shared GIF annotator, selected/cumulative reward, Rulebook subrules, route/target and optional actor outlines |
| `decisions/ADR-021-source-bounded-reactive-traffic.md` | `APPROVED` | Explicit user approval to retain reactive traffic only through source-track validity on 2026-07-21 | Source-bounded ScenarioNet IDM traffic lifecycle; no fallback traffic after an actor record ends |
| `decisions/ADR-022-perception-bounded-semantic-observation.md` | `APPROVED` | Explicit user approval to proceed on 2026-07-21 | OBS-V1.2/ENC-V1.1, planar first-hit LiDAR, symbolic signal visibility, causal memory, and strict preflight policy |
| `decisions/ADR-023-scoped-vehicle-yield-source-contract.md` | `APPROVED` | Explicit user instruction to implement the proposed source-bound vehicle-yield contract on 2026-07-21 | Rulebook v4.7 §7.9 causal live wiring; explicit pairwise and roundabout metadata; no geometric-priority fallback |
| `decisions/ADR-024-runtime-scenario-data-abort.md` | `APPROVED` | Explicit user approval of the typed data-abort/quarantine contract on 2026-07-23 | Typed runtime scenario non-evaluability, valid-prefix truncation, vector selective reset, ACL exclusion, run-local quarantine, evaluation coverage and comparison exclusions |
| `decisions/ADR-025-r2-lateral-rss-clearance-replacement.md` | `APPROVED` | Explicit user instruction "Approvo la specifica v4.8, procedi con l'implementazione" on 2026-07-24 | Rulebook v4.7 §6.4 R2 clearance replacement: vehicle clearance → scoped lateral-RSS metric, static clearance → diagnostic-only, VRU clearance unchanged, new R2 aggregation, `pre_state` snapshot |
| `decisions/ADR-026-dynamic-slot-identity-removal-and-context-quota-ranking.md` | `APPROVED` | Explicit user instruction "sì procedi" on 2026-07-26, after two review rounds (internal architectural analysis plus a user-run external literature search) and explicit rejection of a minimal/conservative framing | ENC-V1.2 (removal of `LatentQueryEncoderV3`'s dynamic-slot identity embedding); OBS-V1.1 §8.1 Context-quota ranking key replaced with plain Euclidean distance; Conflict-quota ranking explicitly kept unchanged |
| `decisions/ADR-027-r1-injury-risk-collision-cost.md` | `APPROVED` | Explicit user message "confermo MAIS3+F, età 65, static→car driver" on 2026-07-26, after the three open decisions were presented with options, numeric consequences, and a motivated recommendation each | Rulebook v4.7 §5.4 R1 collision cost: configured-speed-cap normalization (scenario-dependent, demonstrably inverting real severity) replaced by the MAIS3+F injury-risk logistic of Lubbe et al. (2022); severity level, reference age, and `STATIC_COLLIDABLE` mapping decided; `MOTORCYCLIST` class deferred |
| `decisions/ADR-031-arm-difficulty-threshold-provenance.md` | `APPROVED` | Explicit user confirmation "In pratica sono stati calibrati per poter equilibrare il numero di scenari in ciascun arm, giusto?" / "Sì da va bene" on 2026-07-27 | Documents the previously untraceable `arms.py` A0--A5 difficulty constants as an intentional coverage-driven heuristic (`DEC-024`, promoted from provisional to confirmed), not a statistical calibration like `tau_low`/`tau_dense`; also records the arm/source confounding in the frozen train split (A0--A2 all PG, A3--A5 all Waymo) as a separate open item; no code, spec value, or dataset change | (renumbered from `ADR-028` on 2026-07-29 to resolve a three-way numbering collision)
| `decisions/ADR-032-acl-generate-catalog-decoupling.md` | `APPROVED` | Explicit user instruction "procedi" on 2026-07-29, after the paper-fidelity analysis separating the corrective part from the project addition and after the user accepted redoing the in-progress runs | Generate eligibility decoupled from scenario-buffer membership (`DEC-008`, removes `FIND-001`'s root cause and `FIND-005`); per-arm coverage cycles (`DEC-009`); `buffer_capacity` 1000 -> 250 (`DEC-010`); sequential-path alignment (`DEC-011`); narrows `ADR-028` to a residual safety net |
| `decisions/ADR-036-lidar-arm-temporal-alignment-and-lq-tokenization.md` | `APPROVED` | Explicit user instruction "procedi ad implementare tutto il piano" on 2026-07-30, after gate-by-gate presentation of `DEC-001`–`DEC-010` with a clarification pass on `DEC-008` | `OBS-LIDAR-V2.0`/`ENC-V1.4` (new); 21-frame Rulebook-derived stacking with a validity mask replacing the undated 5-frame default; STECA sector tokenization + circular PE only (no two-stage attention); no neighbor-slot identity embedding (`ADR-026` precedent); `yellow_must_stop` exposure remains forbidden per `ADR-033`; `buffer_size` unified at 300000 across SAC/TD3, `fast` profile overridden to 24000 |
| `decisions/ADR-037-empirical-holdout-policy-and-dual-test-panels.md` | `APPROVED` | Explicit user approval "mi sembra vada tutto bene" on 2026-07-31, following decision-by-decision approval of `DEC-001`, `DEC-003`–`DEC-006` in the ExecPlan | `SCENARIONET-INTEGRATION` v1.2 and `EVAL-PROTOCOL` v1.1 (both new); holdout-first empirical validation/test reservation ignoring `A0`-`A5` labels; dual test endpoints (`test_waymo_empirical` primary, `test_arm_stratified` competence, `test_pg` secondary); per-arm training minimums replacing exact quotas; `checkpoints/final.zip` explicitly retained as sole official checkpoint (materially supersedes `ADR-012` for validation/test splits only) |
| `decisions/ADR-038-waymo-map-based-grouping-key.md` | `APPROVED` | Explicit user approval "mi sembra vada tutto bene" on 2026-07-31, following the same-session decision to resolve `DEC-002` unconditionally rather than gated on audit completion | `SCENARIONET-INTEGRATION` v1.2 §3.2 (amends v1.1 §6.1); map-identity fingerprint grouping fallback for Waymo, adopted unconditionally since the fingerprint used to measure co-location is the same one needed to fix it; co-location audit interrupted at 35,000/54,104 scenarios once found non-decision-relevant, resumable at `M2` for descriptive reporting only |
| `decisions/ADR-039-label-blind-pg-holdout-replenishment.md` | `APPROVED` | Explicit user approval "procedi" on 2026-07-31 | `SCENARIONET-INTEGRATION` v1.2 §3.3; up to five equal-profile PG holdout batches, triggered solely by the total Rulebook-eligible PG count and never by arm deficits |
| `decisions/ADR-042-full-pool-multi-panel-evaluation.md` | `APPROVED` | Explicit user request to implement the approved multi-panel plan on 2026-07-31 | ScenarioNet v1.3 and EVAL-PROTOCOL v1.2: complete pools, frozen smoke/fast subsets, shared validation snapshot, serial final panels, and scope isolation |
| `decisions/ADR-040-v1-2-balanced-train-selection.md` | `APPROVED` | Explicit user confirmation that the v1.1 train result was satisfactory on 2026-07-31 | `SCENARIONET-INTEGRATION` v1.2 §3.1/§3.5; exact 1,100 Waymo + 1,100 PG train targets with v1.1 near-uniform arm selection and structural-cell source compensation |
| `decisions/ADR-041-preserve-scarce-waymo-a0-for-training.md` | `APPROVED` | Explicit user agreement on 2026-07-31 after inspection of the real A0 allocation | `SCENARIONET-INTEGRATION` v1.2 §3.4/§3.5; preserve at least 20 scarce `A0_simple_low_traffic × Waymo` records for train, allowing the stratified A0 source mix to compensate with PG |
| `decisions/ADR-043-evaluation-video-ego-trail-and-checkpoint-overlay.md` | `APPROVED` | Explicit user instruction "come preferisci ... Procedi pure poi ll'implementazione" on 2026-07-31 | Amends `decisions/ADR-020-evaluation-video-diagnostics.md`: permits an ego-actually-traveled trail layer (previously excluded by name) distinct in style from the existing planned-route line, plus discrete planned-checkpoint markers |
| `decisions/ADR-044-step-timing-instrumentation.md` | `APPROVED` | Explicit user answers to the `DEC-001`/`DEC-002`/`DEC-003` options presented on 2026-08-01 (all three recommendations accepted) | New per-chunk, per-component wall-clock breakdown output (`step_timing.csv`, tidy format) and new `evals.csv` GIF-render-timing columns; establishes a new metric/data-logging convention |
| `decisions/ADR-045-configurable-signal-camera-geometry.md` | `APPROVED` | Explicit user approval on 2026-08-01, after the OBS-V1.2 SS6.2 deviation and its consequences were presented | Signal-camera range/FOV/height (80 m/65°/1.2 m in OBS-V1.2 SS6.2) are now overridable via `conf/obs/semantic_v3.yaml`; default unchanged, so no existing experiment's behavior changes unless its config is edited |
| `decisions/ADR-046-drivable-surface-seam-closing.md` | `APPROVED` | Explicit user approval of the audit-derived ExecPlan on 2026-08-01 | Amends v4.7 §7.2.1: 0.10 m morphological closing on the drivable-surface union, removing measured inter-lane-polygon seam off-road false positives |
| `decisions/ADR-047-ego-braking-physical-bound.md` | `APPROVED` | Explicit user approval of the audit-derived ExecPlan on 2026-08-01 | Amends v4.7 §6.2.4 step 10: `MAX_REFERENCE_BRAKE_MPS2` raised from 4.0 to 8.0 m/s² (dry-asphalt tyre-road limit, equal to `b_i`); calibration artifact regenerated (`ego_min_brake_mps2 = 8.0`) |
| `decisions/ADR-048-rss-standstill-applicability.md` | `APPROVED` | Explicit user approval of the audit-derived ExecPlan on 2026-08-01 | Amends v4.7 §6.2.1: longitudinal RSS `NOT_APPLICABLE` when both actors are below 0.1 m/s, removing the permanent R2 cost on a correctly stopped queue |
| `decisions/ADR-049-wrong-carriageway-subrule.md` | `APPROVED` | Explicit user approval of the audit-derived ExecPlan on 2026-08-01 | New v4.7 §7.3-bis: `wrong_carriageway` R3 sub-rule charging opposing-carriageway occupancy by footprint area fraction, silent inside junctions via aligned-lane subtraction |
| `decisions/ADR-050-wrongway-status-deadband.md` | `APPROVED` | Explicit user approval, post-approval follow-up, 2026-08-01 | `wrongway` `status` field (diagnostic only, not `cost`) gains a 0.1 m/s deadband against standstill physics noise; no specification amendment (status is not part of any formula) |
| `decisions/ADR-051-traffic-control-route-successor-extension.md` | `APPROVED` | Explicit user approval, 2026-08-02 | Traffic-control candidate selection extended to the route's unambiguous single successor lane; amends `rulebook_v4.11_specification.md` |
| `decisions/ADR-052-unified-driving-mission-contract.md` | `APPROVED` | Explicit user approval 2026-08-02 | Single immutable mission/tracker authority for R4, observations, Rulebook route relevance, completion, success, metrics, and gate diagnostics; material choices `DEC-MSN-001` through `DEC-MSN-009` approved |
| `decisions/ADR-053-geometric-gate-only-mission-boundaries.md` | `APPROVED` | Explicit user approval 2026-08-03 | Amends ADR-052/DRIVING-MISSION-V1.0: ordered geometric gates are the only task boundary; lane/graph reachability never terminates and Rulebook costs retain legality/risk authority |
| `decisions/ADR-056-wrongway-cost-physics-noise-deadband.md` | `APPROVED` | Explicit user approval 2026-08-06 | Supersedes `ADR-050`'s "cost stays untouched" scope conclusion; `wrongway` `cost` gains the same `0.1 m/s` deadband as `status`; amends `rulebook_v4.12_specification.md` |
| `decisions/ADR-057-scalarization-v1.1-defaults.md` | `APPROVED` | Explicit user approval 2026-08-06 | Resolves `DEC-SCAL11-001` (`A`: `bounded_priority_weighted_rank` becomes default), `DEC-SCAL11-002` (`B`: `symlog` compression off by default), `DEC-SCAL11-004` (`B`: no retroactive algebraic guard for the three `SCAL-V1.0` modes) |
| `decisions/ADR-058-zero-episode-tail-after-logged-horizon.md` | `APPROVED` | Explicit user approval 2026-08-09, under the user's own literature condition | `extra_steps_after_scenario` frozen at `0`; removes the empty-world regime after the logged horizon. Not releasable without `ADR-059` |
| `decisions/ADR-059-progress-ratio-as-primary-reported-metric.md` | `APPROVED` | Explicit user approval 2026-08-09 (`"Scelgo B2-A"`), including acceptance that no ratio threshold is introduced | `route_completion` becomes the primary reported metric; geometric gate stays the binary event; nuPlan making-progress gate at `0.2` in evaluation only; benchmark comparability scoped to the Waymo panel |
| `decisions/ADR-060-unified-memoryless-wrong-direction-subrule.md` | `APPROVED` | Explicit user approval 2026-08-09, plus separate approval of the memoryless reformulation after the Markov-observability constraint was raised | Merges `wrongway` and `wrong_carriageway` into `wrong_direction`; `2`/`6 m/s` anchors; supersedes `ADR-050` and `ADR-056` |
| `decisions/ADR-061-bounded-control-reference-route-polyline.md` | `APPROVED` | Explicit user approval 2026-08-09 | Prefix-identical bounded route extension used only for `route_s_m` of traffic controls; extends `ADR-051`; targets the residual 25.2% of signalised Waymo scenarios |
| `decisions/ADR-062-ego-lidar-excludes-traffic-light-air-walls.md` | `APPROVED` | Explicit user approval 2026-08-09 | Ego raycast mask drops `InvisibleWall`; removes an unintended signal-phase channel from the LiDAR arm without affecting IDM compliance or `vehicle.red_light` |
| `decisions/ADR-063-rss-longitudinal-demoted-to-diagnostic.md` | `APPROVED` | Carried by `RULEBOOK-V5.1`, explicit user approval 2026-08-14 | `rss` longitudinal leaves the reward for a reported diagnostic: not controlled-invariant, 18.29 % of applicable expert steps, and removing it alone accounts for ~97 % of the expert's deficit. Withdraws `DEC-RSEC-001` |
| `decisions/ADR-064-traffic-control-persistence-latches-removed.md` | `APPROVED` | Carried by `RULEBOOK-V5.1`, explicit user approval 2026-08-14 | `crosswalk`/`vehicle_yield` latches removed on observability and credit assignment (86.9 % of traffic-control cost), explicitly not on scale (~2 reward points of 203) |
| `decisions/ADR-065-r3-geometric-criteria-redefined.md` | `APPROVED` | Carried by `RULEBOOK-V5.1`, explicit user approval 2026-08-14 | `offroad` 0.3 m band (nuPlan), `solid_line` graded penetration replacing a binary 1.0, `wrong_carriageway` gated on the ego centroid (0 expert violations in 217,189 steps). Resolves `DEC-RSEC-008` |
| `decisions/ADR-066-wrongway-deleted-dashed-line-retained.md` | `APPROVED` | Carried by `RULEBOOK-V5.1`, explicit user approval 2026-08-14 | `wrongway` deleted (1 step in 217,189, duplicated by `wrong_carriageway` and R4), superseding `ADR-060`; `dashed_line` retained unchanged because both observation paths already carry the 21-step window derived from `DASHED_TCAP_S` |
| `decisions/ADR-067-r2-scoping-clearance-ttc-and-rss-lateral.md` | `APPROVED` | Carried by `RULEBOOK-V5.1`, explicit user approval 2026-08-14 | `clearance` scoped to VRU whose centroid is on the drivable surface (0.327 % -> 0.199 %); `ttc` to nuPlan's uniform 0.95 s; `rss_lateral` deliberately left unchanged after its motivating hypothesis was falsified (14 % benefit only) |
| `decisions/ADR-068-speed-limit-normative-sub-rule.md` | `APPROVED` | Carried by `RULEBOOK-V5.1`, explicit user approval 2026-08-14 | `speed_limit` added at nuPlan's 2.23 m/s tolerance (0.000 % expert violations), closing a degeneracy created by R4's speed-ratio margin; amends both observation contracts and intentionally breaks checkpoint compatibility; the v1 vehicle-cap fallback is prohibited |
| `decisions/ADR-069-scal-v1.2-rank-preserving-scalarization.md` | `APPROVED` | Carried by `RULEBOOK-V5.1`, explicit user approval 2026-08-14 | `SCAL-V1.2` at `a=2.2, sigma=0, phi=0.25, lambda=1`: Veer et al. (ICRA 2023) Thm. 1 structure with severity outside the priority weight and the two adaptations the per-step setting forces. Expert mean +19.90 -> +31.50 on identical costs; admissibility condition verified by exhaustive search |
| `decisions/ADR-070-at-fault-gating-for-interaction-sub-rules.md` | `APPROVED` | Carried by `RULEBOOK-V5.1`, explicit user approval 2026-08-14 | At-fault gating of `clearance`/`ttc`/`rss_lateral` at `v_ego <= 5e-02 m/s`, after §4.10.3 measured 68.2 % of the stopped-ego penalty mass as a controlled-invariance failure (`clearance` alone 79.9 % of its own cost). Anchored on RSS blame and nuPlan's `_get_collision_type`. Expert mean +29.09 -> +31.14, below-standstill 8.82 % -> 7.45 %, macro R3 bit-identical. Four risks recorded as unretired |
| `decisions/ADR-071-r1-at-fault-classification-and-truncation.md` | `APPROVED`, not yet implemented | Explicit user approval 2026-08-12 ("Sì, procedi con ADR-071") | R1 collision impact gains nuPlan-taxonomy at-fault classification; at-fault -> termination (`V=0`) + R1 cost as today; not-at-fault -> truncation (bootstrap `V(s)`), no R1 cost. Extends ADR-070's blame principle from R2 to R1. Coordinates with ADR-058 on the episode contract. Every classifier input already exists at the R1 evaluation point except lane-containment for the lateral branch |
| `decisions/ADR-072-relaxable-lane-rules-below-mission-progress.md` | `APPROVED` | Carried by `RULEBOOK-V5.1`, explicit user approval 2026-08-14 | Five-level hierarchy: `solid_line`, `wrong_carriageway` and `dashed_line` move **below** mission progress as L5, leaving only never-relaxable rules above it. The dividing question is whether a competent driver may relax the rule to complete a mission. Buys exactly **one** ordering, O2 ("completing with a brief violation beats standing still for ever") -- v5.0 already passed O3/O4/O5/O6, and an earlier draft claiming four was wrong. L5 aggregates by normalized sum with a fixed denominator of 3, because `max` would make a second concurrent violation free and break O6. Expert mean +31.14 -> +73.84 from the restructure alone, and +70.70 once ADR-076's L6 is priced at `lambda6 = 0.2`; below standstill 7.45 % -> 3.36 %. O1 is unfixable by any hierarchy and its failure under strict lex is asserted as an executable property |
| `decisions/ADR-073-l4-signed-advance-normalized-by-fixed-reference.md` | `APPROVED` | Carried by `RULEBOOK-V5.1`, explicit user approval 2026-08-14 | L4 is the **signed** route advance over a **fixed** `D_REF = v_ref * dt = 2.2222 m`, superseding v4.7's R4. Amended 2026-08-14 from the monotone (running-maximum) form: the signed form is invariant to repeated traversal by compensation rather than by memory, so it carries no hidden state and **withdraws `REQ-RB5.1-OBS-01`**; `enable_reverse=False` means the agent cannot execute the oscillation the memory guarded against, and `sigma - s` measured non-zero on 20.99 % of expert steps but never above 0.053 m, i.e. projection jitter that the running maximum rectified directionally. Expert mean unchanged (+73.85 -> +73.84). `D_REF` is a unit, not a calibration: it cancels out of the episode ceiling. The clip is calibrated to MetaDrive's own `max_speed_km_h = 80` = `v_ref`, and the Test A figures are a lower bound because it truncates positive advance downwards. *Corrected 2026-09-08*: it does **not** follow that the clip never binds on an agent trajectory — the cap bounds ground travel, not the projection (ADR-035, `ROUTE_CONTINUITY_JUMP_FACTOR = 2.0`). Records two measured rejections: normalizing by each record's own route length (the panel's shortest route, 19.96 m against a 171.98 m mean, dictated the weight ceiling for all 1100, giving 32.7 and an expert mean of -8.49) and capping the advance at the reference speed under that normalization (max `Delta q` identical to six figures, telescoping broken on 1298 steps). Corrects the earlier claim that satiability was needed for O3 | Route completion leaves the reward for the episodic metric. **Unchanged by ADR-076**, which considered folding a per-step time cost into this channel, measured it, and rejected it: inside L4 it would price duration above lane compliance and turn O3 from a theorem into a calibration. `REQ-RB5.1-GAMMA` resolved by ADR-075, then reopened in substance as `REQ-RB5.1-O3-DISCOUNT` when ADR-081 set `gamma = 0.996`: this ADR's own Risk 1 predicted exactly that failure — the shortcut wins **at L4** and L5 is never reached — and the risk register was not consulted when the discount moved |
| `decisions/ADR-074-scal-v1.3-hybrid-scalarization.md` | `APPROVED` | Carried by `RULEBOOK-V5.1`, explicit user approval 2026-08-14 | `SCAL-V1.3` keeps `SCAL-V1.2` verbatim for L1-L3 and gives L4/L5 a **finite** exchange, because no finite weight can dominate a continuous increment as it tends to zero. Two ways to force uniform dominance rejected: an indicator on L4 rewards infinitesimal creep ("stop for ever" becomes "creep for ever along the marking"), an indicator on a progress threshold invents a calibrated cliff. Condition `lambda4 + 0.1*eta < a`; selected `lambda4=2.0, eta=1.0` over the marginally better 2.15 to leave budget for L5. Withdraws v5.0 SS11.8's false claim that the scalarization is episodic lexicographic order -- nothing is lost, since `SCAL-V1.2` never had it either | `eta` is the one weight not pinned by measurement: the expert almost never relaxes, so the panel cannot discriminate. Exposure bounded to one arm of four |
| `decisions/ADR-075-undiscounted-return-for-the-rule-hierarchy.md` | `APPROVED`, **amended by ADR-081** (`gamma = 0.996` since 2026-09-07) | Explicit user approval 2026-08-20 | **`gamma = 1` on every channel and every arm**, resolving `REQ-RB5.1-GAMMA`. The deciding measurement is not about O3: exponential discounting erodes the geometric priority weights at different rates, so at `gamma = 0.99` a collision **157 steps (15.7 s)** away costs less than one non-relaxable violation now -- inside a 20 s episode. O3 measured in the scalar arm fails by **-1.5 to -9.5** at 0.99 and holds exactly only at 1.0, which is the unique value for which `sum Delta q` telescopes. Withdraws two claims the specification made: that the discount is "immaterial" for the scalar arm, and that O3 "holds only approximately" at 0.99. Three remedies falsified with figures: a threshold on L4 (the `tau4` that ties the legal route with the shortcut also ties it with a run abandoning **20 %** of the route), potential-based shaping (`gamma^T Phi_T` still favours the shorter episode, since termination is on arrival), and raising `eta` (bounded by `lambda4 + 0.1*eta < a`). Literature: the field uses 0.99 **unjustified** (MetaDrive itself, and V-Max on Waymax); Tercan & Prabhu (ECAI 2024) prove discounting conflicts with thresholded lexicographic ordering in value-based methods and note the original TLO validation (Vamplew 2011) was **undiscounted**. Six algorithm configs updated; `learning_potential_gamma` must track `gamma` (Ng et al.) | Off-policy stability: the Bellman operator is not a sup-norm contraction at 1, though the task is proper. Declared fallback **`gamma = 0.999` for all arms together**, never per-arm, with the O3 degradation reported. Tercan & Prabhu's own remedy is policy gradient rather than a `gamma` choice, making PPO the safe carrier of the thresholded arm |
| `decisions/ADR-077-acl-v2-outcome-based-windowed-learning-progress.md` | `APPROVED` | Explicit user approval 2026-09-01 of `DEC-201`-`DEC-206` as a block, plus selection of the simpler option on `DEC-207` | ACL `v2.0` replaces the prediction-error learning potential with a **windowed ordinal outcome-based** progress signal: per arm and per dimension, Vargha-Delaney `G` of the `H = 20` most recent Generate episodes against the `H` preceding ones, whose expectation is exactly `0.5` under exchangeability **regardless of variance** -- which removes `LIM-002` (`ADR-029` named this fix and deferred it) at its root, verified over a `250x` noise-scale range rather than argued. Feedback `A = 0.5 + D(G - 0.5)` with a Goldilocks gate `4g(1-g)`; exact conditional permutation deadband at `0.05` **per dimension, uncorrected** (Holm removed on measurement: it halves sensitivity while the false fires it suppresses are symmetric in sign and cost variance, not bias -- **not** because it blocked the curriculum, which the same script falsifies). Five dimensions in the `RULEBOOK-V5.1` order `L1 -> L2 -> L3 -> T -> L5`, with `T` standing in L4's position and L4/L6 excluded as redundant/time-preference. The teacher consumes only environment and Rulebook outcomes, so the curriculum is **identical across PPO/TD3/SAC** and therefore across the four reward settings -- which is what makes the reward-setting x curriculum comparison interpretable. Scenario buffer becomes an arm-balanced recency memory (no usefulness, no rank). The `v1.3` prediction-error channel keeps running, strictly inert, as a diagnostic so both signals are measurable in one run. Schema `acl_ema_v3` -> `acl_progress_v1`, no migration |
| `decisions/ADR-078-sac-learner-update-throughput.md` | `APPROVED` | Explicit user approval 2026-09-06, in two steps (batch/UTD first, then the diagnostic switch) | SAC replay minibatch `256 -> 512` with `update_to_data_ratio 0.5` on every non-smoke profile: `gradient_steps: auto` now resolves to `round(train_freq * n_envs * ratio)`, so with 20 workers the learner consumes the **same 5 120 replay samples per update call in half the optimizer steps**, which is where the measured 179 ms/step learner cost lived (A/B screening `step_timing.csv`, 2026-09-06). The post-update TD-residual learning potential — diagnostic-only under ACL v2.0 `REQ-013`, one extra replay batch and four forward passes per update — is switched off by `update_learning_potential_diagnostic: false` (SAC and TD3). TD3 receives the same setting (third approval step, closes `open_items` `D8`); PPO unchanged pending its first measured run (`D9`). Amends `RL-BASELINES` §3.4 / `REQ-RLB-007` / §9.4 and the `EVAL-PROTOCOL` `DEC-015` parenthetical, following ADR-075's precedent of amending without rewriting. Projected, not measured: ~3.9 -> ~5.2 fps, `thesis` ~107 -> ~80 h; the first run under it must replace the projection |
| `decisions/ADR-079-crash-safe-resume-and-periodic-replay-snapshot.md` | `APPROVED` | Explicit user approval 2026-09-06 of `DEC-RES-002..005` ("approvo tutto") plus the end-of-run cleanup addition (`DEC-RES-006`) | **Supersedes ADR-017.** `TRANSITION-REPLAY` v1.1: the replay buffer is paired with every periodic model checkpoint (`persistence.trigger=periodic_and_final`, cadence `checkpoint.periodic_interval_steps` with crossing semantics, `keep_last=1`), `run_profile.replay_persistence=true` on every standard profile (≈ 7.2 GB per off-policy run at full buffer, 12 writes per `thesis` run); every resume artifact published atomically with the training state as commit marker; SIGTERM handled like Ctrl+C (`compose.yaml` `stop_grace_period: 120s`); REQ-025 empty-segment resume fails closed unless `checkpoint.resume.allow_replay_reset=true` and is then logged as `replay_reset=true`; `checkpoint.resume.checkpoint_name=periodic` resolves to the newest complete pair; torn-snapshot check on `model_num_timesteps`; intermediate replay copies deleted after `final`; interrupt handlers no longer write a mid-chunk snapshot (`DEC-RES-007`, recorded for review). Tracks issue #3 / `open_items` `C13` |
| `decisions/ADR-076-l6-progress-rate-below-relaxable-lane-compliance.md` | `APPROVED` | Explicit user approval 2026-08-20 | A **sixth level `L6 progress_rate` below relaxable lane compliance**, `c_L6 = 1 - clip(Delta q, 0, 1)`, making the scalarization **`SCAL-V1.4`**. Not severable from ADR-075: an undiscounted L4 telescopes, and arrival alone does not bound speed -- **measured**, the median mission needs 3.40 m/s to arrive inside its horizon against a 22.22 m/s vehicle cap, an indifference band **6.5x wide** inside which slower driving strictly reduces L2 exposure. Placed BELOW L5 so an illegal shortcut (`c_L5 > 0`) loses before the level is reached: the weight on L6 is **unconstrained by O3 in the lexicographic and distributional arms**, and only the scalar arm keeps the coupling (`lambda6 < 0.25`, selected 0.2). `sum c_L6 = T - Q`, so L6 ranks by duration exactly while still giving a per-step gradient a flat time counter would not. TLO thresholds: the width needed at `tau4` **collapses** (L4 now ties to exact arithmetic, against 0.400 with a time cost inside L4 and 1.975 at `gamma = 0.99`) and there is **no `tau6`** (last objective unthresholded, Gabor et al. 1998), which makes the arm structurally identical to the setting where TLO was validated undiscounted. **`tau4 = 0` is the fixture value, not a training value** -- learned trajectories never tie exactly, so at 0 the comparison would stop at L4 and L5/L6 would decide nothing. The value is **not fixed here and is sequenced after the rulebook is frozen**: the object a threshold applies to differs by mechanism -- Q-values under Gabor et al. 1998, a slack from the state's own optimum under Li & Czarnecki 2019 (already relative, so it sidesteps the mission-length problem), expected returns under a policy-gradient construction. Settled regardless: the **channel is not renormalized**, so SS4.1.1's measured rejection and the scalar arm's calibration are untouched. Open item `D1`. **Records the rejected alternative**: the same quantity folded into L4 as `kappa = 0.01`, implemented and measured (+69.90 against the +73.84 baseline), rejected because inside L4 every unit of time preference is a unit the shortcut can spend on its own violation -- and because the O3 bound caps it below `a^2 = 4.84`, the thing it would have to outweigh. Literature: CaRL (CoRL 2025) reports the stationary optimum and takes the opposite branch (discount supplies urgency, plus a hard `Blocked` infraction), available to them because their reward has no priority ordering to erode | The scalar arm is measurably weaker: `lambda6 = 0.2` buys 3.18 reward units between a crawl and a full-speed completion against 4.84 for one L2 step, so it prefers speed only while crawling saves fewer than **0.66** L2 steps. A property of scalarization, to be **reported as a result**. L6 charges a legitimately stopped ego; gating it on L3 was rejected as cross-level coupling. The reference shortcut is stipulated, not fitted -- the expert never takes one |
| `decisions/ADR-081-severity-slope-and-the-discount-that-admits-it.md` | `APPROVED` | Explicit user approval 2026-09-07 ("confermo a=2.5 e γ=0.996"); ExecPlan waived by the user, so this register row and the specification callouts are its only record | **`a = 2.5`, `sigma = 0.30`, `gamma = 0.996`**, amending ADR-075 and `RULEBOOK-V5.1` SS4.4/SS5.4/SS5.5; `lambda4 = 2.0`, `eta = 1.0`, `lambda6 = 0.2`, `phi = 0.25` unchanged. `sigma = 0` was inherited from `SCAL-V1.2` and had never been priced in the six-level hierarchy; `a = 2.2` was a lower bound with no upper bound. Criterion: the deceleration `a_req^max = (w2*sigma + phi)*v_ref / (2*lambda4*tau)` below which the reward's local gradient inside a TTC violation points the wrong way -- 1.46 m/s^2 shipped, 12.43 m/s^2 selected, against a braking limit of about 9. Measured by the new `v51_calibration_grid` on the 1100-record Waymo panel (evidence in `docs/audits/reward_calibration_2026-09-07/`): below standstill 3.36 % -> 4.55 %, `w3`/tail 1.04 -> 1.18, expert p1 -61.8 -> -99.4. `gamma = 0.996` follows from `Delta = ln(a)/-ln(gamma) > 199` steps (0.995 gives 183 and fails); the binding comparison is one level apart, so ADR-075's table was 2x too generous. Guarded by `test_every_algorithm_shares_one_hierarchy_preserving_discount` | Checkpoints written under the previous reward or discount are neither comparable nor resumable (compounds `D4`). `scripts/measure_expert_rulebook_transition.py` keeps `FINAL_PRIORITY_BASE = 2.2`, `FINAL_SEVERITY = 0.0` on purpose, so that its baseline row keeps reproducing SS5.5; the production point is priced by the calibration grid, not by those constants |

| `decisions/ADR-083-progress-last-and-the-discount-at-the-measured-horizon.md` | **`PROPOSED`** — the architecture and `gamma = 0.9982` carry explicit user approval 2026-09-09 and every `A7` SS6 gate 2026-09-10, but **this record and `RULEBOOK-V5.2` are new text and are not themselves approved**; no production code may be written against either | Explicit user approval 2026-09-09 (architecture, `gamma = 0.9982`) and 2026-09-10 (`w5 = 0.15`, `phi = 0`, the budget rule, both mandatory-test changes, the specification form, the `AB-LEARN` amendment, the `RB51` disposition, the vocabulary break); ExecPlan `A7`, `M2` | Five channels with progress **last and unthresholded**; `L6 progress_rate` **deleted**; the three negotiable lane rules **above** progress, charged by a bounded satisfaction indicator at `w5 = 0.15` with a severity slope; `phi` removed; one shared `gamma = 0.9982` with `learning_potential_gamma` equal to it and the criterion's **verdict** asserted beside the value. **Amends ADR-075 and ADR-081** (the discount), **supersedes ADR-076** (`L6`), **partly reverts ADR-072** — and the reversal is priced rather than argued: v5.0's pathology was the *price*, not the placement, since v5.0 charged a relaxable violation at `a = 2.2` per step so standing still won past **37** relaxed steps, while at `w5*(1+sigma) = 0.195` it wins only past **416** (mean Waymo span) to **848** (mean PG span) steps against a 500-step episode. Supersedes ADR-069/ADR-074 for the scalar *form* only; ADR-063..ADR-071 and ADR-073 **untouched**. Two candidate architectures were **falsified** rather than merely rejected: charging the negotiable channel as a full priority level fails O2 and O4 because 20 steps of full-severity relaxation cost 26 against `a^3 = 15.6`. **Intentional breaks**: **checkpoint** compatibility (arity 6 -> 5, new schema id, new weight set) and the **recorded channel vocabulary** — the second needing a guard the 2026-08-20 rename did not, because A7 keeps four of five names so a stale consumer produces a *populated and wrong* table rather than an empty one; the vector schema id therefore joins the analysis condition identity. Re-bases two dependent contracts without rewriting either: `AB-LEARN`'s pre-registered arm B (amended **before** any run, the joint freeze surviving) and `ACL-SN-EMA-001` v2.0 SS3.2, whose six-margin *fatal if absent* property makes the ACL as written unable to run on an A7 rulebook. Does **not** fix: no-credit-without-motion (`+1.345` against a `0.000` baseline, made visible rather than caused), the negative-clip ratchet (`+72` reward units per lap at zero net displacement, and it matters *more* under A7), the legal parallel corridor (318 of 3500 records, every remedy shape foreclosed by one approved specification), collide-to-escape on the scalar arm (prefers colliding by 1092.8), and `K2 > K3` |

## ExecPlan Registry

| Feature | Specification | ExecPlan | Reported status | Last document update |
|---|---|---|---|---|
| Observation / signal / chunk-boundary audit fixes v1 | `OBS-V1.3` + `OBS-V1.3.1` (unchanged), `OBS-V1.2` §7, Rulebook v4.7 §7.6.7 mapping, `TRANSITION-REPLAY` v1 episode boundaries; one runtime-source deviation `DEV-AF-001` approved by the user 2026-09-07 | `implementation/observation_signal_audit_fixes_v1_exec_plan.md` (`OBS-AUDIT-FIX-001`) | `IMPLEMENTED` (M1-M4, 2026-09-07). Closes `open_items` `C44` and `C45` and extends `C17` and `C18` (drafted as `C14`-`C17` on the branch, renumbered on merge 2026-09-08 because `main` had taken those numbers): the LiDAR admission gate compared MetaDrive object ids with source actor ids and left the `semantic_v3` agent blind to every other road user (reproduced, `DEC-AF-001`); `context_history` was appended twice per step (`DEC-AF-002`, one batch per step, also halves the observation build cost); flashing red lights aborted the episode because MetaDrive simplifies the state before it is readable (`DEC-AF-003`); the non-curriculum loop reset every slot at each chunk boundary without a truncation flag (`DEC-AF-004`). All regression tests fail on the pre-fix tree (7 failures + 1 import error, verified by bind-mounting the pre-fix sources) and pass after. **Consequence**: policies trained on `semantic_v3` before 2026-09-07 never observed traffic and are not comparable with later ones. The reward-contract findings of the same audit (red-light violation positive expected value under SCAL-V1.4; goal placed before the stop line; ADR-061 unimplemented) are user decisions, not part of this plan | 2026-09-07 |
| Resumable training after abrupt interruption v1 | `specifications/transition_replay_v1_specification.md` REQ-023/024/025/033 as amended by `specifications/transition_replay_v1.1_amendment.md` (`TRANSITION-REPLAY-V1.1`, `APPROVED` 2026-09-06, ADR-079 superseding ADR-017); `specifications/rl_baselines_v1_specification.md` checkpoint policy unchanged | `implementation/resumable_training_after_abrupt_interruption_v1_exec_plan.md` (`RESUME-ABRUPT-001`) | `IMPLEMENTED` (M1–M4, 2026-09-06). Closes [issue #3](https://github.com/EmanueleRsp/thesis-metadrive/issues/3) / `open_items` `C13` under ADR-079 (`DEC-RES-002..006` approved, `DEC-RES-007`/`008` implementation decisions recorded). New `runtime/io/atomic.py`, `runtime/io/resume_snapshot.py`, `runtime/signals.py`; both training loops write every resume artifact atomically with the training state as commit marker, only at chunk boundaries; the replay buffer is paired with each periodic checkpoint (`trigger=periodic_and_final`, crossing semantics, `keep_last=1`, deleted after `final`), `checkpoint.resume.checkpoint_name=periodic` resolves the newest complete pair, torn-snapshot/seed checks, REQ-025 `allow_replay_reset` + `replay_reset=true` event, SIGTERM as Ctrl+C, best keys restored; ACL loops gained periodic checkpoints with a frozen curriculum copy and a vectorized-path interrupt handler. Validation: focused suite `74 passed` + regression `3 passed`, full suite `1704 passed`, ruff lint clean on touched files, focused format clean on new files; kill -9 smoke (`TEST-RES-010`) `PASS`: killed mid-chunk, resumed twice from `periodic` snapshots, ran to the full budget, wrote `final`, removed the intermediate replay copies and completed its three final evaluation panels (`status: completed`), with a strictly monotone `checkpoint_index.csv`; it also found a pair-validator basename defect (fixed, regression test added), and one intermediate attempt died of the unrelated issue #2 (`C7`, non-vectorized data abort). Limitations in ExecPlan §12 (process-level SIGTERM and loop-level `replay_reset`/best-key wiring verified by code reading) | 2026-09-06 |
| Startup coverage for the production composition | None amended; driven by `AGENTS.md`'s regression-test rule and the defect it records | `implementation/production_composition_startup_coverage_exec_plan.md` (`SMOKE-COV`) | `IMPLEMENTED` for the selected option. Closes a **month-old defect that made every `obs=semantic_v3` run unstartable** -- `envs/factory.py` injected three `semantic_v3_signal_*` keys that `ThesisScenarioEnv.default_config()` never declared, and MetaDrive rejects undeclared keys at construction, so `make run-train` itself could not start. Invisible since 2026-08-01 because `make smoke` selects `obs=lidar_state`; surfaced by the `AB-LEARN` screening smoke. `DEC-COV-001` selects a fast non-GPU test performing MetaDrive's **own** construction merge for every `conf/obs/*.yaml`, over a second smoke preset: it covers the whole defect class exhaustively at no cost and runs on every `make test`, whereas a production smoke costs minutes and needs dataset and GPU. The production smoke is **deferred, not rejected** (`M2`), and if added belongs in a separate opt-in pre-launch target rather than inside `make smoke`. Finding kept: the first version of the test compared flat key sets and had a real blind spot, because MetaDrive's rejection recurses into nested dictionaries and the passthrough writes into `vehicle_config.lidar` -- when an external library owns an acceptance rule, assert by calling it. Known limitation: construction is covered, execution is not, so an observation/encoder dimension mismatch still surfaces only at run time | 2026-09-02 |
| Reward learnability A/B screening | `specifications/evaluation_protocol_v1.0_specification.md` (`REQ-018` ablation separation, `DEC-007`) with the v1.1/v1.2/v1.3/v1.3.1 amendments; `specifications/rulebook_v5.1_specification.md` and `SCAL-V1.4` as the reward under test | `implementation/reward_learnability_ab_screening_exec_plan.md` (`AB-LEARN`) | `AWAITING_DECISIONS`. Pre-registers arms **A** (MetaDrive native reward) and **B** (`RULEBOOK-V5.1` six-level vector scalarized by `SCAL-V1.4`) of the A/B/C/D reward x curriculum factorial recorded in `open_items` `D4`; arms **C**/**D** are blocked on the unimplemented ACL (`F5`). Answers optimizability, not correctness: `RULEBOOK-V5.1` established the preference order against 1100 logged Waymo records, and no training run can add to that. Three decisions approved by the user 2026-09-01 -- arm A is expressed as `reward=monitor_only` rather than `reward=native`, because `native` detaches the Rulebook wrapper entirely (`conf/reward/native.yaml`, `behavior: "off"`) and would leave arm A without any compliance metric; budget is `run_profile=medium` (350k steps) on seeds `[0, 1]`, a `REQ-018` diagnostic that de-risks before the six full-budget runs and cannot by itself close the freeze (`DEV-AB-001`); probe algorithm is SAC only. `M1` complete: two additive presets under `conf/presets/learnability/`, with the single-factor invariance **measured** -- the two fully resolved Hydra configurations differ only in `reward.name/type/behavior`, the derived `lambda_env`/`lambda_rule`, and run identity. `M3` blocked on GPU authorization (`DEC-AB-004`), the only open gate. `DEC-AB-005` approved 2026-09-01: a passing screening freezes rulebook and scalarization jointly, the `thesis`-budget confirmation arriving as a by-product of the core runs that follow. Records the asymmetry that makes this coherent -- the **ACL is frozen in the opposite order**, *before* its comparison runs, because `ACL-SN-EMA-001` v2.0 SS11 requires approval to precede any run under it; arms C/D therefore **measure** the curriculum rather than gating it, and a C/D result showing no benefit is a thesis result to report, not a defect to remove by adjusting the ACL until it helps. Pre-registers the hypotheses and the decision rule before any run, because the 2026-08-08 four-configuration diagnostic established that at reduced budget the *ranking between* arms is not the same quantity as the *learning slope within* an arm; that diagnostic's numbers are void, since the reward it measured was subsequently falsified | 2026-09-01 |
| Unified driving mission and route progress v1.0 | Historical `specifications/driving_mission_v1.0_specification.md` (`DRIVING-MISSION-V1.0`; superseded by v1.1 and ADR-054) | `implementation/driving_mission_v1.0_exec_plan.md` | `SUPERSEDED`; partial implementation retained for migration/traceability | 2026-08-04 |
| Route-coordinate driving mission v1.1 | `specifications/driving_mission_v1.1_specification.md` (`DRIVING-MISSION-V1.1`, approved and authoritative); ADR-054 | `implementation/route_coordinate_driving_mission_v1.1_exec_plan.md` | `APPROVED`; implementation authorized | 2026-08-04 |
| Route-coordinate driving mission v1.1.1 amendment | `specifications/driving_mission_v1.1.1_amendment.md` (`DRIVING-MISSION-V1.1.1`, approved and authoritative); ADR-055 | `implementation/route_coordinate_driving_mission_v1.1_exec_plan.md` | `APPROVED`; implementation authorized; corrected builder audit 3,500/3,500; artifact regeneration pending provisioned `pyarrow`; M8 shared-consumer wiring (`AC-RCM-006`) for semantic/LiDAR observation implemented 2026-08-06 | 2026-08-06 |
| **Rulebook v5.1 six-level hierarchy and `SCAL-V1.4`** | `specifications/rulebook_v5.1_specification.md` (`RULEBOOK-V5.1`, **APPROVED** 2026-08-14, amended 2026-08-20 and 2026-09-07); ADR-063..ADR-076, ADR-081 | `implementation/rulebook_v5.1_six_level_hierarchy_exec_plan.md` (`RB51`) | **`IN_PROGRESS`**. **Corrected 2026-09-09: this row had said `AWAITING_DECISIONS` with `DEC-RB51-001`/`-002` gating M4/M7, and "Production is at `SCAL-V1.1` with four margins and fourteen components". All three statements were stale and dated 2026-08-20.** The plan records those gates plus `DEC-RB51-005` **approved 2026-08-20**, and milestones `M0`-`M7` and `M-DIAG` **done by 2026-09-01**; production has run `six_level_priority_weighted_rank` on `vector_schema_id: rulebook_v5_1_six_level_v1` since then (`conf/scalarization/default.yaml`). The `SCAL-V1.1` sentence was the plan's own 2026-08-20 findings-log description of the state *before* implementation, promoted into this register as present tense; it is what made a later session mis-sequence A7 (see `A7` §4.1). What remains open is the tail of `M8` -- the `AC-RB5.1-*` walk-through, the command set as a set, and this row -- plus the conditional `M9`. Supersedes `RSEC-V1` downstream of its `DEC-RSEC-001`. **Its disposition under the A7 architecture is `DEC-A7-009`, awaiting approval** | 2026-09-09 |
| **A7 five-channel rulebook architecture and `gamma = 0.9982`** | **No approved specification yet**: writing it is `DEC-A7-001` and milestone `M2` of the plan, and no production implementation may begin before it is approved. `specifications/rulebook_v5.1_specification.md` (`RULEBOOK-V5.1`) plus `SCAL-V1.4` remain authoritative for the architecture A7 replaces. Evidence of record: `audits/rulebook_architecture_2026-09-09/` and `audits/progress_channel_integrity_2026-09-09/` (the second authoritative wherever the two disagree). A new ADR is required; next free number `ADR-083` | `implementation/rulebook_a7_five_channel_hierarchy_exec_plan.md` (`A7`) | **`AWAITING_DECISIONS`**. The **architecture and `gamma = 0.9982` were approved by the user 2026-09-09**; `w5 = 0.15`, `phi = 0`, the removal of `eta` and `lambda6`, every budget value, and the changes to the mandatory tests are the plan's content and are gates `DEC-A7-001`..`-012`. Five channels `K1..K5` with mission progress **last and unthresholded**, `L6 progress_rate` deleted, negotiable lane compliance charged by a bounded indicator **above** progress: six free weights become four. Takes the discount to the criterion at the **measured** 500-step training horizon, closing `C49`. `lambda4 = 2.0` stands. `M1` is a measurement and depends on no gate. Owns the reward: `AB-LEARN`'s pre-registered arm B is amended by `DEC-A7-008` and `RB51`'s disposition is `DEC-A7-009` | 2026-09-09 |
| Rulebook v2 | `specifications/rulebook_v4.7_specification.md` | `implementation/rulebook_v2_implementation_plan.md` | `COMPLETED; final reconciliation before VERIFIED` | 2026-07-17 |
| Rulebook v2, R2 lateral-RSS clearance replacement | `specifications/rulebook_v4.8_specification.md` | `implementation/r2_lateral_rss_clearance_v4.8_exec_plan.md` | `VERIFIED`; ADR-025 approved; scoped lateral-RSS component, VRU-only clearance, aggregation/registry wiring, and REQ-R2-06 regression test implemented; full rulebook suite (306 tests) and `make rulebook-v2-check` pass | 2026-07-24 |
| Vehicle-yield pre/post-state conformance | `specifications/rulebook_v4.7_specification.md` §7.9 (DEC-005) | `implementation/vehicle_yield_pre_post_state_conformance_v4.7_exec_plan.md` | `VERIFIED`; conformance restored, `make rulebook-v2-check` and full rulebook suite passed | 2026-07-24 |
| Vehicle-yield illegal-entry latch cleanup decoupled from zone selection | `specifications/rulebook_v4.7_specification.md` §2.8.2 (read, not changed) | `implementation/vehicle_yield_illegal_entry_latch_cleanup_exec_plan.md` | `VERIFIED`; merged into `scenarionet-implementation` via rebase on 2026-07-25 after being developed in an isolated worktree; stale latch cleared independent of this-step zone selection; full rulebook suite (272 tests at merge time) and `make rulebook-v2-check` re-run and passed here | 2026-07-25 |
| Crosswalk illegal-entry latch never persisted when zone not selected | `specifications/rulebook_v4.7_specification.md` (crosswalk yield semantics, unchanged) | `implementation/crosswalk_illegal_entry_latch_cleanup_exec_plan.md` | `VERIFIED`; same bug class as the vehicle-yield latch-cleanup fix, found via full-component audit; crosswalk zones now cached, stale latch persistence fixed in `evaluate_crosswalk_yield`'s early-return path; crosswalk entry detection also switched to the swept front bumper for consistency (no demonstrable behavior change found, see plan §"Update 2026-07-25"); full rulebook suite (273 tests) and `make rulebook-v2-check` passed | 2026-07-25 |
| PG roundabout vehicle-yield priority data population | `specifications/rulebook_v4.7_specification.md` (`RoundaboutPriorityRecord`, unchanged; data-sourcing fix only) | `implementation/pg_roundabout_vehicle_yield_priority_exec_plan.md` | `VERIFIED`; `roundabout_priority_records` derived from live MetaDrive `Roundabout` block structure (never inferred from topology), wired into `generate_pg_scenario`; end-to-end verified through the real rulebook PG adapter (128 records, zero validation errors); `movement_priorities` (uncontrolled-intersection pairwise priority) confirmed infeasible without inventing data — MetaDrive `X`/`T` blocks are generated symmetric — and left as a documented permanent limitation, not implemented | 2026-07-25 |
| Rulebook scalarization v1.0 | `specifications/rulebook_scalarization_v1.0_specification.md` | `implementation/rulebook_scalarization_v1.0_exec_plan.md` | `IMPLEMENTATION IN PROGRESS` | 2026-07-17 |
| Rulebook scalarization v1.1, priority-weighted rank amendment | `specifications/rulebook_scalarization_v1.1_specification.md` | `implementation/rulebook_scalarization_v1.1_exec_plan.md` | `IMPLEMENTED`; `ADR-057` approved 2026-08-06; new default mode/base/compression wired end to end; focused suite (42 tests) and full repository suite (1297 passed, 47 pre-existing unrelated failures, zero new) pass; scoped ruff lint/format clean; `make smoke` not run (residual risk, not blocking `IMPLEMENTED`) | 2026-08-06 |
| Rulebook v2, R2/R3 cost-activation corrections | `specifications/rulebook_v4.10_specification.md` | `implementation/rulebook_v2_cost_activation_corrections_v1_exec_plan.md` | `IMPLEMENTED` for M1-M4 (off-road seam closing, ego-braking physical bound, RSS standstill applicability, `wrong_carriageway` sub-rule, PG/Waymo marking-coverage adapter fix); `M0` partial (GIF status overlay only, episode counters pending); `M5` (deferred solid-line deadband, signal persistence) not started; full rulebook suite (325 tests), full repository suite (except 9 pre-existing unrelated failures), and `make smoke` pass | 2026-08-01 |
| Transition-level replay v1 | `specifications/transition_replay_v1_specification.md` | `implementation/transition_replay_v1.0_exec_plan.md` | `IN_PROGRESS`; focused replay/config/persistence/boundary suite re-confirmed 2026-07-26 (26 passed); the 2026-07-17 fast-fail `make smoke` blocker did not reproduce in a bounded 90s attempt but a full uninterrupted smoke run is still required for `VERIFIED` | 2026-07-26 |
| Rulebook v2 catalog filter parallelization | Rulebook v2 §15.11; ScenarioNet v1 §17/§24 | `implementation/rulebook_v2_catalog_filter_parallelization_exec_plan.md` | `IMPLEMENTED` (this index row was stale — the ExecPlan itself has read `IMPLEMENTED` since 2026-07-16); re-verified 2026-07-26 (`6 passed`) | 2026-07-26 |
| ScenarioNet catalog build parallelization | Historical ScenarioNet v1 | `implementation/scenarionet_catalog_build_parallelization_exec_plan.md` | `IMPLEMENTED`; reconciliation under v1.1 pending | 2026-07-16 |
| ScenarioNet pipeline integrity and restructure v2 | Historical ScenarioNet v1 | `implementation/scenarionet_pipeline_restructure_v2_exec_plan.md` | `SUPERSEDED` by v1.1 planning | 2026-07-16 |
| Semantic observation and encoder v1 | Historical `specifications/observation_v1.1_specification.md`; `specifications/encoder_v1.0_specification.md` | `implementation/semantic_observation_encoder_v1_exec_plan.md` | `SUPERSEDED` for selected new semantic development by ADR-022; retain the reported v1.1 implementation record for reproducibility | 2026-07-21 |
| Causal semantic observation route-incompatible static feature crash | `specifications/observation_v1.1_specification.md`; `specifications/observation_v1.2_specification.md` (no change) | `implementation/causal_semantic_route_incompatible_static_feature_bugfix_exec_plan.md` | `VERIFIED`; fixed the 4 pre-existing `test_causal_semantic_batch.py` failures (missing vertical-incompatibility exception handling in the legacy V1.1 builder, plus a diagnostics-propagation ordering gap); realigned one stale test to the corrected contract; full non-integration suite (972 tests) passes with zero failures | 2026-07-26 |
| Semantic v3 actor-cap normalization fix and dead-code cleanup | `specifications/observation_v1.1_specification.md` §10.2 (amended 2026-07-26); `specifications/observation_v1.2_specification.md` (inherits, no change) | `implementation/semantic_v3_actor_cap_normalization_fix_exec_plan.md` | `VERIFIED`; found via a requested deep review of `obs=semantic_v3` against MetaDrive's default LiDAR observation; fixed a non-stationary relative-velocity normalization scale for VRU actors (pedestrians/cyclists never carry `configured_speed_cap_mps`, so the scale previously depended on the VRU's own instantaneous speed instead of falling back to `ego_speed_cap`); also removed an unreachable, already-drifted duplicate builder class (`PerceptionBoundedSemanticBatchBuilderV12`); focused suite (24 tests) and ruff lint pass; restart of the 3 active production runs under the corrected encoding is a separate pending decision, not yet actioned | 2026-07-26 |
| Rulebook v2 dynamic-candidate vertical-incompatibility crash (RSS-long, RSS-lat, crosswalk interactions) | `specifications/rulebook_v4.7_specification.md`; `specifications/rulebook_v4.8_specification.md` (no change) | `implementation/rulebook_v2_dynamic_candidate_vertical_incompatibility_crash_exec_plan.md` | `IMPLEMENTED`, live re-verification in progress; found live in production during 9 real `RUN_PROFILE=thesis` runs (5 of 9 crashed within ~15 minutes); fixed 3 unguarded `route.project` call sites for non-ego candidates (`_rss_candidates`, `_rss_lateral_candidates`, `_build_interactions` crosswalk loop), same design already established for static features; full regression suite (972 + 288 tests) passes; all 9 runs restarted with the fix | 2026-07-26 |
| PPO async-evaluation checkpoint load crash (rollout geometry mismatch) | `specifications/rl_baselines_v1_specification.md` (no change) | `implementation/ppo_eval_rollout_geometry_mismatch_crash_exec_plan.md` | `IMPLEMENTED`, live re-verification in progress; found live in production, all 3 of 3 PPO `RUN_PROFILE=thesis` runs crashed deterministically at their first async evaluation (eval env worker count differs from training's, tripping a rollout/batch-size divisibility check meant only for resume-training); added an optional `validate_rollout_geometry` gate, correctly threaded to skip the check at all 7 evaluation-only checkpoint-load sites while preserving it at the 2 genuine resume-training sites; full regression suite (972 tests) passes; all 3 PPO runs restarted with the fix | 2026-07-26 |
| Scenario ACL vectorized replay-buffer eviction race crash | `specifications/automatic_curriculum_learning_v1_specification.md` (no change) | `implementation/scenario_acl_replay_buffer_eviction_race_crash_exec_plan.md` | `IMPLEMENTED`, live re-verification in progress; found live in production (`td3-0` crashed at ~100k steps); a concurrent vectorized completion can legitimately evict a "replay" scenario's buffer record before its own long-running episode commits, which previously crashed the whole run; now logged as a distinct, auditable `skipped_evicted_before_commit` outcome instead of raising; no change to the approved eviction/ranking contract; full regression suite (972 + 77 tests) passes; `td3-0` restarted with the fix, other 8 unaffected processes left running undisturbed | 2026-07-26 |
| Scenario ACL exhausted-arm MAB starvation (FIND-001) | `specifications/automatic_curriculum_learning_v1.1_specification.md` REQ-002/REQ-003 (amended 2026-07-27 by ADR-028) | `implementation/scenario_acl_exhausted_arm_mab_starvation_exec_plan.md`; `decisions/ADR-028-scenario-acl-generate-eligibility-renormalization.md` | `IMPLEMENTED`; found live in `td3_sb3/seed_0/20260726_055617`, where `A4_vru`'s EMA score stayed bit-identical at `0.715461969165923` from MAB update 1420 to 3118 while holding the highest Generate probability (~0.336). Root cause was **frozen-pool exhaustion under the buffer-membership exclusion**, not ADR-024 quarantine (only 4 UIDs quarantined; 9501 resets produced 9496 committed episodes): `A4_vru`'s entire 333-record Waymo-only train pool (PG is a declared `structural_empty_cells`) had been absorbed by the scenario buffer, so every Generate draw on that arm silently degraded into a Replay, which never feeds the MAB — an absorbing state, since a high-LP arm's buffer records are the least likely to be evicted. **Systematic, not run-specific**: the three other completed ACL runs sat at 332, 224, and 207 of the same 333 records, and two currently live `sac-*-cmp` runs at 61 and 69. Fixed by restricting the Generate arm draw to arms with at least one fresh catalog record, renormalizing the softmax and the `eta/K` exploration floor over that eligible subset (`ScenarioArmBandit.probabilities(eligible_mask=...)`); an ineligible arm can no longer be drawn, degrade, or freeze. Neither of the design's two inspirations (Peng et al. 2024; Abouelazm et al. 2025) hits this case, since neither combines a semantic-arm MAB with a frozen finite per-arm pool. Approved 2026-07-27 (`DEC-EXH-001` option A, `DEC-EXH-002`); runs completed before the fix are kept for reporting with an explicit caveat that curriculum-arm-selection claims from them are not reliable (`DEC-EXH-003`); no dataset rebuild for now (`DEC-EXH-004`). Full repository suite (1024 tests) and focused ruff lint/format pass; `make smoke` and a live confirmation run not executed in this session. **Root cause removed 2026-07-29 by ADR-032/`DEC-008`**: the buffer-membership exclusion that created the absorbing state is gone, so ADR-028's eligibility renormalization is retained only as a residual safety net for the all-quarantined/all-in-flight case | 2026-07-27 |
| Perception-bounded semantic observation and encoder v1.2/v1.1 | `specifications/observation_v1.2_specification.md`; `specifications/encoder_v1.1_specification.md` | `implementation/perception_bounded_semantic_observation_v1.2_exec_plan.md` | `IMPLEMENTED`; M0–M6 complete, including canonical provider-backed `semantic_v3`/`lq_v3` training smoke | 2026-07-22 |
| Dynamic-slot identity removal (encoder) and context-quota ranking by distance (observation) | `specifications/encoder_v1.2_specification.md` (new, supersedes ENC-V1.1 for the LQ variant); `specifications/observation_v1.1_specification.md` §8.1 (amended 2026-07-26) | `implementation/semantic_v3_dynamic_ranking_and_slot_embedding_removal_exec_plan.md` | `VERIFIED`; follow-up to the actor-cap fix above, re-examining the two findings it had deferred/retracted after the user rejected a minimal-change framing and required literature-backed justification; `LatentQueryEncoderV3`'s per-slot dynamic identity embedding removed (spurious under sticky/first-fit slot assignment); non-conflict dynamic-actor ranking changed from lane-precedence to plain Euclidean distance (conflict-zone CPA/TTC ranking kept, validated by the same literature review); full rationale and citations in ADR-026; 39-test focused suite, ruff lint, and format-check pass; **operational incident**: `sac-0` and `ppo-0` (active production runs sharing this checkout) crashed on checkpoint reload mid-implementation (`Unexpected key(s) in state_dict: ...dynamic_slot_embedding.weight`) — user chose to leave them stopped and not preemptively intervene on `td3-0`; restart decision for all 3 runs remains open | 2026-07-26 |
| R1 injury-risk collision cost | `specifications/rulebook_v4.9_specification.md` (new, amends v4.7 §5.4/§5.6/§5.8) | `implementation/r1_injury_risk_collision_cost_v4.9_exec_plan.md` | `IMPLEMENTED`; found while reviewing whether the rulebook's internal normalizations are scientifically valid — R1 normalized the closing speed by the scenario's configured speed cap, so a 4 m/s residential impact scored 0.640 against 0.5625 for a 15 m/s highway impact, inverting real injury severity; replaced with the MAIS3+F closing-speed injury-risk logistic of Lubbe et al. (2022) (GIDAS 1999-2020), which is bounded in (0,1) by construction and makes costs comparable both across scenarios and across actor classes; coefficient transcription verified against the source's published 10%-risk anchors through a rounding-interval test; severity-inversion regression test added; 240-test rulebook suite, full 1008-test suite, and scoped ruff pass; `make smoke` not run (no training environment in this session) | 2026-07-26 |
| Sub-rule dominance and cost diagnostics | none amended; additive diagnostics under `specifications/evaluation_protocol_v1.0_specification.md` (`DEC-SUB-001` resolved: additive, no amendment) | `implementation/subrule_dominance_diagnostics_exec_plan.md` | `IMPLEMENTED`. Motivated by the same review as the R1 fix: R2/R3 aggregate heterogeneous sub-metrics by `max`, which Censi et al. Def. 16 licenses but which guarantees no cardinal comparability between them — an assumption that barely reaches the default scalarizer (boolean `I_k` gate) but is the full signal for a lexicographic learner optimizing `J_2` directly. New `subrule_diagnostics.py` (pure extractor + per-episode accumulator + seed-level aggregation, scoped to R2/R3 per `DEC-SUB-002`), wired into both `Agent.evaluate()` paths (sequential and parallel), a new `subrule_metrics.csv` (schema + emitters in `eval_loop.py`/`train_loop.py`, 5 call sites total), `aggregate_runs.py` wiring, 2 new diagnostic tables (`make_subrule_tables.py`) and 2 new diagnostic plots (`make_plots.py`, gated by the pre-existing `include_diagnostics` flag). `DEC-SUB-004` resolved by discovering the sibling `eval_episodes.csv` already carries a per-episode `source` column (`"pg"`/`"waymo"`, set at scenario-load time), reused directly rather than adding a new field. 10 new focused tests plus the 95 pre-existing agent/evaluate tests pass; full suite 1018/1018 (was 1008; +10, 0 changed); scoped ruff lint/format clean. Not run: `make analyze` end-to-end on a real multi-seed run (none exists in this environment) — verified instead by an isolated smoke test against a hand-built fixture CSV | 2026-07-26 |
| Ride-comfort and jerk diagnostics | none amended; additive diagnostics explicitly licensed by `specifications/rulebook_v5.1_specification.md` §13, which excludes comfort and jerk from the rulebook and the reward while permitting them to be logged as diagnostics only | `implementation/comfort_and_jerk_diagnostics_exec_plan.md` (`EP-COMFORT-DIAG`) | `IMPLEMENTED`. Closes a measurement gap rather than a modelling one: because §13 keeps comfort out of the reward, nothing pressured a policy toward smooth control and nothing measured whether it produced any. New `runtime/comfort_diagnostics.py` carries nuPlan's `ego_is_comfortable` set -- seven kinematic channels (longitudinal acceleration min/max, |lateral acceleration|, |jerk|, |longitudinal jerk|, |yaw rate|, |yaw acceleration|) at the published bounds `2.40/-4.05/4.89/8.37/4.13/0.95/1.93`, plus the conjunction boolean -- as a per-episode accumulator and a seed-level aggregator, wired into both `Agent.evaluate()` paths. The hard part already existed: `RuleRewardWrapper._extract_physical_acceleration` computes heading-projected acceleration on the correct `dt`, so only `ego_state["dt"]` had to be exported (`DEC-CMF-005`) and the derivatives taken. 9 new per-episode columns in `eval_episodes.csv` and 10 aggregate columns in `evals.csv`/`final_eval.csv` (18 writer sites), plus a separate `make_comfort_tables.py` diagnostic table -- deliberately **not** added to `FINAL_METRICS` (`DEC-CMF-004`), so `EVAL-PROTOCOL` v1.3's primary-metric contract is untouched. Scope is evaluation only (`DEC-CMF-002`, user-approved): `train_chunks.csv` is unchanged. 70 new tests; full suite 1598/1598 (was 1528); `make lint` clean, focused `make format-check` clean. Two bugs found and fixed under test. (1) The comfort verdict short-circuited on the first breached bound, reporting "uncomfortable" for episodes merely too short to measure. (2) More serious, and caught only by `make smoke`: the first implementation read `info["ego_state"]` from `RuleRewardWrapper`, which is the **legacy v1 reward path and is not in the live evaluation stack** -- the smoke exited 0 with all 87 episode rows empty. The source was re-decided onto the v2 monitor's post-transition `EnvSnapshot` (`sim_time_s`, `ego.velocity_xy`, `ego.heading_rad`), which is strictly better: the diagnostic and the rulebook now grade the identical state, and the derivative interval is measured simulation time rather than an assumed timestep. `envs/wrappers.py` is unchanged in the final diff. Re-verified: `make smoke` exit 0 with 87/87 episodes carrying a verdict and 0 excluded. **`DEC-CMF-006` (user-approved 2026-09-01): `scipy>=1.10,<2` added and the metric now reproduces nuPlan's filtered instrument**, with the seven bounds and all four Savitzky-Golay specs transcribed from the devkit and pinned by a test -- including that `extract_ego_yaw_rate` never forwards its `window_length`, so the yaw channels really run at window 5. This was not cosmetic: measured on the live env, longitudinal jerk reads **2.35 m/s^3** filtered against **63.3** unfiltered (factor ~27), so the previous version was measuring simulator noise and would have pinned `comfort_rate` at 0 for every arm. Two adaptations remain (`DEV-CMF-001/002`): acceleration is differentiated from velocity, which MetaDrive publishes instead of an acceleration, and the series is segmented at unusable steps so no window spans a gap. **`REQ-CMF-08`: the bounds now pass the repository's own Test A.** `scripts/measure_expert_rulebook_transition.py` -- the instrument behind `RULEBOOK-V5.1` §5.5 -- accumulates a comfort summary per record; measured on the identical panel (**1100 Waymo `train` records, 217,189 transitions, 0 skipped**) the logged expert scores an **expert comfort rate of 0.8973**, with per-channel violation rates at or below **5.73 %** and three channels at exactly 0.00 %. Against this repository's precedents (`rss` longitudinal rejected at 18.29 %, `speed_limit` adopted at 0.000 %) that is a clear pass, and it gives `comfort_rate` a human reference rather than an absolute one. Offline and online instruments share one definition: `ego_kinematics_payload` is called by both. Evidence and thesis-use guidance recorded in `audits/comfort_expert_admissibility_2026-09-01/README.md`. **Remaining caveat:** the jerk channels have a heavy right tail (`max_abs_lon_jerk` p95 4.86 against a worst of 62.5), most likely logged-track discontinuity in a few records, which biases the violation rates conservatively upward | 2026-09-01 |
| Checkpoint-manifest sidecar wiring | none (no spec/ADR change; additive diagnostic tooling) | `implementation/checkpoint_manifest_sidecar_wiring_exec_plan.md` | `VERIFIED`; found via investigation of the operational incident above — the entire pre-existing manifest compatibility contract (`CheckpointManifest`, `build_checkpoint_manifest`, `assert_checkpoint_compatible`) had zero production callers; wired a `<stem>.manifest.json` sidecar into `Agent.save()`/`load_planner()`, fail-open when absent (so `td3-0`/`sac-0`/`ppo-0`'s existing checkpoints stay resumable), scoped to a new shape-only field subset (excludes `git_commit`/`sb3_version`/`seed`/rulebook/scalarization fields, already covered elsewhere, to avoid spurious rejections on unrelated commits); 18-test focused suite, ruff lint, and format-check pass | 2026-07-26 |
| ScenarioNet-ACL training-path wiring gaps | `specifications/evaluation_protocol_v1.0_specification.md` (`REQ-014`/`DEC-014`) for the tracked-subset gap | `implementation/scenario_acl_path_wiring_gaps_exec_plan.md` | `DEFERRED` (documented, not implemented); `train_loop.py:791` returns early into `run_scenario_acl_training`, so wiring added after that line is dead for every production run. Gap A: `REQ-014` tracked-subset GIFs never render — no `panel_manifest_path` configured anywhere in `conf/`, no wiring in `driver.py`, and the asynchronous ACL periodic-eval path (`async_evaluation.py`) has no artifact-recording support at all, so `video.record_intermediate_evals` has no effect on ACL runs; a source-schedule mismatch (waymo-only periodic eval vs source-balanced frozen panel) must also be reconciled. Gap B: `agent.set_checkpoint_manifest` is missing at `driver.py:1493` (only the `set_checkpoint_identity` half is present), so the sidecar is never written for ACL runs — a one-line fix, `env` already in scope. Until Gap A is closed the thesis must not claim `REQ-014` conformance | 2026-07-27 |
| Scenario ACL | `specifications/automatic_curriculum_learning_v1_specification.md` | `implementation/scenario_acl_implementation_plan.md` | Internal stages v1/v2 reported complete; later stages incomplete/deferred | Date not declared in metadata |
| Scenario ACL deterministic vectorized execution | `specifications/automatic_curriculum_learning_v1_specification.md` §12/§28; `specifications/scenarionet_integration_v1.1_specification.md` §23 | `implementation/scenario_acl_vectorized_execution_v1_exec_plan.md` | `COMPLETED`; DEC-VEC-001--006 and resume restart policy approved by ADR-016; focused tests, real TD3/PPO fixture smokes, and checkpoint resume passed | 2026-07-20 |
| Subprocess worker failure diagnostics v1 | `specifications/scenarionet_integration_v1.1_specification.md` §23--§24/§27.2 | `implementation/subproc_worker_failure_diagnostics_v1_exec_plan.md` | `IMPLEMENTED`; worker failures now report command, slot, PID, exit code and remote Python traceback context without changing sampling or execution semantics | 2026-07-21 |
| Subprocess worker fail-fast v2 | `specifications/scenarionet_integration_v1.1_specification.md` §23--§24; `specifications/rl_baselines_v1_specification.md` §8; ADR-019 | `implementation/subproc_worker_fail_fast_v2_exec_plan.md` | `IMPLEMENTED`; readiness-based response collection surfaces any worker failure without waiting behind a slow slot, then reaps owned workers without changing sampling or execution semantics | 2026-07-21 |
| Route projection failure diagnostics v1 | `specifications/observation_v1.1_specification.md` §7.4/§10/§13.5; `specifications/scenarionet_integration_v1.1_specification.md` §17.2/§24 | `implementation/route_projection_failure_diagnostics_v1_exec_plan.md` | `IMPLEMENTED`; failure reports identify live actor/ego/route elevation relationships without changing projection or feature semantics | 2026-07-21 |
| Live training reliability v1 | `specifications/observation_v1.2_specification.md`; Rulebook v4.7 §7.6; ACL v1 §§12--13/§28; RL Baselines v1 | `implementation/live_training_reliability_v1_exec_plan.md` | `IN_PROGRESS`; static optional-map projection, PPO device/event parity, and ACL diagnostics now verified with a real container run (33 tests passed, ruff clean, 2026-07-26); remaining gap is a frozen-catalog scenario with an UNKNOWN pertinent signal (correctly fails closed by design) needing dataset curation/rebuild, out of scope for this session | 2026-07-26 |
| ScenarioNet ACL EMA selection v1.1 | `specifications/automatic_curriculum_learning_v1.1_specification.md` (`ACL-SN-EMA-001`) | `implementation/automatic_curriculum_learning_v1.1_exec_plan.md` | `APPROVED; implementation verified`; EMA arm scores, temperature sampling, 40/60 Generate/Replay, and LP-only replay; **superseded by v1.2 below** | 2026-07-23 |
| ScenarioNet ACL reward-scale normalization v1.2 | `specifications/automatic_curriculum_learning_v1.2_specification.md` (`ACL-SN-EMA-001`, `APPROVED`, supersedes v1.1); ADR-030, ADR-029 | `implementation/automatic_curriculum_learning_v1.2_exec_plan.md` (`ACL-SN-INIT-002`) | `IMPLEMENTED_PENDING_RERUN`; per-arm reward-scale EMA normalization of the LP fed to the shared rank window (removes a measured cross-arm reward-magnitude confound, `FIND-004`) and positive-part `max(delta,0)` TD3/SAC learning potential (aligns with PPO's existing ZPD filter); extended same-day (`DEC-007`) so replay-buffer retention/eviction priority also uses the reward-scale-normalized LP, not raw, since 60% of post-warm-up training time is Replay; checkpoint schema bumped `acl_ema_v1`→`acl_ema_v2`; `DEC-001` closed as uniform initialization (`MEAS-002` refuted the original per-arm boost premise); 102 focused tests pass (twice, before/after `DEC-007`), lint/format clean on touched files; repository-wide `make test`/`make config`/`make smoke` deferred until the three in-progress experiments (TD3/SAC/PPO seed_0) are stopped and restarted, since old checkpoints cannot resume under the new schema by design; known residual limitations: variance inflation is irreducible in this LP family (`LIM-002`), the `DEC-007` buffer call-site wiring has no dedicated closure-level test (`LIM-004`, code-reading + passing suite only) | 2026-07-27; **superseded by v1.3 below** |
| ScenarioNet ACL Generate/catalog decoupling v1.3 | `specifications/automatic_curriculum_learning_v1.3_specification.md` (`ACL-SN-EMA-001`, `v1.3`, `APPROVED` 2026-07-29, supersedes v1.2); ADR-032 | `implementation/automatic_curriculum_learning_v1.3_exec_plan.md` (`ACL-SN-CAT-003`) | `IMPLEMENTED_PENDING_RERUN` (implemented 2026-07-29, M1-M6 complete). Generate eligibility no longer depends on scenario-buffer membership (`DEC-008`): the `v1.1`/`v1.2` definition of a *fresh* record as "one the buffer does not hold" was a finite-catalog mistranslation of the reference methods, which draw exploration levels i.i.d. from the level space and never exclude buffer members (Abouelazm et al. 2025 §III-E.1). It caused `FIND-001` (arm absorbed by the buffer leaves the curriculum; mitigated by ADR-028, root cause removed here) and `FIND-005` (the bandit's per-arm feedback was drawn from each arm's low-LP residual, since the buffer retains high-LP records by construction, so `q_i` estimated the arm's LP conditioned on being below the eviction threshold; analytical, not measurable retrospectively, `LIM-006`). Adds per-arm coverage cycles (`DEC-009`, a declared project addition: at `n=m` draws i.i.d. sampling reaches 63% distinct records against 100% with cycles, on ~333 records per arm) and reduces `buffer_capacity` 1000 -> 250 (`DEC-010`, `RAT-011`: at 1000 of a 2,000-record train catalog the buffer admission rule degraded to "above the median"). Checkpoint schema bumps `acl_ema_v2`->`acl_ema_v3` and coverage state persists under a new `acl_coverage_v1` artifact (`scenario_coverage_state.json`, mandatory on resume); the in-progress experiments must be restarted, accepted by the user. Validation 2026-07-29: full suite PASS (1094 tests in the final run), `make lint` PASS, focused `make format-check` PASS, `make config`/`make config-gpu` PASS, and two ACL-path smoke runs (sequential and vectorized, 4 workers) PASS with `acl_coverage_v1`/`acl_ema_v3` artifacts written and every Generate draw distinct. Known limitations: `LIM-004` (`commit_event` closure has no isolated harness), `LIM-005` (`buffer_capacity` not ablated), `LIM-006` (`FIND-005` analytical only), `LIM-007` (cycle closure and the `generate_on_buffered_record` action need ~333 draws on one arm and are therefore not observable in a smoke-length run; pinned by `TEST-CAT-003`/`004`/`006`, live confirmation deferred to the first full re-run). Test-contract deviation `DEV-CAT-004`: four tests in `test_scenario_acl_arm_exhaustion.py` were rewritten to construct arm unavailability through quarantine/in-flight instead of buffer absorption, since they pinned the semantics `DEC-008` removes; no assertion weakened | 2026-07-29 |
| **ScenarioNet ACL outcome-based windowed learning progress v2.0** | `specifications/automatic_curriculum_learning_v2.0_specification.md` (`ACL-SN-EMA-001`, `v2.0`, **APPROVED** 2026-09-01, supersedes v1.3 in full); ADR-077 | `implementation/automatic_curriculum_learning_v2.0_exec_plan.md` (`ACL-PROG-004`) | **`AWAITING_DECISIONS`**. Specification approved and frozen 2026-09-01 before any comparison run, as SS11's pre-registration requires; the ExecPlan `ACL-PROG-004` is written and its only gate is `DEC-EP-002` (three mandatory tests pin the usefulness-ranked buffer semantics that `REQ-010`/`REQ-011` replace), which blocks `M4` only. **`M1` is deliberately separated and placed first**: it adds the per-episode level statistics and a per-training-episode sink that works with `curriculum=disabled`, because there is no such sink outside the ACL driver today and without it a uniform-sampling learnability run yields no data for `LIM-201`'s `SNR`. Revision 3 re-based the contract from Rulebook v4.7-v4.9's three macro rules onto `RULEBOOK-V5.1`'s six levels (`FIND-014`): five teacher dimensions `L1 -> L2 -> L3 -> T -> L5`, with `T` in L4's position and L4/L6 excluded (`RAT-203`), `ADR-071`'s at-fault classification driving `v_{1,e}`, `ADR-070`'s standstill gating removing stopped-ego steps from `N^app` on L2, and `ADR-063`'s demoted `rss` no longer reaching any teacher input. **No mechanism changed in the re-base**, so `FIND-010`/`FIND-012` remain valid. Implementation surface foreseen: per-slot accumulation of level costs/applicability in the vectorized loop (`agent.py`), a new `progress.py`, then `mab.py`, `buffer.py`, `ranking.py`, `record.py`, `selection.py`, `driver.py`, `config.py`, `conf/curriculum/scenario_acl.yaml`; schema `acl_ema_v3` -> `acl_progress_v1` with no migration, so runs must restart **Remaining gap:** Cannot be validated end to end until a run exists in which the policy measurably improves: the only `SNR` measurement (`FIND-013`, `~0.01`) was taken under the reward `RULEBOOK-V5.1` replaced and is **obsolete, not falsified**, so `H = 20` rests on the synthetic power sweep alone and is provisional (`LIM-201`). Known limitations carried: `LIM-209` (the `L3` dimension is not the same quantity across arms -- on PG four of six L3 sub-rules never apply, and the arms are source-correlated), `LIM-210` (not-at-fault truncations enter `T` unmodelled, by explicit user decision `DEC-207`), `LIM-203` (visit-frequency/transfer confound, monitored via the `Ds` diagnostic) | 2026-09-01 |
| Source-bounded reactive traffic v1 | `specifications/scenarionet_integration_v1.1_specification.md` §17/§23--24; ADR-021 | `implementation/source_bounded_reactive_traffic_v1_exec_plan.md` | `IMPLEMENTED`; approved reactive traffic remains active only while the source track state is valid; focused lifecycle regression passed | 2026-07-21 |
| Deterministic parallel evaluation and final test | `specifications/rl_baselines_v1_specification.md`; `protocols/live_eval_video_protocol.md` | `implementation/parallel_evaluation_test_v1_exec_plan.md` | `IMPLEMENTED`; spawned validation/test workers, ordered metric reduction, ScenarioNet/ACL sequence preservation, live video integration, focused tests, full suite, and end-to-end smoke passed | 2026-07-21 |
| Asynchronous evaluation queue v1 | `specifications/rl_baselines_v1_specification.md` | `implementation/asynchronous_evaluation_v1_exec_plan.md` | `VERIFIED` (this index row was stale — the ExecPlan itself has read `VERIFIED` since 2026-07-21); ordinary and ACL diagnostic evaluation queued against immutable snapshots; staged evaluation and final test remain synchronous/separate; re-verified 2026-07-26 (`23 passed`) | 2026-07-26 |
| Evaluation video diagnostics v1 | `specifications/rl_baselines_v1_specification.md`; Rulebook v4.7; candidate live video protocol | `implementation/evaluation_video_diagnostics_v1_exec_plan.md` | `IMPLEMENTED` (this index row was stale — the ExecPlan itself has read `IMPLEMENTED` since 2026-07-21); shared live/replay annotation, compact panel, and exact route/target/actor overlays implemented; re-verified 2026-07-26 (30 tests passed); only a full-backend visual smoke (`make smoke`, a real training run) remains open, excluded from this session | 2026-07-26 |
| ScenarioNet integration v1 | Historical `specifications/scenarionet_integration_v1_specification.md` | `implementation/scenarionet_integration_implementation_plan.md` | `SUPERSEDED`; retain for traceability | 2026-07-15 |
| ScenarioNet integration v1.1 | `specifications/scenarionet_integration_v1.1_specification.md` | `implementation/scenarionet_integration_spec_v1.1_exec_plan.md` | `IN_PROGRESS` overall (M4/M5 open-ended catalog scaling remains unfinished), but the actual working dataset is real and verified: a frozen, versioned 3,500-record selection (1,750 Waymo + 1,750 PG) at `data/scenarionet/frozen/scenario_selection_index.json` (`created_at` 2026-07-23), re-verified 2026-07-26 with `replay_frozen_dataset.py --verify-only` (all 3,500 source files present) and cross-checked against `catalog/split_report.json` (`total_arm_deficit: 0`, `balance_total_arm_deficit: 0`) — this dataset is not a blocker for running experiments; §16.1 arm-difficulty constant provenance now recorded by ADR-028 (coverage-driven heuristic, not statistically calibrated; visual audit `ISS-020` and source/arm confounding remain separate open items) | 2026-07-27 |
| ScenarioNet existing-source rebuild | `specifications/scenarionet_integration_v1.1_specification.md` | `implementation/scenarionet_existing_source_rebuild_exec_plan.md` | `IMPLEMENTED`; full no-cache eligibility revalidation, split/runtime rebuild, and frozen-index recreation target added | 2026-07-20 |
| ScenarioNet physical-road termination regression | `specifications/scenarionet_integration_v1.1_specification.md` | `implementation/out_of_road_boundary_termination_bugfix_exec_plan.md` | `IN_PROGRESS`; the 2026-07-21 live run exposed a missing-contact false negative after the prior boundary-only false-positive fix | 2026-07-21 |
| Scalar autonomous-driving pipeline audit and completion | Authoritative ScenarioNet v1.1, ACL v1 §28, Rulebook v4.7, scalarization v1.0, observation v1.1, encoder v1.0, and transition replay v1.0 | `implementation/scalar_autonomous_driving_pipeline_audit_exec_plan.md` | `VERIFIED` for the final smoke integration gate; canonical read-only PG/Waymo, Rulebook/scalarization, semantic `(2541,)`/LQ, TD3/PER/n=3, ACL generation/replay, checkpoint/replay/RNG persistence and resume, and final evaluation passed. Full thesis-length/statistical validation remains out of scope; SAC was not required by the final TD3 configuration. | 2026-07-19 |
| Documentation structure | User instructions dated 2026-07-16 | `implementation/repository_documentation_restructure_exec_plan.md` | `VERIFIED` | 2026-07-16 |
| Repository quality commands | User process decision dated 2026-07-16 | `implementation/repository_quality_commands_exec_plan.md` | `VERIFIED` | 2026-07-16 |
| PyTorch cross-architecture compatibility | User request dated 2026-07-19; ADR-013 | `implementation/pytorch_cross_architecture_compatibility_exec_plan.md` | `IN_PROGRESS`; approved 2.9.1 pin and ARM64 CUDA validator exception; ARM64 GPU host validation (build, import, CUDA tensor smoke) now fully passes (2026-07-26, previously blocked by external GPU memory pressure); only the x86_64/RTX 50xx host validation remains open, no such host available in this session | 2026-07-26 |
| Geometry abort and loud quarantine | Deviates from `RSA-V1` `REQ-RSA-001` and ADR-024 by user approval (`DEV-GA-001`, 2026-09-06), which recover only enumerated *data* reasons and declare numerical failures fatal | `implementation/geometry_abort_quarantine_v1_exec_plan.md` | `IMPLEMENTED` for both the training and evaluation paths; a typed geometry error disjoint from the data-abort type over a closed three-reason set, refusing construction without its measured magnitude, its own worker marker, its own forensic JSONL carrying the offending polygon's WKT, and a ceiling (3 consecutive in a batch, or 1 % of episodes) that fails the run when the condition is systemic; scenario quarantine deferred to 3 repeats on one UID because the condition is policy-dependent. Motivated by `open_items` `C9`, which killed both `AB-LEARN` screening arms. **Remaining**: the two thresholds are module constants rather than Hydra configuration, the ledger is not persisted across resume, and `C7`'s vectorized-path limitation is inherited unchanged | 2026-09-06 |
| Runtime scenario data-abort v1 | Rulebook v4.7 RSA-1; Transition Replay v1 RSA-1; ACL v1 RSA-1; RL Baselines v1 RSA-1; ScenarioNet v1.1 RSA-1; ADR-024 | `implementation/runtime_scenario_data_abort_v1_exec_plan.md` | `IMPLEMENTED`; typed data-abort, PER boundary closure, a prefix-preserving PPO/GAE boundary (`MaskedRolloutBuffer`, no more full-rollout discard), evaluation-path exclusion, ACL/non-ACL quarantine persistence across resume, and forensic JSONL wiring implemented and tested (buffer-, backend-, and loop-level, including a real SB3 PPO backend test); uniform replay remains deliberately fatal pending a larger safe N-step mutation; representative smoke not yet run | 2026-07-23 |
| Evaluation and algorithm comparison protocol v1.0 | `specifications/evaluation_protocol_v1.0_specification.md`, `EVAL-PROTOCOL` v1.0 | `implementation/evaluation_protocol_v1.0_exec_plan.md` | `IN_PROGRESS`; Milestones 1--6, 8, 9, 10 complete, live- or unit-verified (incl. Milestone 5's PPO atomic-boundary/`DEC-015` overshoot path live-verified against a real SB3 PPO run, Milestone 2's panel manifest generated against the real ScenarioNet catalog and hash-recorded in run metadata, Milestone 9's tracked-subset feature-diversity GIF selection/rendering and category-taxonomy reconciliation, Milestone 10's applicability-aware R1--R3 seed-level aggregation for `REQ-008`); `REQ-016` full end-to-end analysis regeneration against real aggregated artifacts not run; the 4 pre-existing `test_causal_semantic_batch.py` failures noted in the 2026-07-25 run are fixed (see the causal-semantic bugfix ExecPlan below); full non-integration suite now passes with zero failures (972 tests, 2026-07-26) | 2026-07-25 |
| Evaluation video ego-trail and planned-checkpoint overlay v1 | `decisions/ADR-020-evaluation-video-diagnostics.md` (amended by ADR-043); additive, no dedicated specification | `implementation/evaluation_video_route_and_ego_trail_overlay_v1_exec_plan.md` | `IMPLEMENTED`; user asked whether the evaluation GIF overlay could show the planned route/checkpoints and the ego's actually-traveled path; found the existing `route_past`/`route_future` already draw the planned route (from a frozen `assigned_route_lane_ids` sequence, not MetaDrive's unused live `navigation.checkpoints`) but no discrete checkpoint markers or ego-history trail existed; ADR-020 explicitly excluded "ego-history lines," making the trail a specification deviation requiring approval (`DEC-001`), approved 2026-07-31 and recorded by `decisions/ADR-043-evaluation-video-ego-trail-and-checkpoint-overlay.md`; implemented an additive `RoutePolyline.lane_start_points_xyz` field for discrete checkpoint markers and a per-episode/per-worker-slot ego-trail buffer (toggleable via new `video.topdown.draw_ego_trail`, default on); 61+3 focused tests pass, scoped ruff lint/format-check clean (one pre-existing unrelated `agent.py` format-baseline item confirmed via `git stash`), `make config` clean; a broader filtered regression run surfaced 4 pre-existing, unrelated `test_eval_artifacts.py` failures (confirmed via `git stash` baseline comparison) | 2026-07-31 |
| Route-projection hotspot (F8) v1 | Rulebook v4.7 §2.9.2 and §2.9.4, which allow an accelerated selection whose result is equivalent; `docs/open_items.md` `F8`; no scientific behaviour changes | `implementation/route_projection_hotspot_f8_v1_exec_plan.md` | `IMPLEMENTED`; `RoutePolyline.project` vectorised with NumPy (equivalence to the pre-fix Python algorithm tested at a declared 1e-9 m tolerance with identical segment choice, `DEC-F8-001`, on synthetic routes and frozen validation-panel records), the drivable/carriageway vertical gate decided from the centerline z-range with a 1e-6 m guard band, and the full-footprint road-exit classification memoised per `(adapter, ego pose)`; measured on the profiled Waymo case without profiler: 1135 → 312 ms/step (3.6×), returns identical to every digit; adversarial review recorded in the plan | 2026-09-08 |
| Step timing instrumentation v1 | `decisions/ADR-044-step-timing-instrumentation.md`; additive, no dedicated specification | `implementation/step_timing_instrumentation_v1_exec_plan.md` | `IMPLEMENTED`; user asked how to log FPS plus a per-component average step-cost breakdown (seconds/percentages) across observation, policy inference, env step, Rulebook, gradient update, IPC/vec-env sync, and eval-time GIF rendering, for cross-algorithm comparison; found FPS and the full per-component `phase_seconds` breakdown were already measured in `agent.py`'s rollout loop but dropped before reaching any CSV (only reached the JSONL run event log); persisted it as a new tidy `step_timing.csv` (one row per `(run_id, chunk_id, component)`, avoiding a fixed-width schema against the dynamic Rulebook/learner-detail key set, `DEC-001`), added a derived `env_step_ipc_overhead` component, and added new isolated GIF-render-timing columns on `evals.csv` (`DEC-002`); replay-buffer-write isolation from ACL bookkeeping deferred (`DEC-003`); all three decisions recorded by `decisions/ADR-044-step-timing-instrumentation.md`; 42 focused tests pass, scoped ruff lint/format-check clean (two pre-existing unrelated `agent.py`/`train_loop.py` format-baseline items confirmed via `ruff format --diff` showing only untouched regions); a broader filtered regression run surfaced the same 4 pre-existing `test_eval_artifacts.py` failures as the prior session plus one unrelated pre-existing `test_run_metadata.py` failure (confirmed untouched by `git diff --stat`); two real `make smoke` runs (default and `env.vectorized.enabled=true`) confirmed `step_timing.csv`/`evals.csv` are correctly populated end-to-end and, on the vectorized run, surfaced a real gap where `evals.csv` is written from 7 sites repository-wide, not the 4 in `train_loop.py` found on the first pass -- `final_panels.py`, `eval_loop.py`, and 2 sites in `curriculum/scenario_acl/driver.py` were missed and fixed | 2026-08-01 |

## Maintenance Rules

1. Register one authoritative specification for each selected feature version.
2. Record path, stable ID when available, version, approval evidence, status,
   related ADRs, and ExecPlan.
3. Never infer authority or supersession from a filename, date, or larger version.
4. Preserve historical documents needed for traceability.
5. Keep specification status distinct from implementation status.
6. `VERIFIED` requires mandatory validation and final reconciliation.
7. Templates guide future documents; they do not invalidate an approved legacy
   specification solely because its structure differs.
8. Update this index when authority, version, implementation status, or an
   applicable ADR changes.
